(()=>{const dialog=document.querySelector('[data-inspection-dialog]');if(!dialog)return;const form=dialog.querySelector('[data-inspection-form]'),label=dialog.querySelector('[data-inspection-object]');document.querySelectorAll('[data-inspection-schedule]').forEach(button=>button.addEventListener('click',()=>{form.action='/pilot/objects/'+button.dataset.objectId+'/inspection-schedule';label.textContent=button.dataset.objectLabel;dialog.showModal()}));dialog.querySelectorAll('[data-inspection-close]').forEach(button=>button.addEventListener('click',()=>dialog.close()));dialog.addEventListener('cancel',()=>dialog.close());})();
