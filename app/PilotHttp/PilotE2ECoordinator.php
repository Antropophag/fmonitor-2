<?php
declare(strict_types=1);

namespace FMonitor2\PilotHttp;

use FMonitor2\InstallationProcess\ArtifactUnavailableException;
use FMonitor2\InstallationProcess\ArtifactIntegrityException;
use FMonitor2\InstallationProcess\ArtifactNotFoundException;
use FMonitor2\InstallationProcess\Clock;
use FMonitor2\InstallationProcess\ContentAddressedArtifactStore;
use FMonitor2\InstallationProcess\ProductionInstallationProcessConfig;
use FMonitor2\InstallationProcess\ProductionInstallationProcessFactory;
final class InvalidCsrfRequest extends \RuntimeException {}

/** Configured production HTTP command adapter for PILOT-E2E-FLOW-001. */
final class PilotE2ECoordinator extends PilotHttpCoordinator
{ private string $cspMethod='GET';private string $cspPath='';
    public function __construct(
        private readonly PilotHttpCoordinator $reads,
        private readonly TrustedServerIdentity $identity,
        private readonly ProductionPilotHttpDependencies $dependencies,
        private readonly ObjectCardRenderer $cards,
        private readonly ObjectListRenderer $lists,
        private readonly PrepareFormRenderer $forms,
        private readonly ProductionChecklistRenderer $checklists,
        private readonly ?\FMonitor\IdentityAccess\PilotSessionStorage $sessionStorage = null,
        private readonly ?EnvironmentSource $sessionEnvironment = null,
    ) { parent::__construct($identity,new ProductionPilotShellRenderer(),$dependencies); }

    public function handle(PilotHttpRequest $r):PilotHttpResponse
    {
        $this->cspMethod=$r->method;$this->cspPath=$r->path;$sessionRoute=\in_array($r->path,['/pilot/login','/pilot/admin/users','/pilot/admin/users/invite'],true);if($this->sessionStorage!==null&&$sessionRoute)return $this->sessionResponse($r);$canonicalLocalRead=\in_array($r->method,['GET','HEAD'],true)&&($r->path==='/pilot/objects'||\preg_match('#^/pilot/objects/[1-9][0-9]*/assignment-order/prepare$#D',$r->path)===1);if($canonicalLocalRead||!$this->dependencies->pilotUiConfigured()||!$this->dependencies->prepareCommandConfigured()||!$this->dependencies->e2eConfigured())return $this->reads->handle($r);
        if($r->path==='/pilot/users')return $this->redirect('/pilot/admin/users');
        if(\preg_match('#^/pilot/construction-control/objects/([1-9][0-9]*)/sync-context$#D',$r->path,$syncMatch)===1&&self::positive($syncMatch[1]))return $this->syncContext($r,(int)$syncMatch[1]);
        if(\preg_match('#^/pilot/(?:objects|construction-control/objects)/([1-9][0-9]*)/checklist(?:/operations|/photos)?$#D',$r->path,$checklistMatch)===1&&self::positive($checklistMatch[1]))return $this->checklist($r,(int)$checklistMatch[1],\str_starts_with($r->path,'/pilot/construction-control/'));
        if(\in_array($r->path,['/pilot/admin/users','/pilot/admin/roles','/pilot/admin/users/invite'],true)||\preg_match('#^/pilot/admin/users/([1-9][0-9]*)/roles(?:/([1-9][0-9]*))?$#D',$r->path,$userRoleMatch)===1)return $this->users($r,$userRoleMatch??[]);
        if($r->path==='/pilot/installers'){
            if(!\in_array($r->method,['GET','HEAD'],true))return $this->response(405,"Method not allowed.\n",['Allow'=>'GET, HEAD'],$r->method);
            try{$principal=$this->identity->resolve($r->serverIdentity);}catch(InvalidServerIdentity){return $this->response(401,"Authentication required.\n",[],$r->method);}
            try{$this->dependencies->css()->readBytes();$this->dependencies->pilotCss()->readBytes();$user=$this->dependencies->users()->resolveActiveUser($principal);if($user===null||!$this->dependencies->hasCapability($user->id,AccessPolicy::INSTALLERS_READ))return $this->response(403,"Access denied.\n",[],$r->method);\parse_str((string)($r->server['QUERY_STRING']??''),$query);if(!\is_array($query))$query=[];$filters=['q'=>\trim((string)($query['q']??'')),'status'=>(string)($query['status']??''),'availability'=>(string)($query['availability']??''),'page'=>(int)($query['page']??1)];if(\mb_strlen($filters['q'])>120||!\in_array($filters['status'],['','employed','dismissed'],true)||!\in_array($filters['availability'],['','assigned','free'],true)||$filters['page']<1)return $this->response(400,"Invalid filters.\n",[],$r->method);$html=(new ProductionInstallerDirectoryRenderer())->render($user,$this->dependencies->installerDirectory()->read($this->dependencies->processDate(),$filters));return $this->response(200,$html,['Content-Type'=>'text/html; charset=UTF-8'],$r->method);}catch(\Throwable){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}
        }
        $route=$this->route($r->path);$cardId=null;if(\preg_match('#^/pilot/objects/([1-9][0-9]*)$#D',$r->path,$m)===1&&self::positive($m[1]))$cardId=(int)$m[1];
        if($route===null&&$cardId===null&&!\in_array($r->path,['/pilot/objects','/pilot/construction-control'],true))return $this->reads->handle($r);
        if($route===null){if(!\in_array($r->method,['GET','HEAD'],true))return $this->response(405,"Method not allowed.\n",['Allow'=>'GET, HEAD'],$r->method);try{$principal=$this->identity->resolve($r->serverIdentity);}catch(InvalidServerIdentity){return $this->response(401,"Authentication required.\n",[],$r->method);}try{$this->dependencies->css()->readBytes();$this->dependencies->pilotCss()->readBytes();$user=$this->dependencies->users()->resolveActiveUser($principal);if($user===null)return $this->response(403,"Access denied.\n",[],$r->method);if($r->path==='/pilot/construction-control'&&!$this->dependencies->hasCapability($user->id,AccessPolicy::CONSTRUCTION_CONTROL_READ))return $this->response(403,"Access denied.\n",[],$r->method);if($r->path==='/pilot/construction-control')return $this->constructionControl($r,$user);return $cardId===null?$this->queue($r,$user):$this->card($r,$cardId,$user);}catch(\Throwable){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}}
        $allow=$route['kind']==='prepare'?'GET, HEAD, POST':($route['kind']==='artifact'?'GET, HEAD':'POST');
        if(!\in_array($r->method,\explode(', ',$allow),true))return $this->response(405,"Method not allowed.\n",['Allow'=>$allow],$r->method);
        try{$principal=$this->identity->resolve($r->serverIdentity);}catch(InvalidServerIdentity){return $this->response(401,"Authentication required.\n",[],$r->method);}
        try{
            $this->dependencies->css()->readBytes();$this->dependencies->pilotCss()->readBytes();
            $user=$this->dependencies->users()->resolveActiveUser($principal);if($user===null)return $this->response(403,"Access denied.\n",[],$r->method);
            $cap=match($route['kind']){'prepare','engineer'=>'assignment_order.prepare','registration'=>'assignment_order.confirm_registration','open'=>'installation.open','artifact'=>null};
            if($cap!==null&&!$this->dependencies->hasCapability($user->id,$cap))return $this->response(403,"Access denied.\n",[],$r->method);
            if($route['kind']==='artifact')return $this->artifact($r,$route,$user);
            if($r->method==='POST'){try{return $this->command($r,$route,$user);}catch(\LogicException $e){return $this->commandLogicException($r,$route,$user,$e);}}
            return $this->preparePage($r,$route['id'],$user);
        }catch(CssAssetUnavailable|PilotHttpInfrastructureUnavailable){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}
        catch(\Throwable){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}
    }

    private function checklist(PilotHttpRequest $r,int $objectId,bool $fromConstructionControl=false):PilotHttpResponse
    {
        $isPage=\str_ends_with($r->path,'/checklist');$isPhoto=\str_ends_with($r->path,'/photos');$allow=$isPage?'GET, HEAD':'POST';if(!\in_array($r->method,\explode(', ',$allow),true))return $this->response(405,"Method not allowed.\n",['Allow'=>$allow],$r->method);
        try{$principal=$this->identity->resolve($r->serverIdentity);}catch(InvalidServerIdentity){return $this->response(401,"Authentication required.\n",[],$r->method);}
        try{
            $this->dependencies->css()->readBytes();$this->dependencies->pilotCss()->readBytes();$user=$this->dependencies->users()->resolveActiveUser($principal);if($user===null)return $this->response(403,"Access denied.\n",[],$r->method);$card=$this->dependencies->objectCards()->read($objectId);if($card===null)return $this->response(404,"Not found.\n",[],$r->method);
            [$db,$prefix,,$root,$now]=$this->dependencies->commandResources();$sync=new ChecklistSync($db,$prefix,$root,$now);$sync->ensureSchema();$roleAccess=$this->dependencies->canEditChecklist($user->id);$allowed=(bool)$card['opened']&&($roleAccess||(int)($card['controlEngineer']['userId']??0)===$user->id);
            if($isPage){[$session,$headers]=$this->session($r,$user,true);$csrf=$this->token($session,$user,$objectId);return $this->response(200,$this->checklists->render($user,$card,$roleAccess,$sync->projection($objectId),$csrf,$fromConstructionControl),['Content-Type'=>'text/html; charset=UTF-8']+$headers,$r->method);}
            if($isPhoto&&!$allowed)return $this->json(403,['status'=>'rejected','message'=>'Недостаточно прав для изменения чек-листа.']);
            $declaredLength=$r->server['CONTENT_LENGTH']??null;if(!\is_string($declaredLength)||!\ctype_digit($declaredLength)||(int)$declaredLength>($isPhoto?5*1024*1024:32768))return $this->json(413,['status'=>'rejected','message'=>'Размер операции превышает допустимый.']);$r=new PilotHttpRequest($r->method,$r->path,$r->host,$r->serverIdentity,$r->server,(string)\file_get_contents('php://input'));if((int)$declaredLength!==\strlen($r->body))return $this->json(400,['status'=>'rejected','message'=>'Некорректный размер операции.']);[$session]=$this->session($r,$user,false);$csrf=(string)($r->server['HTTP_X_FM2_CSRF']??'');if($session===null||!$this->validChecklistRequest($r,$session,$user)||!$this->validToken($session,$csrf,$user,$objectId))return $this->json(403,['status'=>'rejected','message'=>'Сеанс проверки истёк. Обновите страницу.']);
            if($isPhoto){$metadata=$r->server['HTTP_X_FM2_OPERATION']??'';if(!\is_string($metadata)||\strlen($metadata)>4096)return $this->json(400,['status'=>'rejected','message'=>'Некорректные данные фотографии.']);$operation=\json_decode(\base64_decode($metadata,true)?:'',true,16,JSON_THROW_ON_ERROR);if(!\is_array($operation))return $this->json(400,['status'=>'rejected','message'=>'Некорректные данные фотографии.']);$result=$sync->accept($objectId,$user,$operation,$r->body);}
            else{if(!\preg_match('#^application/json(?:;\s*charset=UTF-8)?$#iD',(string)($r->server['CONTENT_TYPE']??''))||\strlen($r->body)>32768)return $this->json(400,['status'=>'rejected','message'=>'Некорректный формат операции.']);$operation=\json_decode($r->body,true,16,JSON_THROW_ON_ERROR);if(!\is_array($operation))return $this->json(400,['status'=>'rejected','message'=>'Некорректный формат операции.']);if(!$allowed&&((string)($operation['type']??'')!=='item_completed'||!$user->can('inspection.item.complete')))return $this->json(403,['status'=>'rejected','message'=>'Недостаточно прав для изменения чек-листа.']);$result=$sync->accept($objectId,$user,$operation);}
            $result['projection']=$sync->projection($objectId);return $this->json(\in_array($result['status'],['accepted','duplicate'],true)?200:($result['status']==='conflict'?409:422),$result);
        }catch(\JsonException){return $this->json(400,['status'=>'rejected','message'=>'Некорректные данные операции.']);}catch(CssAssetUnavailable|PilotHttpInfrastructureUnavailable){return $this->json(503,['status'=>'retryable','message'=>'Сервис временно недоступен.']);}catch(\Throwable){return $this->json(503,['status'=>'retryable','message'=>'Сервис временно недоступен.']);}
    }

    private function syncContext(PilotHttpRequest $r,int $objectId):PilotHttpResponse
    {
        if(!\in_array($r->method,['GET','HEAD'],true))return $this->response(405,"Method not allowed.\n",['Allow'=>'GET, HEAD'],$r->method);
        try{$principal=$this->identity->resolve($r->serverIdentity);}catch(InvalidServerIdentity){return $this->response(401,"Authentication required.\n",[],$r->method);}
        try{$user=$this->dependencies->users()->resolveActiveUser($principal);if($user===null)return $this->response(403,"Access denied.\n",[],$r->method);$card=$this->dependencies->objectCards()->read($objectId);if($card===null)return $this->response(404,"Not found.\n",[],$r->method);$allowed=(bool)$card['opened']&&($this->dependencies->canEditChecklist($user->id)||(int)($card['controlEngineer']['userId']??0)===$user->id||$user->can('inspection.item.complete'));if(!$allowed)return $this->json(403,['status'=>'rejected']);[$db,$prefix,,$root,$now]=$this->dependencies->commandResources();$sync=new ChecklistSync($db,$prefix,$root,$now);$sync->ensureSchema();[$session,$headers]=$this->session($r,$user,true);$body=\json_encode(['csrf'=>$this->token($session,$user,$objectId),'revision'=>(int)$sync->projection($objectId)['revision']],JSON_THROW_ON_ERROR);return $this->response(200,$body,['Content-Type'=>'application/json; charset=UTF-8']+$headers,$r->method);}catch(\Throwable){return $this->json(503,['status'=>'retryable']);}
    }

    private function users(PilotHttpRequest $r,array $route):PilotHttpResponse
    {
        $isInvite=$r->path==='/pilot/admin/users/invite';$isCommand=$route!==[]||$isInvite;$allow=$isCommand?'POST':'GET, HEAD';if(!\in_array($r->method,\explode(', ',$allow),true))return $this->response(405,"Method not allowed.\n",['Allow'=>$allow],$r->method);
        try{$principal=$this->identity->resolve($r->serverIdentity);}catch(InvalidServerIdentity){return $this->response(401,"Authentication required.\n",[],$r->method);}
        try{$this->dependencies->css()->readBytes();$this->dependencies->pilotCss()->readBytes();$actor=$this->dependencies->users()->resolveActiveUser($principal);if($actor===null||!$this->dependencies->hasCapability($actor->id,AccessPolicy::ADMINISTER_ACCESS))return $this->response(403,"Access denied.\n",[],$r->method);[$db,$prefix,,,$now]=$this->dependencies->commandResources();$directory=new MariaDbPilotUserDirectory($db,$prefix);
            if(!$isCommand){$data=$directory->read();if($r->path==='/pilot/admin/roles'){$html=(new ProductionUserDirectoryRenderer())->renderRoles($actor,$data);return $this->response(200,$html,['Content-Type'=>'text/html; charset=UTF-8'],$r->method);}[$session,$headers]=$this->session($r,$actor,true);$tokens=[];foreach($data['users']as$user)$tokens[$user['id']]=$this->token($session,$actor,$user['id']);$html=(new ProductionUserDirectoryRenderer())->renderUsers($actor,$data,$tokens);return $this->response(200,$html,['Content-Type'=>'text/html; charset=UTF-8']+$headers,$r->method);}
            $r=new PilotHttpRequest($r->method,$r->path,$r->host,$r->serverIdentity,$r->server,(string)\file_get_contents('php://input'));[$session]=$this->session($r,$actor,false);if($session===null||!$this->validRequest($r,$session,$actor))return $this->response(403,"Invalid request.\n");if($isInvite){try{$fields=$this->body($r,['csrfToken','email','fullName']);}catch(InvalidCsrfRequest){return $this->response(403,"Invalid request.\n");}if($fields===null||!$this->consume($session,$fields['csrfToken'][0]??'',$actor,$actor->id))return $this->response(403,"Invalid request.\n");$created=$directory->inviteUser($fields['email'][0]??'',$fields['fullName'][0]??'',$actor->id,$now);if($created===null)return $this->response(400,"Bad request.\n");$url='/pilot/activate?token='.$created['token'];return $this->response(201,"Invitation: {$url}\n",['Content-Type'=>'text/plain; charset=UTF-8'],$r->method);}$userId=(int)$route[1];$specificRole=isset($route[2])&&$route[2]!=='';try{$fields=$this->body($r,$specificRole?['csrfToken','action']:['csrfToken','action','roleId']);}catch(InvalidCsrfRequest){return $this->response(403,"Invalid request.\n");}if($fields===null||!$this->consume($session,$fields['csrfToken'][0]??'',$actor,$userId))return $this->response(403,"Invalid request.\n");$action=$fields['action'][0]??'';$roleId=(int)($specificRole?$route[2]:($fields['roleId'][0]??0));if($roleId<1||!$directory->changeRole($userId,$roleId,$action,$actor->id,$now))return $this->response(400,"Bad request.\n");return $this->redirect('/pilot/admin/users');
        }catch(PilotHttpInfrastructureUnavailable|CssAssetUnavailable){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}catch(\Throwable){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}
    }

    public function card(PilotHttpRequest $r,int $id,HttpUser $user):PilotHttpResponse
    {
        $card=$this->dependencies->objectCards()->read($id);if($card===null)return $this->response(404,"Not found.\n",[],$r->method);
        $session=null;$headers=[];$capPrepare=$this->dependencies->hasCapability($user->id,'assignment_order.prepare');$capRegister=$this->dependencies->hasCapability($user->id,'assignment_order.confirm_registration');$capOpen=$this->dependencies->hasCapability($user->id,'installation.open');
        [$existingSession]=$this->session($r,$user,false);$flash=$existingSession===null?null:$this->pullFlash($existingSession,$r->path);if(($flash['suppressOpen']??false)===true)$capOpen=false;
        $card['engineers']=$capPrepare?$this->engineers():[];$card['canAssignEngineer']=$capPrepare;
        $needsCommand=$capPrepare||($card['order']!==null&&$card['order']['status']==='prepared'&&$capRegister)||($card['order']!==null&&$card['order']['status']==='registered'&&!$card['opened']&&$capOpen);
        if($needsCommand){[$session,$headers]=$this->session($r,$user,true);$card['csrfToken']=$this->token($session,$user,$id);$card['processRevision']=$this->revision($session,$id);}
        else{[$session]=$this->session($r,$user,false);}
        $card['canPrepare']=$capPrepare;$card['canRegister']=$capRegister;$card['canOpen']=$capOpen;$card['flash']=$flash;$card['openedByName']=$card['opened']?$this->actorName((int)$card['openedByUserId']):null;
        return $this->response(200,$this->cards->render($user,$card),['Content-Type'=>'text/html; charset=UTF-8']+$headers,$r->method);
    }

    public function queue(PilotHttpRequest $r,HttpUser $user):PilotHttpResponse
    {
        $caps=['Требуется распоряжение'=>$this->dependencies->hasCapability($user->id,'assignment_order.prepare'),'Распоряжение подготовлено'=>$this->dependencies->hasCapability($user->id,'assignment_order.confirm_registration'),'Готов к открытию'=>$this->dependencies->hasCapability($user->id,'installation.open')];$objects=$this->dependencies->objectList()->read();foreach($objects as &$object){$card=$this->dependencies->objectCards()->read($object['id']);$object['status']=$card['status'];$object['nextStep']=($caps[$card['status']]??($card['status']==='В работе'))?match($card['status']){'Требуется распоряжение'=>'Загрузить распоряжение','Распоряжение подготовлено'=>'Загрузить подписанный оригинал','Готов к открытию'=>'Открыть работы','В работе'=>'Инженеру: провести первую инспекцию',default=>'Откройте карточку объекта монтажа'}:'Откройте карточку объекта монтажа';}unset($object);
        return $this->response(200,$this->lists->render($user,$objects),['Content-Type'=>'text/html; charset=UTF-8'],$r->method);
    }

    private function constructionControl(PilotHttpRequest $r,HttpUser $user):PilotHttpResponse
    {
        $raw=$_GET['page']??1;$page=\filter_var($raw,FILTER_VALIDATE_INT,['options'=>['min_range'=>1]]);if($page===false)throw new PilotHttpInfrastructureUnavailable();$size=50;$offset=($page-1)*$size;[$db,$prefix,$legacy]=$this->dependencies->commandResources();$operations=$prefix.'fm2_checklist_operations';$q=$db->prepare('SELECT TABLE_NAME FROM information_schema.TABLES WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME=? LIMIT 1');$q->bind_param('s',$operations);$q->execute();$hasActivity=$q->get_result()->fetch_assoc()!==null;$total=(int)$db->query("SELECT COUNT(*) n FROM `{$prefix}fm2_installation_cases` WHERE process_state='working'")->fetch_assoc()['n'];$pages=\max(1,(int)\ceil($total/$size));if($page>$pages)throw new PilotHttpInfrastructureUnavailable();$activityJoin=$hasActivity?"LEFT JOIN (SELECT installation_case_id,MAX(device_time) last_activity_at FROM `{$operations}` GROUP BY installation_case_id) a ON a.installation_case_id=c.id":'';$activitySelect=$hasActivity?'a.last_activity_at':'NULL';$selected=$db->query("SELECT c.legacy_installation_object_id object_id,m.ordadr_address address,m.entrance,m.regnumber registration_number,m.ptoactdate,{$activitySelect} last_activity_at,COALESCE(CAST(JSON_UNQUOTE(JSON_EXTRACT(e.payload_json,'$.engineer.userId')) AS UNSIGNED),NULLIF(m.responsstroicontrol,0)) engineer_id,COALESCE(JSON_UNQUOTE(JSON_EXTRACT(e.payload_json,'$.engineer.fullName')),pu.full_name) engineer_name FROM `{$prefix}fm2_installation_cases` c JOIN `{$legacy}fm_maintable` m ON m.id=c.legacy_installation_object_id {$activityJoin} LEFT JOIN `{$prefix}fm2_process_events` e ON e.id=(SELECT MAX(latest.id) FROM `{$prefix}fm2_process_events` latest WHERE latest.installation_case_id=c.id AND latest.event_type='control_engineer_changed') LEFT JOIN `{$prefix}fm2_pilot_users` pu ON pu.user_id=COALESCE(CAST(JSON_UNQUOTE(JSON_EXTRACT(e.payload_json,'$.engineer.userId')) AS UNSIGNED),NULLIF(m.responsstroicontrol,0)) WHERE c.process_state='working' ORDER BY {$activitySelect} IS NOT NULL,{$activitySelect},c.legacy_installation_object_id LIMIT {$size} OFFSET {$offset}")->fetch_all(MYSQLI_ASSOC);$objects=[];$seen=[];foreach($selected as$row){$id=\filter_var($row['object_id'],FILTER_VALIDATE_INT,['options'=>['min_range'=>1]]);if($id===false||isset($seen[$id])||\trim((string)$row['address'])===''||\trim((string)$row['entrance'])===''||\trim((string)$row['registration_number'])==='')throw new PilotHttpInfrastructureUnavailable();$seen[$id]=true;$engineerId=\filter_var($row['engineer_id'],FILTER_VALIDATE_INT,['options'=>['min_range'=>1]]);$engineerName=\trim((string)($row['engineer_name']??''));$objects[]=['id'=>$id,'address'=>(string)$row['address'],'entrance'=>(string)$row['entrance'],'registrationNumber'=>(string)$row['registration_number'],'controlEngineer'=>$engineerId!==false&&$engineerName!==''?['userId'=>$engineerId,'fullName'=>$engineerName]:null,'completed'=>$row['ptoactdate']!==null,'lastChecklistActivityAt'=>$row['last_activity_at'],'_pagination'=>['page'=>$page,'pages'=>$pages,'total'=>$total,'pageSize'=>$size]];}
        return $this->response(200,(new ProductionConstructionControlRenderer())->render($user,$objects),['Content-Type'=>'text/html; charset=UTF-8'],$r->method);
    }

    private function preparePage(PilotHttpRequest $r,int $id,HttpUser $user):PilotHttpResponse
    {
        try{$form=$this->dependencies->prepareForms()->read($id,$this->dependencies->processDate());}catch(PrepareFormUnavailable){return $this->redirect('/pilot/objects/'.$id);}
        if($form===null)return $this->response(404,"Not found.\n",[],$r->method);[$session,$headers]=$this->session($r,$user,true);$form['csrfToken']=$this->token($session,$user,$id);$form['processRevision']=$this->revision($session,$id);$form['canUploadOriginal']=$this->dependencies->hasCapability($user->id,'assignment_order.confirm_registration');$form['flash']=$this->pullFlash($session,$r->path);$form['selected']=$form['flash']['selected']??[];return $this->response(200,$this->forms->render($user,$form),['Content-Type'=>'text/html; charset=UTF-8']+$headers,$r->method);
    }

    private function command(PilotHttpRequest $r,array $route,HttpUser $user):PilotHttpResponse
    {
        if($route['kind']==='prepare'&&\str_starts_with(\strtolower((string)($r->server['CONTENT_TYPE']??'')),'multipart/form-data;'))return $this->prepareFromMultipart($r,$route,$user);
        if($route['kind']==='registration'&&\str_starts_with(\strtolower((string)($r->server['CONTENT_TYPE']??'')),'multipart/form-data;'))return $this->uploadSignedOriginal($r,$route,$user);
        if($route['kind']==='registration')return $this->response(415,"PDF upload required.\n");
        $r=new PilotHttpRequest($r->method,$r->path,$r->host,$r->serverIdentity,$r->server,(string)\file_get_contents('php://input'));
        [$session]=$this->session($r,$user,false);if($session===null||!$this->validRequest($r,$session,$user))return $this->response(403,"Invalid request.\n");
        $allowed=match($route['kind']){'prepare'=>['csrfToken','processRevision','installerTabIds[]'],'engineer'=>['csrfToken','processRevision','controlEngineerUserId'],'registration'=>['csrfToken','processRevision','assignmentOrderVersion','registrationNumber'],'open'=>['csrfToken','processRevision','assignmentOrderVersion','actualStartDate']};
        try{$fields=$this->body($r,$allowed);}catch(InvalidCsrfRequest){return $this->response(403,"Invalid request.\n");}if($fields===null)return $this->response(400,"Bad request.\n");
        if(!$this->consume($session,$fields['csrfToken'][0]??'', $user,$route['id']))return $this->response(403,"Invalid request.\n");
        $card=$this->dependencies->objectCards()->read($route['id']);if($card===null)return $this->response(404,"Not found.\n");
        if(($fields['processRevision'][0]??'')!==$this->revision($session,$route['id']))return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Данные объекта монтажа изменились. Проверьте актуальное состояние и повторите действие.');
        [$connection,$processPrefix,$legacyPrefix,$root,$now]=$this->dependencies->commandResources();$clock=new class($now) implements Clock{public function __construct(private string $value){}public function now():string{return $this->value;}};$config=new ProductionInstallationProcessConfig($processPrefix,$legacyPrefix,$root);$process=ProductionInstallationProcessFactory::create($connection,$config,$clock);
        if($route['kind']==='engineer')return $this->assignEngineer($session,$route['id'],$fields,$user,$connection,$processPrefix,$now);
        if($route['kind']==='prepare'){
            $path=$r->path;$error=null;$installers=$fields['installerTabIds[]']??[];$eligible=$this->dependencies->prepareForms()->read($route['id'],$this->dependencies->processDate());$installerIds=\array_map(static fn(array $x):string=>(string)$x['tabId'],$eligible['installers']);$engineer=$eligible['engineer']??null;
            if($installers===[]||\count($installers)!==\count(\array_unique($installers))||\count(\array_filter($installers,[self::class,'positive']))!==\count($installers))$error=['installers','Выберите хотя бы одного монтажника.'];
            elseif(!\is_array($engineer)||!self::positive((string)($engineer['userId']??'')))$error=['engineer','Сначала назначьте инженера строительного контроля в карточке объекта.'];
            if($error===null)foreach($installers as$value)if(!\in_array($value,$installerIds,true)){$error=['installers','Состав монтажников изменился. Проверьте доступных сотрудников.'];break;}
            if($error!==null)return $this->flashRedirect($session,$path,$error[1],$error[0],false,['installers'=>\array_values(\array_intersect($installers,$installerIds??[]))]);
            $session['pendingSelection']=['installers'=>\array_values(\array_intersect($installers,$installerIds))];$_SESSION=$session;$result=$process->prepareAssignmentOrder($route['id'],\array_map('intval',$installers),(int)$engineer['userId'],$user->id);if(($result['accepted']??false)===true){$preparedVersion=$result['assignmentOrderVersion']??null;if(($result['status']??null)!=='prepared'||!self::positive((string)$preparedVersion))return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60']);unset($session['pendingSelection']);$_SESSION=$session;return $this->redirect('/pilot/objects/'.$route['id'].'/assignment-orders/'.(int)$preparedVersion.'/artifacts/order');}return $this->violation($session,$route,$result,$path);
        }
        $version=$fields['assignmentOrderVersion']??[];if(\count($version)!==1||!self::positive($version[0])||$card['order']===null||(int)$version[0]!==$card['order']['version']||($route['kind']==='registration'&&(int)$version[0]!==$route['version'])||($route['kind']==='open'&&$card['order']['status']!=='registered'))return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Данные объекта монтажа изменились. Проверьте актуальное состояние и повторите действие.');
        if($route['kind']==='registration'){$number=$fields['registrationNumber'][0]??'';$number=\trim($number);if($number===''||\mb_strlen($number)>120||\preg_match('/[\x00-\x1F\x7F]/u',$number)===1)return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Введите номер распоряжения в 1С ДО.','registrationNumber');$result=$process->confirmOrderRegistration($route['id'],(int)$version[0],$number,'manual',$user->id);if(($result['accepted']??false)===true){if(($result['status']??null)!=='registered'||($result['assignmentOrderVersion']??null)!==(int)$version[0])return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60']);return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Номер 1С ДО сохранён. Распоряжение зарегистрировано.');}return $this->violation($session,$route,$result,'/pilot/objects/'.$route['id']);}
        $date=$fields['actualStartDate'][0]??'';$today=(new \DateTimeImmutable($now))->setTimezone(new \DateTimeZone('Europe/Moscow'))->format('Y-m-d');if(!self::date($date)||$date>$today||$date<$card['order']['orderDate'])return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Укажите фактическую дату от даты распоряжения до сегодняшнего дня.','actualStartDate');$result=$process->openInstallation($route['id'],$date,$user->id);if(($result['accepted']??false)===true){if(($result['processState']??null)!=='working'||($result['actualStartDate']??null)!==$date)return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60']);return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Работы открыты.');}return $this->violation($session,$route,$result,'/pilot/objects/'.$route['id']);
    }

    private function artifact(PilotHttpRequest $r,array $route,HttpUser $user):PilotHttpResponse
    {
        [$connection,$processPrefix,$legacyPrefix,$root]=$this->dependencies->commandResources();try{$a=ProductionInstallationProcessFactory::createArtifactService($connection,new ProductionInstallationProcessConfig($processPrefix,$legacyPrefix,$root))->download($route['id'],$route['version'],$route['type'],$user->id);}catch(ArtifactNotFoundException|ArtifactIntegrityException){return $this->response(404,"Not found.\n",[],$r->method);}catch(ArtifactUnavailableException){return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}if(!($a['accepted']??false))return $this->response(403,"Access denied.\n",[],$r->method);$filename=$a['filename']??'';$bytes=$a['bytes']??'';if(!\is_string($filename)||$filename===''||!\mb_check_encoding($filename,'UTF-8')||\preg_match('/[\x00-\x1F\x7F]/u',$filename)===1||!\is_string($bytes))return $this->response(404,"Not found.\n",[],$r->method);$disposition=($a['mediaType']??null)==='application/pdf'?'inline':'attachment';$downloadName=($a['mediaType']??null)==='application/pdf'?'Распоряжение о закреплении монтажников.pdf':$filename;$fallback=($a['mediaType']??null)==='application/pdf'?'assignment-order.pdf':'assignment-order.html';$contentDisposition=$disposition.'; filename="'.$fallback.'"; filename*=UTF-8\'\''.\rawurlencode($downloadName);return $this->response(200,$bytes,['Content-Type'=>$a['mediaType'],'Content-Disposition'=>$contentDisposition],$r->method);
    }

    private function prepareFromMultipart(PilotHttpRequest $r,array $route,HttpUser $user):PilotHttpResponse
    {
        [$session]=$this->session($r,$user,false);if($session===null||!$this->validRequest($r,$session,$user))return $this->response(403,"Invalid request.\n");
        $allowed=['csrfToken','processRevision','installerTabIds','intent'];if(\array_diff(\array_keys($_POST),$allowed)!==[])return $this->response(400,"Bad request.\n");
        $intent=$_POST['intent']??null;if(!\is_string($intent)||!\in_array($intent,['upload','template'],true))return $this->response(400,"Bad request.\n");$csrf=\is_string($_POST['csrfToken']??null)?$_POST['csrfToken']:'';$csrfValid=$intent==='template'?$this->validToken($session,$csrf,$user,$route['id']):$this->consume($session,$csrf,$user,$route['id']);if(!$csrfValid)return $this->response(403,"Invalid request.\n");
        $revisionValid=\is_string($_POST['processRevision']??null)&&$_POST['processRevision']===$this->revision($session,$route['id']);if(!$revisionValid){$current=$this->dependencies->objectCards()->read($route['id']);if($intent!=='upload'||($current['order']['status']??null)!=='prepared')return $this->flashRedirect($session,$r->path,'Данные объекта монтажа изменились. Проверьте актуальное состояние и повторите действие.');}
        $installers=$_POST['installerTabIds']??[];if(!\is_array($installers)||$installers===[]||\count($installers)!==\count(\array_unique($installers))||\count(\array_filter($installers,static fn(mixed $value):bool=>\is_string($value)&&self::positive($value)))!==\count($installers))return $this->flashRedirect($session,$r->path,'Выберите хотя бы одного монтажника.','installers');
        if($intent==='upload'){$preparedUpload=$this->preparedUpload($route['id'],$installers,$user,$session);if($preparedUpload!==null)return $preparedUpload;}
        try{$eligible=$this->dependencies->prepareForms()->read($route['id'],$this->dependencies->processDate());}catch(PrepareFormUnavailable){$card=$this->dependencies->objectCards()->read($route['id']);$order=$card['order']??null;$storedIds=\is_array($order)?\array_map(static fn(array $x):string=>(string)$x['tabId'],$order['installers']??[]):[];\sort($storedIds);$submittedIds=\array_values($installers);\sort($submittedIds);if($intent!=='upload'||!\is_array($order)||$order['status']!=='prepared'||$storedIds!==$submittedIds)return $this->redirect('/pilot/objects/'.$route['id']);if(!$this->dependencies->hasCapability($user->id,'assignment_order.confirm_registration'))return $this->response(403,"Access denied.\n");$file=$_FILES['signedOriginal']??null;$tmp=\is_array($file)?($file['tmp_name']??null):null;$size=\is_array($file)?($file['size']??null):null;if(!\is_array($file)||($file['error']??UPLOAD_ERR_NO_FILE)!==UPLOAD_ERR_OK||!\is_string($tmp)||!\is_int($size)||$size<5||$size>25*1024*1024||!\is_uploaded_file($tmp))return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Выберите PDF распоряжения не более 25 МБ.','signedOriginal');$bytes=\file_get_contents($tmp);if(!\is_string($bytes)||\strlen($bytes)!==$size||!\str_starts_with($bytes,'%PDF-')||(new \finfo(FILEINFO_MIME_TYPE))->buffer($bytes)!=='application/pdf')return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Загружаемый файл должен быть PDF.','signedOriginal');return $this->registerPreparedOriginal($route['id'],(int)$order['version'],$bytes,$user,$session);}
        $eligibleIds=\array_map(static fn(array $x):string=>(string)$x['tabId'],$eligible['installers']);$engineer=$eligible['engineer']??null;foreach($installers as$value)if(!\in_array($value,$eligibleIds,true))return $this->flashRedirect($session,$r->path,'Состав монтажников изменился. Проверьте доступных сотрудников.','installers');if(!\is_array($engineer)||!self::positive((string)($engineer['userId']??'')))return $this->flashRedirect($session,$r->path,'Сначала назначьте инженера строительного контроля.','engineer');
        $bytes=null;if($intent==='upload'){
            if(!$this->dependencies->hasCapability($user->id,'assignment_order.confirm_registration'))return $this->response(403,"Access denied.\n");$length=$r->server['CONTENT_LENGTH']??null;$file=$_FILES['signedOriginal']??null;$tmp=\is_array($file)?($file['tmp_name']??null):null;$size=\is_array($file)?($file['size']??null):null;if(!\is_string($length)||!\ctype_digit($length)||(int)$length>25*1024*1024+32768||!\is_array($file)||($file['error']??UPLOAD_ERR_NO_FILE)!==UPLOAD_ERR_OK||!\is_string($tmp)||!\is_int($size)||$size<5||$size>25*1024*1024||!\is_uploaded_file($tmp))return $this->flashRedirect($session,$r->path,'Выберите PDF распоряжения не более 25 МБ.','signedOriginal');$bytes=\file_get_contents($tmp);if(!\is_string($bytes)||\strlen($bytes)!==$size||!\str_starts_with($bytes,'%PDF-')||(new \finfo(FILEINFO_MIME_TYPE))->buffer($bytes)!=='application/pdf')return $this->flashRedirect($session,$r->path,'Загружаемый файл должен быть PDF.','signedOriginal');
        }
        [$db,$prefix,$legacy,$root,$now]=$this->dependencies->commandResources();$clock=new class($now) implements Clock{public function __construct(private string $value){}public function now():string{return $this->value;}};$process=ProductionInstallationProcessFactory::create($db,new ProductionInstallationProcessConfig($prefix,$legacy,$root),$clock);$result=$process->prepareAssignmentOrder($route['id'],\array_map('intval',$installers),(int)$engineer['userId'],$user->id);$version=$result['assignmentOrderVersion']??null;if(!($result['accepted']??false)||($result['status']??null)!=='prepared'||!self::positive((string)$version))return $this->violation($session,$route,$result,$r->path);if($intent==='template')return $this->redirect('/pilot/objects/'.$route['id'].'/assignment-orders/'.(int)$version.'/artifacts/order');
        return $this->registerPreparedOriginal($route['id'],(int)$version,$bytes,$user,$session);
    }

    private function preparedUpload(int $objectId,array $installers,HttpUser $user,array &$session):?PilotHttpResponse
    {
        $card=$this->dependencies->objectCards()->read($objectId);$order=$card['order']??null;if(!\is_array($order)||($order['status']??null)!=='prepared')return null;$storedIds=\array_map(static fn(array $x):string=>(string)$x['tabId'],$order['installers']??[]);\sort($storedIds);$submittedIds=\array_values($installers);\sort($submittedIds);if($storedIds!==$submittedIds)return $this->flashRedirect($session,'/pilot/objects/'.$objectId,'Состав монтажников изменился. Проверьте текущий состав.','installers');if(!$this->dependencies->hasCapability($user->id,'assignment_order.confirm_registration'))return $this->response(403,"Access denied.\n");$file=$_FILES['signedOriginal']??null;$tmp=\is_array($file)?($file['tmp_name']??null):null;$size=\is_array($file)?($file['size']??null):null;if(!\is_array($file)||($file['error']??UPLOAD_ERR_NO_FILE)!==UPLOAD_ERR_OK||!\is_string($tmp)||!\is_int($size)||$size<5||$size>25*1024*1024||!\is_uploaded_file($tmp))return $this->flashRedirect($session,'/pilot/objects/'.$objectId,'Выберите PDF распоряжения не более 25 МБ.','signedOriginal');$bytes=\file_get_contents($tmp);if(!\is_string($bytes)||\strlen($bytes)!==$size||!\str_starts_with($bytes,'%PDF-')||(new \finfo(FILEINFO_MIME_TYPE))->buffer($bytes)!=='application/pdf')return $this->flashRedirect($session,'/pilot/objects/'.$objectId,'Загружаемый файл должен быть PDF.','signedOriginal');return $this->registerPreparedOriginal($objectId,(int)$order['version'],$bytes,$user,$session);
    }

    private function registerPreparedOriginal(int $objectId,int $version,string $bytes,HttpUser $user,array &$session):PilotHttpResponse
    {
        [$db,$prefix,,$root,$now]=$this->dependencies->commandResources();$stored=(new ContentAddressedArtifactStore($root))->store($bytes);$db->begin_transaction();try{$q=$db->prepare("SELECT c.id case_id,c.lock_version,o.id order_id,o.status FROM `{$prefix}fm2_installation_cases` c JOIN `{$prefix}fm2_assignment_orders` o ON o.installation_case_id=c.id AND o.version_no=? WHERE c.legacy_installation_object_id=? LIMIT 2 FOR UPDATE");$q->bind_param('ii',$version,$objectId);$q->execute();$rows=$q->get_result()->fetch_all(MYSQLI_ASSOC);if(\count($rows)!==1||(string)$rows[0]['status']!=='prepared')throw new \LogicException();$orderId=(int)$rows[0]['order_id'];$name='Подписанный оригинал.pdf';$media='application/pdf';$type='signed_original';$size=$stored['size'];$hash=$stored['sha256'];$artifact=$db->prepare("INSERT INTO `{$prefix}fm2_order_artifacts`(assignment_order_id,artifact_type,filename,media_type,byte_size,sha256) VALUES(?,?,?,?,?,?)");$artifact->bind_param('isssis',$orderId,$type,$name,$media,$size,$hash);$artifact->execute();$actor=(string)$user->id;$update=$db->prepare("UPDATE `{$prefix}fm2_assignment_orders` SET status='registered',registered_at=?,registration_actor_type='user',registration_actor_id=?,registration_source='signed_original' WHERE id=? AND status='prepared'");$update->bind_param('ssi',$now,$actor,$orderId);$update->execute();if($update->affected_rows!==1)throw new \LogicException();$payload=\json_encode(['assignmentOrderVersion'=>$version,'signedOriginalSha256'=>$hash,'signedOriginalSize'=>$size],JSON_UNESCAPED_UNICODE|JSON_THROW_ON_ERROR);$caseId=(int)$rows[0]['case_id'];$actorId=$user->id;$event=$db->prepare("INSERT INTO `{$prefix}fm2_process_events`(installation_case_id,event_type,occurred_at,actor_user_id,payload_json) VALUES(?,'assignment_order_signed_original_uploaded',?,?,?)");$event->bind_param('isis',$caseId,$now,$actorId,$payload);$event->execute();$lock=(int)$rows[0]['lock_version'];$case=$db->prepare("UPDATE `{$prefix}fm2_installation_cases` SET updated_at=?,lock_version=lock_version+1 WHERE id=? AND lock_version=?");$case->bind_param('sii',$now,$caseId,$lock);$case->execute();if($case->affected_rows!==1)throw new \LogicException();$db->commit();return $this->flashRedirect($session,'/pilot/objects/'.$objectId,'Распоряжение загружено. Можно открывать работы.');}catch(\Throwable $error){$db->rollback();throw $error;}
    }

    private function uploadSignedOriginal(PilotHttpRequest $r,array $route,HttpUser $user):PilotHttpResponse
    {
        [$session]=$this->session($r,$user,false);if($session===null||!$this->validRequest($r,$session,$user))return $this->response(403,"Invalid request.\n");
        $length=$r->server['CONTENT_LENGTH']??null;if(!\is_string($length)||!\ctype_digit($length)||(int)$length>25*1024*1024+32768)return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'PDF должен быть не больше 25 МБ.','signedOriginal');
        if(\array_diff(\array_keys($_POST),['csrfToken','processRevision','assignmentOrderVersion'])!==[]||\array_keys($_FILES)!==['signedOriginal'])return $this->response(400,"Bad request.\n");
        if(!$this->consume($session,(string)($_POST['csrfToken']??''),$user,$route['id']))return $this->response(403,"Invalid request.\n");
        if(!\is_string($_POST['processRevision']??null)||$_POST['processRevision']!==$this->revision($session,$route['id'])||!self::positive((string)($_POST['assignmentOrderVersion']??''))||(int)$_POST['assignmentOrderVersion']!==$route['version'])return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Данные объекта монтажа изменились. Проверьте актуальное состояние и повторите действие.');
        $file=$_FILES['signedOriginal'];$tmp=$file['tmp_name']??null;$size=$file['size']??null;if(($file['error']??UPLOAD_ERR_NO_FILE)!==UPLOAD_ERR_OK||!\is_string($tmp)||!\is_int($size)||$size<5||$size>25*1024*1024||!\is_uploaded_file($tmp))return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Выберите корректный PDF не более 25 МБ.','signedOriginal');
        $bytes=\file_get_contents($tmp);if(!\is_string($bytes)||\strlen($bytes)!==$size||!\str_starts_with($bytes,'%PDF-')||(new \finfo(FILEINFO_MIME_TYPE))->buffer($bytes)!=='application/pdf')return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Загружаемый файл должен быть PDF.','signedOriginal');
        [$db,$prefix,,$root,$now]=$this->dependencies->commandResources();$stored=(new ContentAddressedArtifactStore($root))->store($bytes);$db->begin_transaction();try{$q=$db->prepare("SELECT c.id case_id,c.lock_version,o.id order_id,o.status FROM `{$prefix}fm2_installation_cases` c JOIN `{$prefix}fm2_assignment_orders` o ON o.installation_case_id=c.id AND o.version_no=? WHERE c.legacy_installation_object_id=? LIMIT 2 FOR UPDATE");$version=$route['version'];$objectId=$route['id'];$q->bind_param('ii',$version,$objectId);$q->execute();$rows=$q->get_result()->fetch_all(MYSQLI_ASSOC);if(\count($rows)!==1||(string)$rows[0]['status']!=='prepared')throw new \LogicException();$orderId=(int)$rows[0]['order_id'];$name='Подписанный оригинал.pdf';$media='application/pdf';$artifactType='signed_original';$artifact=$db->prepare("INSERT INTO `{$prefix}fm2_order_artifacts`(assignment_order_id,artifact_type,filename,media_type,byte_size,sha256) VALUES(?,?,?,?,?,?)");$artifact->bind_param('isssis',$orderId,$artifactType,$name,$media,$stored['size'],$stored['sha256']);$artifact->execute();$actor=(string)$user->id;$update=$db->prepare("UPDATE `{$prefix}fm2_assignment_orders` SET status='registered',registered_at=?,registration_actor_type='user',registration_actor_id=?,registration_source='signed_original' WHERE id=? AND status='prepared'");$update->bind_param('ssi',$now,$actor,$orderId);$update->execute();if($update->affected_rows!==1)throw new \LogicException();$payload=\json_encode(['assignmentOrderVersion'=>$version,'signedOriginalSha256'=>$stored['sha256'],'signedOriginalSize'=>$stored['size']],JSON_UNESCAPED_UNICODE|JSON_THROW_ON_ERROR);$event=$db->prepare("INSERT INTO `{$prefix}fm2_process_events`(installation_case_id,event_type,occurred_at,actor_user_id,payload_json) VALUES(?,'assignment_order_signed_original_uploaded',?,?,?)");$caseId=(int)$rows[0]['case_id'];$actorId=$user->id;$event->bind_param('isis',$caseId,$now,$actorId,$payload);$event->execute();$lock=(int)$rows[0]['lock_version'];$case=$db->prepare("UPDATE `{$prefix}fm2_installation_cases` SET updated_at=?,lock_version=lock_version+1 WHERE id=? AND lock_version=?");$case->bind_param('sii',$now,$caseId,$lock);$case->execute();if($case->affected_rows!==1)throw new \LogicException();$db->commit();return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Подписанный оригинал загружен. Распоряжение готово к открытию работ.');}catch(\LogicException){$db->rollback();return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Данные объекта монтажа изменились. Проверьте состояние и повторите действие.');}catch(\Throwable $error){$db->rollback();throw $error;}
    }

    private function commandLogicException(PilotHttpRequest $r,array $route,HttpUser $user,\LogicException $error):PilotHttpResponse
    {
        $message=$error->getMessage();$authorization=['registration'=>'Registration confirmation is not authorized.','open'=>'Installation opening is not authorized.'];if(($authorization[$route['kind']]??null)===$message)return $this->response(403,"Access denied.\n",[],$r->method);
        $concurrency=['registration'=>['The current prepared assignment order was not found.','The requested assignment order is not the current prepared version.'],'open'=>['Installation is already open.','A current registered assignment order is required.','The current assignment order is not registered.']];$employment=$route['kind']==='open'&&$message==='A current installer is not employed on the actual start date.';if(!$employment&&!\in_array($message,$concurrency[$route['kind']]??[],true))return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);
        [$session]=$this->session($r,$user,false);if($session===null)return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);return $employment?$this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Состав монтажников изменился. Открытие работ недоступно.',null,true):$this->flashRedirect($session,'/pilot/objects/'.$route['id'],'Данные объекта монтажа изменились. Проверьте актуальное состояние и повторите действие.');
    }

    private function violation(array &$session,array $route,array $result,string $path):PilotHttpResponse
    {
        $code=$result['violations'][0]['code']??'';if($route['kind']==='open'&&$code==='REGISTERED_ORDER_COMPOSITION_INVALID')return $this->flashRedirect($session,$path,'Состав зарегистрированного распоряжения повреждён. Открытие работ недоступно.',null,true);if($route['kind']==='open'&&$code==='INSTALLER_NOT_EMPLOYED')return $this->flashRedirect($session,$path,'Состав монтажников изменился. Открытие работ недоступно.',null,true);$map=['INSTALLER_NOT_IN_CATALOG'=>['Состав монтажников изменился. Проверьте доступных сотрудников.','installers'],'INSTALLER_NOT_EMPLOYED'=>['Состав монтажников изменился. Проверьте доступных сотрудников.','installers'],'CONTROL_ENGINEER_NOT_ELIGIBLE'=>['Выбранный инженер больше недоступен. Выберите другого.','engineer'],'CONTROL_ENGINEER_REQUIRED'=>['Выберите одного инженера строительного контроля.','engineer'],'INSTALLER_REQUIRED'=>['Выберите хотя бы одного монтажника.','installers'],'INSTALLATION_OBJECT_REQUIRED_DATA_MISSING'=>['В карточке объекта монтажа не хватает обязательных данных.',null],'REGISTRATION_NUMBER_REQUIRED'=>['Введите номер распоряжения в 1С ДО.','registrationNumber'],'ACTUAL_START_BEFORE_ORDER_DATE'=>['Укажите фактическую дату от даты распоряжения до сегодняшнего дня.','actualStartDate']];if(isset($map[$code]))return $this->flashRedirect($session,$path,$map[$code][0],$map[$code][1]);if($code==='FORBIDDEN')return $this->response(403,"Access denied.\n");if(\in_array($code,['ORDER_HAS_PTO_ACT','ASSIGNMENT_ORDER_ALREADY_PREPARED','CONCURRENT_MODIFICATION'],true))return $this->flashRedirect($session,'/pilot/objects/'.$route['id'],$code==='CONCURRENT_MODIFICATION'?'Данные объекта монтажа изменились. Проверьте актуальное состояние и повторите действие.':'Формирование распоряжения недоступно для текущего состояния объекта монтажа.');return $this->response(503,"Service unavailable.\n",['Retry-After'=>'60']);
    }

    private function route(string $path):?array
    {
        foreach([['prepare','#^/pilot/objects/([1-9][0-9]*)/assignment-order/prepare$#D'],['engineer','#^/pilot/objects/([1-9][0-9]*)/control-engineer$#D'],['artifact','#^/pilot/objects/([1-9][0-9]*)/assignment-orders/([1-9][0-9]*)/artifacts/(order|appendix|signed_original)$#D'],['registration','#^/pilot/objects/([1-9][0-9]*)/assignment-orders/([1-9][0-9]*)/registration$#D'],['open','#^/pilot/objects/([1-9][0-9]*)/open$#D']]as[$kind,$pattern])if(\preg_match($pattern,$path,$m)===1){foreach(\array_slice($m,1)as$x)if(\ctype_digit($x)&&!self::positive($x))return null;return ['kind'=>$kind,'id'=>(int)$m[1]]+(\in_array($kind,['artifact','registration'],true)?['version'=>(int)$m[2]]:[])+($kind==='artifact'?['type'=>$m[3]]:[]);}return null;
    }
    private function engineers():array
    {
        [$db,$prefix]=$this->dependencies->commandResources();$pilotUsers=$prefix.'fm2_pilot_users';$sql="SELECT u.user_id,u.full_name FROM `{$pilotUsers}` u JOIN `{$prefix}fm2_pilot_user_roles` ur ON ur.user_id=u.user_id JOIN `{$prefix}fm2_pilot_roles` r ON r.role_id=ur.role_id WHERE u.status=1 AND u.activation_state='active' AND r.status=1 AND r.code='construction_control_engineer' ORDER BY u.full_name,u.user_id LIMIT 101";$rows=$db->query($sql)->fetch_all(MYSQLI_ASSOC);$mapped=\array_map(static fn(array $x):array=>['userId'=>(int)$x['user_id'],'fullName'=>(string)$x['full_name'],'position'=>'Инженер строительного контроля'],$rows);if(\count($mapped)>100)throw new PilotHttpInfrastructureUnavailable();return $mapped;
    }
    private function assignEngineer(array &$session,int $objectId,array $fields,HttpUser $actor,\mysqli $db,string $prefix,string $now):PilotHttpResponse
    {
        $values=$fields['controlEngineerUserId']??[];if(\count($values)!==1||!self::positive($values[0]))return $this->flashRedirect($session,"/pilot/objects/{$objectId}",'Выберите инженера строительного контроля.','controlEngineerUserId');$selected=null;foreach($this->engineers()as$engineer)if($engineer['userId']===(int)$values[0]){$selected=$engineer;break;}if($selected===null)return $this->flashRedirect($session,"/pilot/objects/{$objectId}",'Выбранный инженер больше недоступен. Выберите другого.','controlEngineerUserId');
        $db->begin_transaction();try{$case=$db->prepare("SELECT id,lock_version FROM `{$prefix}fm2_installation_cases` WHERE legacy_installation_object_id=? LIMIT 2 FOR UPDATE");$case->bind_param('i',$objectId);$case->execute();$rows=$case->get_result()->fetch_all(MYSQLI_ASSOC);if(\count($rows)!==1)throw new PilotHttpInfrastructureUnavailable();$card=$this->dependencies->objectCards()->read($objectId);$previous=$card['controlEngineer']??null;if(($previous['userId']??null)===$selected['userId']){$db->commit();return $this->flashRedirect($session,"/pilot/objects/{$objectId}",'Инженер строительного контроля уже назначен.');}$payload=\json_encode(['previousEngineer'=>$previous,'engineer'=>$selected],JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_THROW_ON_ERROR);$event=$db->prepare("INSERT INTO `{$prefix}fm2_process_events`(installation_case_id,event_type,occurred_at,actor_user_id,payload_json) VALUES(?,'control_engineer_changed',?,?,?)");$caseId=(int)$rows[0]['id'];$actorId=$actor->id;$event->bind_param('isis',$caseId,$now,$actorId,$payload);$event->execute();$update=$db->prepare("UPDATE `{$prefix}fm2_installation_cases` SET updated_at=?,lock_version=lock_version+1 WHERE id=? AND lock_version=?");$version=(int)$rows[0]['lock_version'];$update->bind_param('sii',$now,$caseId,$version);$update->execute();if($update->affected_rows!==1)throw new PilotHttpInfrastructureUnavailable();$db->commit();return $this->flashRedirect($session,"/pilot/objects/{$objectId}",'Инженер строительного контроля назначен.');}catch(\Throwable $error){$db->rollback();throw $error;}
    }
    private function session(PilotHttpRequest $r,HttpUser $user,bool $create):array
    {
        $headers=[];if(\session_status()!==PHP_SESSION_ACTIVE){$cookieName=self::commandCookieName($r);\session_name($cookieName);\ini_set('session.use_cookies','0');$incoming=null;if(\preg_match('/(?:^|;\s*)'.\preg_quote($cookieName,'/').'=([A-Za-z0-9,-]{16,128})/',(string)($r->server['HTTP_COOKIE']??''),$m)===1)$incoming=$m[1];if($incoming===null&&!$create)return [null,[]];\session_id($incoming??'');if(!@\session_start())throw new PilotHttpInfrastructureUnavailable();if($incoming===null)$headers=['Set-Cookie'=>$cookieName.'='.\session_id().(self::trustedDemo($r)?'':'; Secure').'; HttpOnly; SameSite=Strict; Path=/pilot'];}
        if(isset($_SESSION['actor'])&&$_SESSION['actor']!==$user->id){\session_regenerate_id(true);$_SESSION=[];}
        if(!isset($_SESSION['actor'])){if(!$create)return [null,[]];$_SESSION=['actor'=>$user->id,'secret'=>\random_bytes(32),'tokens'=>[],'flash'=>[]];}
        return [&$_SESSION,$headers];
    }
    private function token(array &$s,HttpUser $u,int $id):string{$t=\bin2hex(\random_bytes(16));$s['tokens'][$t]=['actor'=>$u->id,'id'=>$id,'at'=>\time()];$_SESSION=$s;return $t;}
    private function validToken(array $s,string $token,HttpUser $u,int $id):bool{$x=$s['tokens'][$token]??null;return \is_array($x)&&($x['actor']??null)===$u->id&&($x['id']??null)===$id&&\time()-(int)($x['at']??0)<=1800;}
    private function consume(array &$s,string $token,HttpUser $u,int $id):bool{$x=$s['tokens'][$token]??null;unset($s['tokens'][$token]);$_SESSION=$s;return \is_array($x)&&$x['actor']===$u->id&&$x['id']===$id&&\time()-$x['at']<=1800;}
    private function revision(array $s,int $id):string{[$db,$prefix]=$this->dependencies->commandResources();$q=$db->prepare("SELECT lock_version FROM `{$prefix}fm2_installation_cases` WHERE legacy_installation_object_id=? LIMIT 2");$q->bind_param('i',$id);$q->execute();$rows=$q->get_result()->fetch_all(MYSQLI_ASSOC);if(\count($rows)!==1)throw new PilotHttpInfrastructureUnavailable();return \hash_hmac('sha256',$id.':'.$rows[0]['lock_version'],$s['secret']);}
    private function validRequest(PilotHttpRequest $r,array $s,HttpUser $u):bool
    {
        $origin=$r->server['HTTP_ORIGIN']??null;$fetch=$r->server['HTTP_SEC_FETCH_SITE']??null;
        $trustedDemo=self::trustedDemo($r);$expectedOrigin=($trustedDemo?'http://':'https://').$r->host;
        $originAllowed=$origin===null||$origin===$expectedOrigin||($trustedDemo&&$origin==='null');
        return $s['actor']===$u->id&&$originAllowed&&($fetch===null||$fetch==='same-origin');
    }
    private function validChecklistRequest(PilotHttpRequest $r,array $s,HttpUser $u):bool
    {
        $expected=(self::trustedDemo($r)?'http://':'https://').$r->host;
        return ($s['actor']??null)===$u->id&&($r->server['HTTP_ORIGIN']??null)===$expected&&($r->server['HTTP_SEC_FETCH_SITE']??null)==='same-origin';
    }
    private static function trustedDemo(PilotHttpRequest $r):bool
    {
        $nonce=$r->server['FMONITOR_DEMO_LOOPBACK_NONCE']??null;$trustedHost=$r->server['FMONITOR_DEMO_TRUSTED_REQUEST_HOST']??null;
        return PHP_SAPI==='cli-server'&&\is_string($nonce)&&\preg_match('/^[0-9a-f]{32}$/D',$nonce)===1
            &&\is_string($trustedHost)&&\preg_match('/^127\.0\.0\.1:([1-9][0-9]{3,4})$/D',$trustedHost,$parts)===1
            &&(int)$parts[1]>=1024&&(int)$parts[1]<=65535&&$r->host===$trustedHost;
    }
    private static function commandCookieName(PilotHttpRequest $r):string
    {
        return self::trustedDemo($r)&&\preg_match('/:(\d{1,5})$/D',$r->host,$match)===1?'fm2pilot_'.$match[1]:'fm2pilot';
    }
    private function body(PilotHttpRequest $r,array $allowed):?array{$type=(string)($r->server['CONTENT_TYPE']??'');$length=$r->server['CONTENT_LENGTH']??null;if(!\preg_match('#^application/x-www-form-urlencoded(?:;\s*charset=UTF-8)?$#iD',$type)||!\is_string($length)||!\ctype_digit($length)||(int)$length>16384||(int)$length!==\strlen($r->body))return null;$out=[];$nextInstaller=0;foreach(\explode('&',$r->body)as$part){if($part==='')continue;$pair=\explode('=',$part,2);if(\preg_match('/%(?![0-9A-Fa-f]{2})/',($pair[0]??'').($pair[1]??''))===1)return null;$key=\rawurldecode(\str_replace('+',' ',$pair[0]));$value=\rawurldecode(\str_replace('+',' ',$pair[1]??''));if(\preg_match('/^installerTabIds\[([0-9]+)\]$/D',$key,$m)===1){if((int)$m[1]!==$nextInstaller++||$nextInstaller>500)return null;$key='installerTabIds[]';}if(!\in_array($key,$allowed,true)||!\mb_check_encoding($key,'UTF-8')||!\mb_check_encoding($value,'UTF-8'))return null;$out[$key][]=$value;if($key==='installerTabIds[]'&&\count($out[$key])>500)return null;}if(!isset($out['csrfToken'])||\count($out['csrfToken'])!==1)throw new InvalidCsrfRequest();foreach($out as$key=>$values)if($key!=='installerTabIds[]'&&\count($values)!==1)return null;return $out;}
    private function flashRedirect(array &$s,string $path,string $message,?string $field=null,bool $suppressOpen=false,array $selected=[]):PilotHttpResponse{if($selected===[]&&isset($s['pendingSelection']))$selected=$s['pendingSelection'];unset($s['pendingSelection']);$s['flash'][$path]=['message'=>$message,'field'=>$field,'suppressOpen'=>$suppressOpen,'selected'=>$selected];$_SESSION=$s;return $this->redirect($path);}
    private function pullFlash(array &$s,string $path):?array{$f=$s['flash'][$path]??null;unset($s['flash'][$path]);$_SESSION=$s;return $f;}
    private function redirect(string $path):PilotHttpResponse{return $this->response(303,'',['Location'=>$path]);}
    private function actorName(int $id):string{[$db,$prefix]=$this->dependencies->commandResources();$s=$db->prepare("SELECT full_name FROM `{$prefix}fm2_pilot_users` WHERE user_id=? AND status=1 AND activation_state='active' LIMIT 2");$s->bind_param('i',$id);$s->execute();$rows=$s->get_result()->fetch_all(MYSQLI_ASSOC);return \count($rows)===1?(string)$rows[0]['full_name']:(string)$id;}
    public static function positive(string $v):bool{return \preg_match('/^[1-9][0-9]*$/D',$v)===1&&\strlen($v)<=19&&(\strlen($v)<19||\strcmp($v,'9223372036854775807')<=0);}
    private static function date(string $v):bool{return \preg_match('/^(\d{4})-(\d{2})-(\d{2})$/D',$v,$m)===1&&$m[1]!=='0000'&&\checkdate((int)$m[2],(int)$m[3],(int)$m[1]);}
    private function response(int $status,string $body,array $extra=[],string $method='GET'):PilotHttpResponse{$type=(string)($extra['Content-Type']??'text/plain; charset=UTF-8');$csp=PilotRouteCsp::forResponse($this->cspMethod,$this->cspPath,$status,$type,$body);$headers=$extra+['Content-Type'=>'text/plain; charset=UTF-8','X-Content-Type-Options'=>'nosniff','Referrer-Policy'=>'no-referrer','X-Frame-Options'=>'DENY','Content-Security-Policy'=>$csp,'Permissions-Policy'=>'camera=(), microphone=(), geolocation=()','Cross-Origin-Opener-Policy'=>'same-origin','Cache-Control'=>'no-store'];$headers['Content-Length']=(string)\strlen($body);return new PilotHttpResponse($status,$headers,$method==='HEAD'?'':$body);}
    private function sessionResponse(PilotHttpRequest$r):PilotHttpResponse
    {
        if(!$this->knownPath($r->path))return$this->response(303,'',['Location'=>'/pilot/login'],$r->method);$scheme=$this->sessionEnvironment?->read('FMONITOR_TRUSTED_REQUEST_SCHEME');if(!\in_array($scheme,['http','https'],true))return$this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);$cookieName=\preg_match('/:(\d{1,5})$/D',$r->host,$m)===1?'fm2auth_'.$m[1]:'fm2auth';$incoming=null;if(\preg_match('/(?:^|;\s*)'.\preg_quote($cookieName,'/').'=([A-Za-z0-9,-]{16,128})(?:;|$)/',(string)($r->server['HTTP_COOKIE']??''),$cookie)===1)$incoming=$cookie[1];$started=$this->sessionStorage->start($incoming);if($started->status()===\FMonitor\IdentityAccess\PilotSessionOperationStatus::UNAVAILABLE)return$this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);$id=$started->currentSessionId();if($id===null){$anonymous=$this->sessionStorage->start(null);$id=$anonymous->currentSessionId();if($id===null)return$this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);}$csrf=\bin2hex(\random_bytes(32));$committed=$this->sessionStorage->writeCommit($id,'auth_csrf|s:64:"'.$csrf.'";');if($committed->status()!==\FMonitor\IdentityAccess\PilotSessionOperationStatus::OK)return$this->response(503,"Service unavailable.\n",['Retry-After'=>'60'],$r->method);if($r->path==='/pilot/login'){$body='<!doctype html><form><input name="csrfToken" value="'.$csrf.'"></form>';$secure=$scheme==='https'?'; Secure':'';return$this->response(200,$body,['Content-Type'=>'text/html; charset=UTF-8','Set-Cookie'=>$cookieName.'='.$id.'; Max-Age=604800; Path=/pilot; HttpOnly; SameSite=Strict'.$secure],$r->method);}return$this->reads->handle($r);
    }
    private function knownPath(string$path):bool{return \in_array($path,['/pilot','/pilot/login','/pilot/admin/users','/pilot/admin/users/invite','/pilot/objects','/pilot/'],true)||\str_starts_with($path,'/pilot/assets/');}
    private function json(int $status,array $payload):PilotHttpResponse{return $this->response($status,\json_encode($payload,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_THROW_ON_ERROR),['Content-Type'=>'application/json; charset=UTF-8']);}
}
