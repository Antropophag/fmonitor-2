<?php
declare(strict_types=1);
namespace FMonitor2\InstallationProcess;

final class OtizSettlementV2SchemaMigration
{
 public static function apply(\mysqli $db,string $prefix):array
 {
  if(preg_match('/^[A-Za-z0-9_]{0,25}$/D',$prefix)!==1)throw new \InvalidArgumentException('INVALID_TABLE_PREFIX');
  $defs=[
   'fm2_otiz_calculation_revisions'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,revision INT UNSIGNED NOT NULL DEFAULT 1,status VARCHAR(24) NOT NULL,report_date DATE NOT NULL,actor_user_id BIGINT UNSIGNED NOT NULL,input_json LONGTEXT NOT NULL,projection_json LONGTEXT NOT NULL,replaces_calculation_id BIGINT UNSIGNED NULL,created_at VARCHAR(40) NOT NULL,updated_at VARCHAR(40) NOT NULL,PRIMARY KEY(id),KEY status_report(status,report_date),CONSTRAINT calc_status CHECK(status IN (\'draft\',\'accepted\',\'cancelled\',\'deleted\'))',
   'fm2_otiz_entitlement_claims'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,calculation_id BIGINT UNSIGNED NOT NULL,object_id BIGINT UNSIGNED NOT NULL,source_kind VARCHAR(40) NOT NULL,source_id VARCHAR(120) NOT NULL,source_revision VARCHAR(80) NOT NULL,entitlement_kind VARCHAR(40) NOT NULL,recognition_date DATE NOT NULL,gross_cents BIGINT NOT NULL,active TINYINT NOT NULL DEFAULT 1,active_claim_key CHAR(64) GENERATED ALWAYS AS (IF(active=1,SHA2(CONCAT_WS(\'|\',object_id,source_kind,source_id,source_revision,entitlement_kind,recognition_date),256),NULL)) STORED,PRIMARY KEY(id),UNIQUE KEY active_entitlement(active_claim_key),KEY claim_lineage(object_id,source_kind,source_id,source_revision,entitlement_kind,recognition_date,calculation_id),KEY calculation(calculation_id),CONSTRAINT claim_calc FOREIGN KEY(calculation_id) REFERENCES @calc(id),CONSTRAINT claim_amount CHECK(gross_cents>=0),CONSTRAINT claim_active CHECK(active IN(0,1))',
   'fm2_otiz_recipient_obligations'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,calculation_id BIGINT UNSIGNED NOT NULL,object_id BIGINT UNSIGNED NOT NULL,employee_id VARCHAR(120) NOT NULL,tab_number VARCHAR(80) NOT NULL,full_name VARCHAR(255) NOT NULL,amount_cents BIGINT NOT NULL,PRIMARY KEY(id),KEY calculation(calculation_id),KEY object_id(object_id),CONSTRAINT obligation_calc FOREIGN KEY(calculation_id) REFERENCES @calc(id),CONSTRAINT obligation_amount CHECK(amount_cents>=0)',
   'fm2_otiz_deductions'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,calculation_id BIGINT UNSIGNED NOT NULL,object_id BIGINT UNSIGNED NOT NULL,employee_id VARCHAR(120) NULL,amount_cents BIGINT NOT NULL,reason VARCHAR(500) NOT NULL,document VARCHAR(255) NOT NULL,active TINYINT NOT NULL DEFAULT 1,created_at VARCHAR(40) NOT NULL,PRIMARY KEY(id),KEY calculation(calculation_id),CONSTRAINT deduction_calc FOREIGN KEY(calculation_id) REFERENCES @calc(id),CONSTRAINT deduction_amount CHECK(amount_cents>=0),CONSTRAINT deduction_active CHECK(active IN(0,1))',
   'fm2_otiz_payment_decisions'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,calculation_id BIGINT UNSIGNED NOT NULL,employee_id VARCHAR(120) NOT NULL,decision VARCHAR(24) NOT NULL,reason VARCHAR(500) NOT NULL,active TINYINT NOT NULL DEFAULT 1,created_at VARCHAR(40) NOT NULL,PRIMARY KEY(id),KEY calculation(calculation_id),CONSTRAINT decision_calc FOREIGN KEY(calculation_id) REFERENCES @calc(id),CONSTRAINT decision_value CHECK(decision IN(\'pay\',\'do_not_pay\')),CONSTRAINT decision_active CHECK(active IN(0,1)),CONSTRAINT decision_reason CHECK(decision=\'pay\' OR CHAR_LENGTH(TRIM(reason))>0)',
   'fm2_otiz_payment_facts'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,calculation_id BIGINT UNSIGNED NOT NULL,amount_cents BIGINT NOT NULL,payment_date DATE NOT NULL,created_at VARCHAR(40) NOT NULL,PRIMARY KEY(id),KEY calculation(calculation_id),CONSTRAINT payment_calc FOREIGN KEY(calculation_id) REFERENCES @calc(id),CONSTRAINT payment_amount CHECK(amount_cents>=0)',
   'fm2_otiz_payment_reversals'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,payment_id BIGINT UNSIGNED NOT NULL,amount_cents BIGINT NOT NULL,reason VARCHAR(500) NOT NULL,created_at VARCHAR(40) NOT NULL,PRIMARY KEY(id),UNIQUE KEY payment(payment_id),CONSTRAINT reversal_payment FOREIGN KEY(payment_id) REFERENCES @payment(id),CONSTRAINT reversal_amount CHECK(amount_cents>=0)',
   'fm2_otiz_admission_inputs'=>'object_id BIGINT UNSIGNED NOT NULL,source_revision VARCHAR(120) NOT NULL,decision VARCHAR(24) NOT NULL,reason_code VARCHAR(80) NULL,observed_at VARCHAR(40) NOT NULL,incident_id VARCHAR(120) NULL,operation_id CHAR(36) NULL,producer_user_id BIGINT UNSIGNED NULL,evidence_json LONGTEXT NULL,PRIMARY KEY(object_id,source_revision),UNIQUE KEY admission_operation(operation_id),KEY incident_lineage(object_id,incident_id,observed_at),KEY object_latest(object_id,observed_at),CONSTRAINT admission_decision CHECK(decision IN(\'allow\',\'blocked\',\'unknown\')),CONSTRAINT admission_reason CHECK(decision<>\'blocked\' OR CHAR_LENGTH(TRIM(reason_code))>0)',
   'fm2_otiz_unlinked_legacy_totals'=>'object_id BIGINT UNSIGNED NOT NULL,amount_cents BIGINT NOT NULL,source_ref VARCHAR(255) NOT NULL,observed_at VARCHAR(40) NOT NULL,PRIMARY KEY(object_id,source_ref),CONSTRAINT legacy_total_amount CHECK(amount_cents>=0),CONSTRAINT legacy_total_source CHECK(CHAR_LENGTH(TRIM(source_ref))>0)',
   'fm2_otiz_v2_operations'=>'actor_user_id BIGINT UNSIGNED NOT NULL,operation_id CHAR(36) NOT NULL,request_sha256 CHAR(64) NOT NULL,result_json LONGTEXT NOT NULL,created_at VARCHAR(40) NOT NULL,PRIMARY KEY(actor_user_id,operation_id)',
   'fm2_otiz_v2_events'=>'id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,calculation_id BIGINT UNSIGNED NULL,event_type VARCHAR(40) NOT NULL,payload_json LONGTEXT NOT NULL,actor_user_id BIGINT UNSIGNED NOT NULL,created_at VARCHAR(40) NOT NULL,PRIMARY KEY(id),KEY calculation(calculation_id)'];
  $created=[];foreach($defs as$name=>$columns){$table=$prefix.$name;$exists=(int)$db->query("SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='".$db->real_escape_string($table)."'")->fetch_column();if(!$exists){$columns=str_replace(['@calc','@payment'],['`'.$prefix.'fm2_otiz_calculation_revisions`','`'.$prefix.'fm2_otiz_payment_facts`'],$columns);$db->query("CREATE TABLE `{$table}`({$columns}) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci");$created[]=$table;}}
  return['applied'=>$created!==[],'schemaVersion'=>36,'tablesCreated'=>$created];
 }
}
