<?php
declare(strict_types=1);
require dirname(__DIR__).'/bootstrap.php';
require_once dirname(__DIR__,2).'/app/AssignmentOrderOriginal/AssignmentOrderOriginalRuntime.php';
require_once dirname(__DIR__).'/Support/SelectionHttpFixture.php';
use FMonitor2\Tests\Support\SelectionHttpFixture;
$f=null;
try{
 $f=new SelectionHttpFixture(true,static fn():array=>['FMONITOR_NOW'=>'2026-09-18T12:00:00+03:00']);$db=$f->original->selection->db;
 FMonitor2\InstallationProcess\LegacyControlEngineerMigrationSchema::apply($db,'');FMonitor2\InstallationProcess\LegacyControlEngineerImportSchemaMigration::apply($db,'');$db->query('DELETE FROM fm2_control_engineer_assignments');$db->query('ALTER TABLE fm_maintable ADD zavnumber VARCHAR(120) NULL');
 $db->query("INSERT INTO fm2_pilot_role_permissions(role_id,permission)VALUES(4,'access.administer')ON DUPLICATE KEY UPDATE permission=VALUES(permission)");$db->query("INSERT INTO fm2_pilot_users(full_name,email,phone,status,activation_state,session_version,source_updated_at)VALUES('Инженер Импорт','engineer.pending@shlz.ru','',1,'pending_invitation',1,'2026-09-18T09:00:00Z')");$engineer=(int)$db->insert_id;$role=(int)$db->query("SELECT role_id FROM fm2_pilot_roles WHERE code='construction_control_engineer'")->fetch_column();$db->query("INSERT INTO fm2_pilot_user_roles VALUES({$engineer},{$role},'legacy_import','2026-09-18T09:00:00Z',NULL)");$linkRequest='20260918-0000-4000-8000-000000000077';$db->query("INSERT INTO fm2_legacy_identity_links(local_user_id,legacy_user_id,legacy_name_snapshot,legacy_email_snapshot,legacy_status_snapshot,legacy_role_id_snapshot,legacy_role_status_snapshot,linked_by_user_id,linked_at_utc,request_id,request_fingerprint)VALUES({$engineer},77,'Инженер Импорт','engineer.pending@shlz.ru',1,16,1,99,'2026-09-18 09:00:00','{$linkRequest}','".str_repeat('b',64)."')");
 $users=$f->request('GET','/pilot/admin/users','',99);assertSameValue(200,$users['status'],'directory');foreach(['Инженер Импорт','Ожидает приглашения']as$m)assertSameValue(true,str_contains($users['body'],$m),'directory '.$m);assertSameValue(false,str_contains($users['body'],'/pilot/activate?token='),'no fabricated token');if(preg_match('#action="/pilot/admin/users/'.$engineer.'/invitation".*?name="csrfToken" value="([0-9a-f]{32})"#s',$users['body'],$form)!==1)throw new TestFailure('pending invitation action');$origin=['Origin'=>'http://127.0.0.1:'.$f->port,'Sec-Fetch-Site'=>'same-origin'];assertSameValue(403,$f->request('POST',"/pilot/admin/users/{$engineer}/invitation",http_build_query(['csrfToken'=>$form[1]]),18,$origin)['status'],'forbidden invite');assertSameValue(0,(int)$db->query("SELECT COUNT(*) FROM fm2_pilot_invitations WHERE user_id={$engineer}")->fetch_column(),'denied no facts');assertSameValue(303,$f->request('POST',"/pilot/admin/users/{$engineer}/invitation",http_build_query(['csrfToken'=>$form[1]]),99,$origin)['status'],'issue invite');assertSameValue([1,'invited',1,1],[(int)$db->query("SELECT COUNT(*) FROM fm2_pilot_invitations WHERE user_id={$engineer} AND used_at IS NULL AND revoked_at IS NULL")->fetch_column(),(string)$db->query("SELECT activation_state FROM fm2_pilot_users WHERE user_id={$engineer}")->fetch_column(),(int)$db->query("SELECT COUNT(*) FROM fm2_pilot_auth_credentials WHERE user_id={$engineer} AND password_hash IS NULL")->fetch_column(),(int)$db->query("SELECT COUNT(*) FROM fm2_legacy_identity_links WHERE local_user_id={$engineer} AND legacy_user_id=77 AND supersedes_link_id IS NULL")->fetch_column()],'invitation creates empty credential and preserves identity');$issued=$f->request('GET','/pilot/admin/users','',99);if(preg_match('#/pilot/activate\?token=([A-Za-z0-9_-]{43})#',$issued['body'],$token)!==1)throw new TestFailure('issued token visible once');$password='Safe imported engineer passphrase 2026!';$activated=$f->request('POST','/pilot/activate',http_build_query(['token'=>$token[1],'password'=>$password,'passwordConfirmation'=>$password]),null);assertSameValue(true,str_contains($activated['body'],'Учётная запись активирована'),'imported engineer activates');$credential=$db->query("SELECT u.activation_state,c.password_hash FROM fm2_pilot_users u JOIN fm2_pilot_auth_credentials c ON c.user_id=u.user_id WHERE u.user_id={$engineer}")->fetch_assoc();assertSameValue(['active',true],[$credential['activation_state'],password_verify($password,(string)$credential['password_hash'])],'same imported user gains credential');
 echo "PASS LEGACY-CONTROL-ENGINEER-IMPORT-001 invitation HTTP lifecycle\n";
}finally{if($f instanceof SelectionHttpFixture)$f->close();}
