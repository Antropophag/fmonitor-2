const fs=require('node:fs'),crypto=require('node:crypto');
const c=JSON.parse(fs.readFileSync(process.argv[2],'utf8')),{chromium}=require(c.playwright);
(async()=>{const browser=await chromium.launch({headless:true}),evidence={viewports:{},screenshots:{}};try{for(const width of[1440,390]){const context=await browser.newContext({viewport:{width,height:1000}}),page=await context.newPage();await page.goto(c.origin+'/pilot/login');await page.locator('[name=email]').fill(c.email);await page.locator('[name=email]').locator('xpath=ancestor::form').locator('button[type=submit]').click();await page.locator('[name=password]').fill(c.password);await page.locator('[name=password]').locator('xpath=ancestor::form').locator('button[type=submit]').click();const response=await page.goto(c.origin+'/pilot/dashboard');if(response.status()!==200)throw new Error('dashboard status '+response.status());for(const title of['Объекты по этапам процесса','Плановая нагрузка на 6 недель','Давность последней подтверждённой активности'])if(!(await page.locator('main').innerText()).includes(title))throw new Error('missing '+title);const bars=page.locator('[data-dashboard-bar]');if(await bars.count()!==23)throw new Error('expected 23 bars, got '+await bars.count());const first=bars.first();await first.focus();const name=await first.getAttribute('aria-label');if(!name||!name.includes(':')||!name.match(/\d/))throw new Error('bar accessible name '+name);if(!await first.getAttribute('href'))throw new Error('bar has no drill-down href');const focus=await first.evaluate(e=>getComputedStyle(e).outlineStyle!=='none'||getComputedStyle(e).boxShadow!=='none');if(!focus)throw new Error('bar focus is not visible');const geometry=await page.evaluate(()=>({scroll:document.documentElement.scrollWidth,viewport:document.documentElement.clientWidth,widgets:document.querySelectorAll('[data-dashboard-chart]').length,labels:[...document.querySelectorAll('[data-dashboard-bar]')].map(e=>({text:e.textContent.trim(),name:e.getAttribute('aria-label'),href:e.getAttribute('href')})),clipped:[...document.querySelectorAll('main [data-dashboard-chart],main [data-dashboard-bar]')].some(e=>{const r=e.getBoundingClientRect();return r.left<0||r.right>document.documentElement.clientWidth+1})}));if(geometry.widgets!==3||geometry.scroll>geometry.viewport+1||geometry.clipped)throw new Error('responsive geometry '+JSON.stringify(geometry));const shot=c.artifacts+`/dashboard-bar-charts-${width}.png`;await page.screenshot({path:shot,fullPage:true});const bytes=fs.readFileSync(shot);evidence.viewports[width]=geometry;evidence.screenshots[width]={path:shot,sha256:crypto.createHash('sha256').update(bytes).digest('hex'),bytes:bytes.length};await context.close();}fs.writeFileSync(c.result,JSON.stringify(evidence));}finally{await browser.close();}})().catch(e=>{process.stderr.write(e.stack||String(e));process.exit(1);});
