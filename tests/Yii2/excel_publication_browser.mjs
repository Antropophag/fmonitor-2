import fs from 'node:fs';
import {createRequire} from 'node:module';
const [port,modulePath,artifacts,resultPath,fkrEmail,otizEmail,password,pdfPath]=process.argv.slice(2);
const {chromium}=createRequire(import.meta.url)(modulePath);
const browser=await chromium.launch({headless:true});
const base=`http://127.0.0.1:${port}`;const result={};
try{
 const login=async(email)=>{const context=await browser.newContext({viewport:{width:390,height:844},acceptDownloads:true});const page=await context.newPage();page.setDefaultTimeout(12000);await page.goto(base+'/pilot/login');await page.locator('[name=email]').fill(email);await page.locator('button[type=submit]').click();await page.locator('[name=password]').fill(password);await page.locator('button[type=submit]').click();await page.waitForURL(/\/pilot\/objects/);return page;};
 const fkr=await login(fkrEmail);await fkr.goto(base+'/pilot/objects/4512');await fkr.locator('a[href="/pilot/objects/4512/deadline-certificates"]').click();if(await fkr.locator('[name=sourceLabel], [name=sourceLocator]').count())throw new Error('RED_ASSERTION certificate upload must not require technical provenance fields');await fkr.locator('[name=certificateDate]').fill('2026-09-14');await fkr.locator('[name=newDeadline]').fill('2026-09-12');await fkr.locator('[name=pdf]').setInputFiles(pdfPath);result.certificateRequest=await fkr.locator('[name=requestId]').inputValue();await fkr.locator('form').filter({has:fkr.locator('[name=pdf]')}).locator('button[type=submit]').click();await fkr.waitForURL(/deadline-certificates$/);await fkr.locator('a[href$="/pdf"]').waitFor();result.certificateUploaded=true;
 const page=await login(otizEmail);await page.goto(base+'/pilot/otiz/payments');await page.locator('[name=reportDate]').fill('2026-09-14');await page.getByRole('button',{name:'Подготовить расчёт'}).click();await page.waitForURL(/snapshots\/\d+\?created=1/);result.firstSnapshot=Number(new URL(page.url()).pathname.split('/').pop());
 await page.getByText('Как рассчитана сумма',{exact:true}).click();const text=await page.locator('body').innerText();for(const label of ['Выплачено ранее','Остаток до штрафа','Штраф за просрочку','Справка о переносе срока','Исходный плановый срок','2026-09-05','2026-09-12','ПТО отсутствует'])if(!text.includes(label))throw new Error('Missing trace label '+label);const normalized=text.replace(/\s/g,'');for(const value of ['13000,00','260,00','12740,00'])if(!normalized.includes(value))throw new Error('Missing exact first trace value '+value);if(await page.locator('a[href*="deadline-certificates/"][href$="/pdf"]').count()!==1)throw new Error('Missing exact certificate revision link');result.trace=text;
 await page.screenshot({path:artifacts+'/excel-mobile-draft.png',fullPage:true});await page.getByRole('button',{name:'Подтвердить расчёт'}).click();await page.waitForURL(/accepted=1/);const downloadPromise=page.waitForEvent('download');await page.getByRole('link',{name:'Скачать реестр XLSX'}).click();await(await downloadPromise).saveAs(artifacts+'/excel.xlsx');await page.getByRole('button',{name:'Отметить выплаты выполненными'}).click();await page.waitForURL(/paid=1/);
 await page.goto(base+'/pilot/otiz/payments');await page.locator('[name=reportDate]').fill('2026-09-14');await page.getByRole('button',{name:'Подготовить расчёт'}).click();await page.waitForURL(/snapshots\/\d+\?created=1/);result.secondSnapshot=Number(new URL(page.url()).pathname.split('/').pop());await page.screenshot({path:artifacts+'/excel-mobile-recalculated.png',fullPage:true});result.completed=true;fs.writeFileSync(resultPath,JSON.stringify(result));
}finally{await browser.close();}
