<?php
declare(strict_types=1);require dirname(__DIR__).'/bootstrap.php';require __DIR__.'/InspectionFixture.php';$f=null;try{$f=new InspectionFixture(dirname(__DIR__,2));$f->open();$h=$f->http;$page=$f->page();$csrf=InspectionFixture::csrf($page);$original=InspectionFixture::operation();$first=InspectionFixture::result($f->send($original,$csrf),200,'accepted');$h->db->query("UPDATE {$h->p}fm2_installation_cases SET process_state='completed' WHERE id=6101");$before=$h->facts();$op=InspectionFixture::operation(2,$first['revision']);$op['type']='completion_retracted';$op['originalClientOperationId']=$original['clientOperationId'];$op['reason']='Недоступно';$r=$f->send($op,$csrf);assertSameValue(409,$r['status'],'INTENDED_RED completed retraction is domain rejection');assertSameValue($before,$h->facts(),'no facts');echo"PASS completed retraction admission\n";}finally{if($f instanceof InspectionFixture)$f->close();}
