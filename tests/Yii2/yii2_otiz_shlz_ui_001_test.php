<?php
declare(strict_types=1);
require dirname(__DIR__).'/bootstrap.php';
require dirname(__DIR__,2).'/app/autoload.php';
require __DIR__.'/Yii2AuthFixture.php';

use FMonitor2\Tests\Yii2\Yii2AuthFixture;

// OTIZ-SHLZ-UI-001 A1-A7: real authenticated Yii2/Chromium presentation seam.
$root=dirname(__DIR__,2);$fixture=null;$server=null;$browser=null;
$artifacts=sys_get_temp_dir().'/fmonitor-otiz-shlz-ui-'.bin2hex(random_bytes(6));mkdir($artifacts,0700);
try{
    $fixture=new Yii2AuthFixture($root);$fixture->setPermission('otiz.manage');$db=$fixture->db;$p=$fixture->prefix;$hash=str_repeat('e',64);
    $legacy=$db->prepare("INSERT INTO {$p}fm_maintable(id,ordadr_address,regnumber) VALUES(?,?,?)");$detail=$db->prepare("INSERT INTO {$p}fm2_pilot_object_details(object_id,schema_version,content_sha256,payload_json,captured_at) VALUES(?,'technical-object-detail-v1',?,?,'2026-09-01T09:00:00Z')");
    for($index=1;$index<=52;$index++){$id=8000+$index;$address='Адрес реестра '.str_pad((string)$index,2,'0',STR_PAD_LEFT);$registration='OTIZ-'.str_pad((string)$index,3,'0',STR_PAD_LEFT);$legacy->bind_param('iss',$id,$address,$registration);$legacy->execute();$material=$index===51?'41':($index===52?'999':'86');$payload=json_encode(['schemaVersion'=>'technical-object-detail-v1','objectId'=>$id,'fields'=>['floors'=>['raw'=>'5'],'weight'=>['raw'=>'320'],'pitmaterial'=>['raw'=>$material,'display'=>$material],'lift_type'=>['display'=>'Пассажирский']]],JSON_UNESCAPED_UNICODE|JSON_THROW_ON_ERROR);$detailHash=hash('sha256',$payload);$detail->bind_param('iss',$id,$detailHash,$payload);$detail->execute();}$legacy->close();$detail->close();
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshots(id,report_date,status,rules_version,calculated_at,calculated_by_user_id,accepted_at,accepted_by_user_id,total_pool_cents,total_closed_cents,total_available_cents,content_hash) VALUES(501,'2026-09-30','accepted','premium-calculation-v2-excel','2026-10-01T09:00:00+03:00',9101,'2026-10-01T10:00:00+03:00',9101,100000,0,100000,'{$hash}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_objects(snapshot_id,object_id,regnumber,address,previous_progress_bp,current_progress_bp,progress_fact_date,premium_cents,shaft_bp,kss_bp,accrued_cents,fund_cents,closed_before_cents,remaining_cents,pool_cents,distributed_cents,undistributed_cents,calculation_state,inputs_json) VALUES(501,7301,'BROWSER-LONG-OBJECT-IDENTITY','Очень длинный адрес объекта для проверки переноса содержимого на узком экране без потери связи с расчётом',0,10000,'2026-09-30',100000,10000,10000,100000,100000,0,100000,100000,100000,0,'ready','{}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_allocations(snapshot_id,object_id,tab_id,full_name,position_name,contribution_bp,base_ktu_bp,adjustment_ktu_bp,effective_ktu_bp,share_bp,amount_cents,employment_status,participation_basis) VALUES(501,7301,'7001','Browser Installer','Installer',10000,10000,0,10000,10000,100000,'employed','Recorded work')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_issues(snapshot_id,object_id,severity,issue_code,message,owner_role) VALUES(501,7301,'warning','UI_WARNING','Synthetic warning retained','OTIZ owner with long responsibility label')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_payment_closures(snapshot_id,object_id,closed_on,paid_cents,discipline_cents,deadline_cents,basis,artifact,created_by_user_id,created_at) VALUES(501,7301,'2026-10-02',0,10000,0,'Long retained discipline basis for contained ledger verification','evidence://ui-fixture',9101,'2026-10-02T10:00:00+03:00')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshots(id,report_date,status,rules_version,calculated_at,calculated_by_user_id,total_pool_cents,total_closed_cents,total_available_cents,content_hash) VALUES(502,'2026-10-31','draft','premium-calculation-v2-excel','2026-11-01T09:00:00+03:00',9101,0,0,0,'{$hash}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_objects(snapshot_id,object_id,regnumber,address,previous_progress_bp,current_progress_bp,progress_fact_date,premium_cents,shaft_bp,kss_bp,accrued_cents,fund_cents,closed_before_cents,remaining_cents,pool_cents,distributed_cents,undistributed_cents,calculation_state,inputs_json) VALUES(502,7302,'BLOCKED-EMPTY-OBJECT','Address without allocations or closures',0,0,'2026-10-31',0,10000,10000,0,0,0,0,0,0,0,'blocked','{}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_issues(snapshot_id,object_id,severity,issue_code,message,owner_role) VALUES(502,7302,'blocker','MISSING_EVIDENCE','Required evidence is absent','ОТиЗ')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshots(id,report_date,status,rules_version,calculated_at,calculated_by_user_id,total_pool_cents,total_closed_cents,total_available_cents,content_hash) VALUES(503,'2026-11-30','draft','premium-calculation-v2-excel','2026-12-01T09:00:00+03:00',9101,100000,0,100000,'{$hash}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_objects(snapshot_id,object_id,regnumber,address,previous_progress_bp,current_progress_bp,progress_fact_date,premium_cents,shaft_bp,kss_bp,accrued_cents,fund_cents,closed_before_cents,remaining_cents,pool_cents,distributed_cents,undistributed_cents,calculation_state,inputs_json) VALUES(503,7303,'DRAFT-READY','Ready draft address',0,10000,'2026-11-30',100000,10000,10000,100000,100000,0,100000,100000,100000,0,'ready','{}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_allocations(snapshot_id,object_id,tab_id,full_name,position_name,contribution_bp,base_ktu_bp,adjustment_ktu_bp,effective_ktu_bp,share_bp,amount_cents,employment_status,participation_basis) VALUES(503,7303,'7002','Draft Installer','Installer',10000,10000,0,10000,10000,100000,'employed','Recorded work')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshots(id,report_date,status,rules_version,calculated_at,calculated_by_user_id,total_pool_cents,total_closed_cents,total_available_cents,content_hash) VALUES(504,'2026-12-31','draft','premium-calculation-v2-excel','2027-01-01T09:00:00+03:00',9101,100,30,70,'{$hash}')");
    $trace=json_encode(['premiumCalculation'=>['formulaTrace'=>[['step'=>'progress','resultCents'=>150]]]],JSON_THROW_ON_ERROR);$traceSql=$db->real_escape_string($trace);$db->query("INSERT INTO {$p}fm2_pilot_otiz_snapshot_objects(snapshot_id,object_id,regnumber,address,previous_progress_bp,current_progress_bp,progress_fact_date,premium_cents,shaft_bp,kss_bp,accrued_cents,fund_cents,closed_before_cents,remaining_cents,pool_cents,distributed_cents,undistributed_cents,calculation_state,inputs_json) VALUES(504,8001,'OTIZ-001','Адрес реестра 01',0,0,'2026-12-31',52000000,11500,10000,100,59800000,0,100,100,0,100,'ready','{$traceSql}')");
    $db->query("INSERT INTO {$p}fm2_pilot_otiz_payment_closures(snapshot_id,object_id,closed_on,paid_cents,discipline_cents,deadline_cents,basis,artifact,created_by_user_id,created_at) VALUES(504,8001,'2027-01-02',0,10,20,'Stored versus trace row balance fixture','',9101,'2027-01-02T10:00:00+03:00')");
    $factFingerprint=static function()use($db,$p):string{$tables=['fm2_pilot_otiz_snapshots','fm2_pilot_otiz_snapshot_objects','fm2_pilot_otiz_snapshot_allocations','fm2_pilot_otiz_snapshot_issues','fm2_pilot_otiz_payment_closures','fm2_pilot_otiz_events','fm2_otiz_publications','fm2_otiz_settlement_operations'];$facts=[];foreach($tables as$table){$rows=$db->query("SELECT * FROM {$p}{$table}")->fetch_all(MYSQLI_ASSOC);$encoded=array_map(static fn(array$row):string=>json_encode($row,JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_THROW_ON_ERROR),$rows);sort($encoded,SORT_STRING);$facts[$table]=$encoded;}return hash('sha256',json_encode($facts,JSON_THROW_ON_ERROR));};$readOnlyBefore=$factFingerprint();
    $listener=stream_socket_server('tcp://127.0.0.1:0',$errno,$error);if(!is_resource($listener))throw new TestFailure('SETUP_FAILURE: port allocation');$address=stream_socket_get_name($listener,false);$port=(int)substr($address,strrpos($address,':')+1);fclose($listener);
    $env=getenv();foreach(array_keys($env)as$key)if(str_starts_with($key,'FMONITOR_'))unset($env[$key]);$env=array_replace($env,$fixture->environment(),['FMONITOR_TRUSTED_REQUEST_HOST'=>'127.0.0.1:'.$port]);
    $server=proc_open([PHP_BINARY,'-d','display_errors=0','-S','127.0.0.1:'.$port,$root.'/public/yii.php'],[0=>['file','/dev/null','r'],1=>['file',$artifacts.'/server.log','a'],2=>['file',$artifacts.'/server.log','a']],$pipes,$root,$env);if(!is_resource($server))throw new TestFailure('SETUP_FAILURE: Yii server');
    $ready=false;$deadline=microtime(true)+5;do{$socket=@fsockopen('127.0.0.1',$port,$errno,$error,.1);if(is_resource($socket)){fclose($socket);$ready=true;break;}usleep(20000);}while(microtime(true)<$deadline);assertSameValue(true,$ready,'SETUP_FAILURE: Yii listener ready');
    $config=['origin'=>'http://127.0.0.1:'.$port,'email'=>$fixture->email,'password'=>$fixture->password,'artifacts'=>$artifacts,'result'=>$artifacts.'/result.json','playwright'=>getenv('FMONITOR_TEST_PLAYWRIGHT_MODULE')?:dirname($root).'/shlz-ui/node_modules/playwright'];$configPath=$artifacts.'/config.json';file_put_contents($configPath,json_encode($config,JSON_THROW_ON_ERROR));chmod($configPath,0600);
    $browser=proc_open([getenv('FMONITOR_TEST_NODE_BINARY')?:'node',__DIR__.'/otiz_shlz_ui_browser.mjs',$configPath],[0=>['file','/dev/null','r'],1=>['file',$artifacts.'/browser.log','a'],2=>['file',$artifacts.'/browser.log','a']],$pipes,$root);if(!is_resource($browser))throw new TestFailure('SETUP_FAILURE: browser');
    $deadline=microtime(true)+60;$readObserved=false;$permissionDenied=false;$permissionFacts=null;$permissionChecked=false;$permissionRestored=false;$acceptFacts=null;$acceptChecked=false;$emptyObserved=false;do{$state=proc_get_status($browser);if(!$readObserved&&is_file($artifacts.'/read-check')){assertSameValue($readOnlyBefore,$factFingerprint(),'GET and HEAD preserve all OTIZ facts');file_put_contents($artifacts.'/read-ready','ok');$readObserved=true;}if(!$permissionDenied&&is_file($artifacts.'/permission-request')){$permissionFacts=$factFingerprint();$db->query("DELETE FROM {$p}fm2_pilot_role_permissions WHERE role_id=9201");file_put_contents($artifacts.'/permission-denied','ok');$permissionDenied=true;}if($permissionDenied&&!$permissionChecked&&is_file($artifacts.'/permission-action-done')){assertSameValue($permissionFacts,$factFingerprint(),'forbidden malformed POST and denied GET add no OTIZ facts');file_put_contents($artifacts.'/permission-facts-ready','ok');$permissionChecked=true;}if($permissionChecked&&!$permissionRestored&&is_file($artifacts.'/permission-restore-request')){$db->query("INSERT INTO {$p}fm2_pilot_role_permissions(role_id,permission) VALUES(9201,'otiz.manage')");file_put_contents($artifacts.'/permission-restored','ok');$permissionRestored=true;}if($permissionRestored&&$acceptFacts===null&&is_file($artifacts.'/accept-first-done')){$acceptFacts=$factFingerprint();file_put_contents($artifacts.'/accept-first-ready','ok');}if($acceptFacts!==null&&!$acceptChecked&&is_file($artifacts.'/accept-repeat-done')){assertSameValue($acceptFacts,$factFingerprint(),'repeated accepted snapshot POST adds no snapshot or event facts');file_put_contents($artifacts.'/accept-repeat-ready','ok');$acceptChecked=true;}if(!$emptyObserved&&is_file($artifacts.'/empty-request')){$before=[(int)$db->query("SELECT COUNT(*) FROM {$p}fm2_pilot_otiz_snapshots")->fetch_column(),(int)$db->query("SELECT COUNT(*) FROM {$p}fm2_pilot_otiz_events")->fetch_column()];$db->query("DELETE FROM {$p}fm2_pilot_otiz_snapshot_issues");$db->query("DELETE FROM {$p}fm2_pilot_otiz_snapshot_allocations");$db->query("DELETE FROM {$p}fm2_pilot_otiz_payment_closures");$db->query("DELETE FROM {$p}fm2_pilot_otiz_snapshot_objects");$db->query("DELETE FROM {$p}fm2_pilot_otiz_snapshots");file_put_contents($artifacts.'/empty-ready',json_encode($before,JSON_THROW_ON_ERROR));$emptyObserved=true;}if(!$state['running'])break;usleep(20000);}while(microtime(true)<$deadline);if($state['running']){proc_terminate($browser,9);throw new TestFailure('SETUP_FAILURE: browser deadline');}$exit=$state['exitcode'];proc_close($browser);$browser=null;
    assertSameValue(0,$exit,'INTENDED_RED OTIZ shlz UI: '.file_get_contents($artifacts.'/browser.log').' evidence '.$artifacts);
    $observed=json_decode((string)file_get_contents($artifacts.'/result.json'),true,flags:JSON_THROW_ON_ERROR);assertSameValue('complete',$observed['stage'],'browser completed entire matrix');
    echo "PASS: OTIZ-SHLZ-UI-001 authenticated responsive browser seam; artifacts {$artifacts}\n";
}finally{if(is_resource($browser)){proc_terminate($browser,9);proc_close($browser);}if(is_resource($server)){proc_terminate($server);proc_close($server);}if($fixture instanceof Yii2AuthFixture)$fixture->close();if(is_file($artifacts.'/config.json'))unlink($artifacts.'/config.json');}
