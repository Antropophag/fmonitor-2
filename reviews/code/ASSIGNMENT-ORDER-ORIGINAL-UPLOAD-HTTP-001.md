# Code review: ASSIGNMENT-ORDER-ORIGINAL-UPLOAD-HTTP-001

- Reviewer: `/root/original_gate5`, independently tasked agent; did not author production code or tests.
- Implementation author: root implementation agent.
- Reviewed commit: `7cb79d04121b2c96544eb4448d2a26d3e0505829`, production diff against `051eaf0c33d257b29f3e5b99343af2e811b3c857`.
- Specification: HTTP v0.3; independent Gate 1 v3 APPROVED; inherited original, selected-composition and attempt-audit contracts.
- Approved test review: Gate 3 v2 plus bounded input corrections v3/v4; earlier records preserved.
- Verdict: `APPROVED` for the reviewed production code. Browser acceptance and integration completion remain separate outstanding evidence below.

## Findings

No blocking production findings in this bounded operator upload/correction review.

Reviewed the submission query/source/factory, original transport/body/resources/handler/view/client, fresh selection integration, route admission/CSP and rapid router changes against the approved tests and specification. The read owner validates the registered immutable selection and original lineage/backing through the existing StoredReader in one read-only snapshot. It neither reads private PDF bytes nor writes audit or domain facts. Template date uses the separately approved identity-bound date reader; correction defaults to the current original date. Local read and exact action grants remain distinct from the native process capability, and wrong object/order cannot fall back to a physical preparation writer.

HTTP obtains the actor from trusted session context, rejects malformed metadata before CSRF and local admission, and invokes only `createForSelections` / `submitAssignmentOrderOriginal` for mutations. Canonical base64/JSON round-trip rejects duplicate/extra keys and alternate encodings. Raw acquisition is bounded to 20 MiB plus one detection byte, compares observed length, and closes both native input and owned memory resources. Configuration constructs the real password-file fresh terminal reader and checks availability; it introduces no runtime grants, credentials, DDL or storage-directory creation. Native command results retain the existing eleven-field encoder and status mapping; local admission failures use the closed error envelope and allowlisted diagnostics.

Replay, immutable revisions, audit policy and no-apply/no-open ownership remain with the approved native application. The HTTP flow test detects duplicate revisions and changed non-original rows and exercises correction of an older accepted order after a new pending selection. Local/native grant tests distinguish upload, correction and read and preserve the existing repeated native denial audit semantics. Framing rejection evidence correctly distinguishes pre-PHP server rejection from adapter responses.

The view escapes snapshot content, uses public shlz controls and a disabled no-JavaScript fallback. The client sends the actual File, suppresses concurrent submissions, retains intent for unchanged network/503 retries and changes intent after input/4xx. User feedback uses textContent. CSP extension is limited to successful GET/HEAD submission HTML with the external script; error/result responses keep BASE. The router's narrowly scoped HEAD representation handling preserves decorated Content-Length while suppressing bytes. Small classes stay within the current architecture boundary; no new domain logic was added to rapid-pilot.

## Verification evidence

Inspected `/Users/antropophag/.local/state/fmonitor2-verification/original-http-20260907/final-green-manifest.json`, independently matched all six test-file SHA-256 values to the reviewed checkout, and read the corresponding logs. All six recorded commands exit 0: HTTP admission, bounds/exact 20 MiB, flow/replay/correction, permissions, prefill/manager, and Node client. These HTTP executions belong to the implementation agent; this reviewer does not claim to have rerun them.

Also inspected the three `selection_http_*-regression-green.log` files, `architecture-final.log` (seven rules PASS), `visual-final.log`, `focus-final.log`, and `impeccable-final.log` (empty findings). Independently ran `node tests/Verification/original_upload_client_001_test.mjs` and `git diff --check 051eaf0 7cb79d0`; both passed.

## Required changes and completion boundary

No required production changes from this review. This approval does not mark the child change Done or archive it: the operations record currently proves actual browser direct initial upload, but browser correction/retry and final layout evidence remain incomplete due the reported native file-picker control problem. Complete and record that acceptance before closing the UI slice. Literal full `make verify` at the reviewed source was still running when this record was written; no `VERIFY_OK` or launch/integration completion is asserted here. Parent general original history/download and assigned-engineer read policy remain outside this child review.

Only this review record was written by the reviewer; no production edits or commit were made.
