# Test review: OBJECT-DETAILS-EDITING-001

- Reviewer: independent Gate 3 reviewer `/root/gate3_review`; authored none of the specification or reviewed tests
- Test author: root delivery agent
- Reviewed source: base `d189102c8b080200436fcd566afa185bf13393e0` plus retained snapshot `/Users/antropophag/.local/share/fmonitor-2/delivery-harness/packages/20260921T154413Z-59da087ffd/snapshot/source.patch`, SHA-256 `59f9157a8793e37d5761ace76c76c7a47d6a0ade58d5f7da5c71ec3c596653c8` (candidate source `3096afaf09b7626986d8e2c0a59e1d08013b9c95bff3f35064ac7f6f6ce5aa45`)
- Agreed review scope / prior findings disposition: first Gate 3 review of the six intended-RED tests, their acceptance mapping and evidence, and the minimal Quality Graph capability ownership addition; no prior findings
- Specification: `specs/OBJECT-DETAILS-EDITING-001.md`
- Public seam: `ObjectDetailsEditApplication` command, canonical Yii `POST /pilot/objects/<positive-id>/details`, effective public readers, canonical migration/recovery, and rendered browser flow
- Red command and intended failure: six package-recorded commands listed under RED evidence below; all returned exit 255 at candidate source, but each stopped at its first sentinel/trigger assertion
- Verdict: `CHANGES_REQUESTED`

## Findings

1. **BLOCKER — the candidate does not cover the complete accepted behavior matrix.** The lifecycle marks Gate 2 tasks complete, but large normative areas have no executable assertion. `tests/InstallationProcess/object_details_editing_001_test.php:10-19` checks one normalization example and a few invalid scalars, but does not independently exercise fill and correction for every editable field, address/entrance bounds, reference code/display coherence, null clearing versus omitted/blank neighbors, duplicate/non-string/non-scalar keys, both plan fields before and after each fixing fact, decimal boundaries, or the factory-number 120-byte and zero-candidate semantics. `tests/InstallationProcess/object_details_editing_mariadb_001_test.php:14-24` has no inactive-user, missing-capability, out-of-scope-object, exact replay identity/outcome/event, real two-connection race, losing-command facts, immutable second edit, or actor/time snapshot checks. `tests/InstallationProcess/object_details_effective_consumers_001_test.php:9-15` tests only the new projection and direct table insertion: it does not run identical import or assert conflict/hash repeatability, and invokes none of the named card, queue/search/filter/pagination, new-document, Bitrix, ERP candidate/matching/fact freshness, or OTIZ norm/evidence consumers required by A9-A12. `tests/Yii2/yii2_object_details_editing_001_test.php:4-9` omits guest/read-only/inactive/scope authorization, CSRF and method rejection, conflict/unavailable outcome mapping, validation input retention, conditional field visibility, escaped old/new values, second immutable edit, and deterministic chronology beyond eight entries. Correction: add table-driven, observable public-seam cases for every A1-A15 row; use direct DB reads only to prove persisted facts/no-facts after invoking the owner/HTTP/import/consumer seam, not as a substitute for those seams.

2. **BLOCKER — four of six RED results are wrapper-only class-existence failures, so the captured evidence does not validate the tests behind the wrapper.** The records for `object_details_editing_001_test.php:8`, `object_details_editing_mariadb_001_test.php:10`, `object_details_effective_consumers_001_test.php:8`, and `object_details_editing_recovery_001_test.php:5` stop before any behavioral assertion. This proves only that the planned class is absent; constructor signatures, fixture setup, SQL, expected values and all later assertions remain unexecuted and could still be broken or vacuous. The Yii records similarly stop at the first missing markup trigger (`tests/Yii2/yii2_object_details_editing_001_test.php:5` and `tests/Yii2/yii2_object_details_editing_browser_001_test.php:5`), leaving their remaining assertions unvalidated. Correction: capture staged RED evidence that reaches each independent behavioral cluster (using the repository's established anti-wrapper intended-RED approach), and retain relevant failures showing missing behavior rather than setup errors. All six commands must remain deterministic and isolated when their complete bodies execute.

3. **BLOCKER — the alleged browser test is a static source-token inspection, not the specified browser/public UI seam.** `tests/Yii2/yii2_object_details_editing_browser_001_test.php:4-9` never starts Yii or a browser and never opens, cancels, backdrop/Escape-closes, submits, retains invalid values, reloads, or checks focus/touch behavior. It can pass with dead markup/CSS/JS, and `showModal`, `requestId`, and `expectedRevision` substring checks are not behavior. The planned `tests/Support/object_details_editing_browser.cjs` is neither present in the candidate nor executed by this wrapper. Correction: add the real registered Playwright flow against canonical Yii, exercising the two-group modal, unchanged card plus authorized icon, all close paths with zero writes, validation retention, successful reload and history pagination/escaping; keep source-shape checks only as supplemental checks.

4. **BLOCKER — concurrency, replay, and rejection assertions are not independently discriminating.** `tests/InstallationProcess/object_details_editing_mariadb_001_test.php:13` accepts a request ID argument but every outcome is checked only by status/revision, without original replay outcome/event identity or accepted-request row counts. Lines 20-23 conflate request-content conflict, stale revision, forbidden field, and authorization into broad statuses; the sole authorization denial does not distinguish active identity, exact capability, and object scope. No simultaneous transactions/connections are used, so A6's one-winner guarantee is untested. Correction: assert stable reason/outcome objects and exact before/after counts for overrides, requests, and events for every rejection; add a deterministic two-connection barrier/race and verify exactly one winner plus no loser success/replay facts.

5. **BLOCKER — A15 migration and recovery coverage is only a class sentinel plus source-string inventory.** `tests/Runtime/object_details_editing_recovery_001_test.php:5-7` does not apply a migration or perform backup/restore. It cannot detect invalid DDL, destructive partial repair, repeat/concurrent migration failure, missing indexes/constraints/auto-increment, runtime DDL/DML privilege violations, loss of overrides/events/requests, or failure of the next revision after restore. Correction: exercise fresh, repeat, exact-upgrade, compatible-partial, incompatible and concurrent migration states through the canonical migration seam, then perform the existing recovery round-trip and prove byte/fact preservation, replay continuity and a subsequent accepted write.

6. **HIGH — persisted event/history expectations are implementation-shaped and materially incomplete.** `tests/InstallationProcess/object_details_editing_mariadb_001_test.php:17-18` expects only an ordered list of three `field` keys, derived from the proposed JSON representation, while the contract requires independently meaningful old/new typed/raw/display values, stable labels/units/reference labels, object/case identity, actor ID/display snapshot, server time, and immutability after later changes. `tests/Yii2/yii2_object_details_editing_001_test.php:7` searches a few strings but does not establish old-to-new association or escaping. Correction: derive expected history from explicit fixture inputs and the specification's worked meaning, assert the complete observable event projection (without copying implementation output), mutate current users/references/object values afterward, and prove the first event remains unchanged and safely rendered.

7. **HIGH — the consumer test bypasses the state-changing public seam and therefore cannot establish integration behavior.** `tests/InstallationProcess/object_details_effective_consumers_001_test.php:12` inserts directly into the proposed table and only calls `MariaDbEffectiveObjectDetails`. This could pass while the application owner writes a different shape, import erases overrides, or every real consumer continues to read legacy values. Correction: seed the override through the reviewed application command, run the actual importer and each named public consumer, and assert effective value plus truthful field-level provenance; separately prove old snapshots/documents/payments remain byte-for-byte unchanged and Save makes no network/financial call.

8. **HIGH — HTTP security and no-write cases are incomplete.** `tests/Yii2/yii2_object_details_editing_001_test.php:4-8` tests one authorized POST and three validation cases only. It does not cover unauthenticated access, authenticated actor without exact capability, inactive actor, object-scope denial before existence disclosure, missing/invalid CSRF, GET/HEAD/PUT/PATCH/DELETE routing, malformed/duplicate payload, request conflict, stale revision, or persistence-unavailable safe response. Correction: add canonical HTTP cases with exact stable status/redirect conventions and assert zero domain/schema facts and no sensitive SQL/schema/credential/stack leakage for every rejection.

9. **HIGH — the Quality Graph capability entry does not have a verifier that verifies the protected owner.** `.quality-graph/verification-policy.json:547-567` assigns only `tests/Yii2/yii2_object_card_001_test.php`; its new assertions at lines 33-34 merely check an edit trigger and absence of `shaftBp`. They do not invoke `ObjectDetailsEditApplication`, validate its authorization/history/persistence invariants, or verify the registered owner/storage/schema files. A future deletion or bypass of the owner could leave this verifier green. Correction: register one or more public-seam tests that materially exercise the capability owner (at minimum the completed application/MariaDB test, plus HTTP where policy supports multiple verifiers), and add a focused governance assertion if needed to lock exact protected owner patterns and consumer edges. Re-run the planner after changing policy/tests.

10. **MEDIUM — static substring assertions are weak and can yield false positives.** `tests/Yii2/yii2_object_details_editing_browser_001_test.php:6-9`, `tests/Runtime/object_details_editing_recovery_001_test.php:6-7`, and parts of `tests/Yii2/yii2_object_details_editing_001_test.php:5-7` accept unrelated comments, dead templates, duplicated CSS, or values elsewhere on the page. Correction: prefer DOM roles/names/relationships, rendered field counts/groups, HTTP outcomes, database facts, migration introspection and recovery outputs; where source checks remain necessary, parse the relevant structure rather than search the entire file.

11. **MEDIUM — acceptance traceability was compressed in a way that hides missing scenarios.** `openspec/changes/edit-object-details-with-history/verification-input.json` maps ranges such as `A9-A12-effective-consumers` to a single 17-line projection test and `A8-A13-yii-history-and-http` to one short happy-path test. The planner reports no missing tests because file-level mapping exists, not because each observable acceptance is asserted. Correction: map each A1-A15 acceptance separately (or enumerate named subcases under a grouped mapping) to the exact tests/assertion groups that exercise it; do not mark Gate 2 tasks complete until those cases and reviewed RED evidence exist.

## RED evidence validity

- `php tests/InstallationProcess/object_details_editing_001_test.php`: **intended absence, insufficient Gate 2 evidence** — fails at line 8 because the registry class is absent; body unproven.
- `php tests/InstallationProcess/object_details_editing_mariadb_001_test.php`: **intended absence, insufficient Gate 2 evidence** — fixture reaches MariaDB, then fails at line 10 because the factory class is absent; owner behavior unproven.
- `php tests/InstallationProcess/object_details_effective_consumers_001_test.php`: **intended absence, insufficient Gate 2 evidence** — fixture reaches MariaDB, then fails at line 8 because the reader class is absent; consumer behavior unproven.
- `php tests/Runtime/object_details_editing_recovery_001_test.php`: **intended absence, insufficient Gate 2 evidence** — fails at line 5 because the migration class is absent; no migration or restore is exercised.
- `php tests/Yii2/yii2_object_details_editing_001_test.php`: **valid first UI RED only, insufficient matrix evidence** — canonical Yii starts and card returns 200, then line 5 fails on the missing edit trigger; POST/history/security assertions are unproven.
- `php tests/Yii2/yii2_object_details_editing_browser_001_test.php`: **intended source-shape RED, not valid browser evidence** — fails at line 5 on the missing token and never executes a browser.

The failures are deterministic at the captured source and are not environment/setup failures. That fact does not cure the masked bodies or absent scenarios.

## Required changes

1. Complete executable A1-A15 coverage at the public seams described above, including distinct rejection/no-write, replay, real concurrency, immutable history, import, all named consumers, security, browser, migration and recovery cases.
2. Replace wrapper-only/single-first-assertion evidence with staged complete-body RED evidence for every independent behavior cluster.
3. Replace the static browser wrapper with an actual canonical Yii browser flow and register/execute it.
4. Strengthen the Quality Graph verifier assignment so the protected state-changing owner itself is exercised, then regenerate the verification plan.
5. Restore one-to-one traceability from every acceptance item to concrete assertion groups, capture the corrected exact-source RED records, and return the complete candidate for independent Gate 3 rereview.
