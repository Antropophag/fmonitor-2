# Pilot demo bootstrap current-contract author evidence — 2026-09-07

Scope is limited to the protected bootstrap executable specification and its
planning artifacts. Production `bin/` and `app/` are outside this author slice.

The verifier now requires the literal 49-table canonical v19 catalogue plus the
three legacy-shaped import tables in the same generation prefix. It proves the
configured local actor through active `fm2_pilot_users`, exact user-role rows and
the seven exact FKR permissions. It also proves `status` rejects an intact ready
marker when one canonical table is missing.

The asset oracle compares `/pilot/assets/pilot.css` with the approved
`rapid-pilot/pilot.css` served by the demo launcher. An incomplete selection with
valid launcher CSRF and literal-null Origin must return exact HTTP 422 and the
`control_engineer_required` user message; 404/500/503 cannot satisfy it.

The native card's `/assignment-order/prepare` entry is retained and the demo
coordinator must redirect it to the current `/assignment-order/selection` form.
The bootstrap-owned mutation is current composition selection, with exact
installer/control-engineer facts and restart persistence. The old prepare,
combined HTML artifacts, manual registration number/status and registered
opening journey are removed as superseded representation. The mandatory
`pilot_e2e_flow_001_test.php` child remains in the unconditional contract list
and owns the full current original/correction, atomic application+opening,
checklist and completion proof.

Retained assertions include configured-actor spoof resistance, trusted CSRF and
Origin policy, permission rows, read-only rejection snapshots, occupied reset,
owner fingerprint and nonce markers, prefix collision, interrupted generation,
forged owner/foreign database preservation, foreign filesystem preservation and
bounded cleanup. Shared process/legacy prefix is required for new generations;
the two database marker tables remain independent ownership anchors. Historical
dual-prefix generations are not rewritten.

No database or browser test was run because the coordinator owns the shared DB
slot. Author checks:

- `php -l tests/InstallationProcess/pilot_demo_bootstrap_001_test.php` — PASS.
- `git diff --check` — PASS.
- stale business assertions `registrationNumber`, `12-Р`, `fm2l_`, `exact v4`,
  `v4 process`, `В работе` — absent. The prepare route occurs only as the current
  card entry and its exact redirect to selection.

Exact test SHA-256 before independent review:

`2d5c4e705b2d390d6155119ab2ee69b700342863911f61c5253e7ed553529cf7`

This is author evidence, not an independent test-review verdict and not GREEN.

## Coordinator continuation — 2026-09-08

After the author slice, actual private-body execution exposed and reconciled three
remaining predecessor expectations: the legacy suffix regex also matched
`fm2_pilot_users`, visible card dates use `dd.mm.yyyy`, and browser fields send
`installerTabIds[]` while `http_build_query` had generated numbered indices.
The test now compares the exact three-table remainder after canonical49, retains
literal displayed dates, and encodes the actual browser field names. The native
application parser was not relaxed.

Test SHA256 `49379179ddbd6a3c9d07734ca1c1f73455cf3a6a5964e36231f9d80e3e2e79de`
passed the entire private body, including current selection and lifecycle checks.
Log `runtime/bootstrap-body-017.log`, SHA256
`026b91a851303ff21e44b51a915c6d8c56bb28c9e3c144d7a50ab7d4bcab73b6`.
This private diagnostic excludes only the already recorded child loop; there is
no committed skip mechanism. Original whole caller attempt001 hit the first
migration child60s deadline; standalone measurement is underway. Whole-caller
GREEN and independent final approval remain separate requirements.


Original whole caller002 passed with all mandatory children and unchanged deadlines
on the recreated disposable `fmonitor2-test` tmpfs environment. Exit0,144.03s;
`runtime/bootstrap-whole-002.log` SHA256
`9f7ed5164ea7d95e7280d7f4e1f781acf03fdb8e98758213b9e088d16517feaf`.
The standalone migration child on the accumulated old test instance had passed
in90.67s; the old instance held38schemas/2332tables. No owner DB or volume was
recreated. This environmental correction did not add skips or weaken timeouts.
