# Handoff: №15 / Bitrix order document links — operational completion

- **Mode / authorization / artifact authors:** owner reprioritized delivery from merged PR #146 to a working customer-facing local stand and explicitly selected the legacy webhook. Root authored scope/spec/tests and performed stand operations; separate `gpt-5.6-sol/low` executors implemented runtime/UI deltas; separate reviewers approved the incremental delivery and final UI deltas. No inherited autonomous merge/deploy/settings authorization.
- **Role:** next root session completes one bounded presentation correction, verifies exact source, then decides publication with the owner.
- **Outcome and scope:** real Bitrix Disk root `1809812` is configured; legacy links were seeded, incremental/batched refresh works at real scale, object card and construction-control surfaces expose exact-`zavnumber` documentation. Excludes unrelated current goal #157, generalized integrations, object identity and rapid-pilot logic.
- **Worktree / branch / base / candidate:** `/Users/antropophag/code/fmonitor-2-main-runtime`; branch `codex/finish-bitrix-order-document-links`; base `b1542f92009b8dc4216a36962ff38a51e0b6c388` (`origin/main` at start); candidate HEAD `86d85d095a0a025f443ec82fd6ef1e86c6d00d94`; worktree clean at handoff. Commits: `0c3cd7a7`, `f965f35a`, `aeeeca02`, `489d0741`, `bd84085e`, `2f69ad54`, `86d85d09`.
- **Read:** `AGENTS.md`; `docs/operations/current-delivery-goal.md` (note owner reprioritization in this assignment); `specs/BITRIX-ORDER-DOCUMENT-LINKS-001.md`; `openspec/changes/bitrix-order-document-links/{design.md,tasks.md,specs/integration/bitrix-order-document-links/spec.md}`; `docs/development-process.md`.
- **Candidate completeness:** delivery supports 25,000 direct folders and a separately bounded 50,000-row expanded projection; lists Bitrix pages, reuses exact unchanged folder ID/name URLs, fetches new/renamed links with official batch calls of at most 50, accepts exact portal HTTPS or exact `bitrix24public.com`, and transactionally replaces only a complete validated projection. Queue uses a noninteractive black pinned `shlz-ui/folder-file-open` indicator; checklist shows the заводской номер in the upper-right hero above progress and links that number directly when exactly one effective URL exists. Multiple distinct links fail soft. The separate documentation button/layout is absent. Opening-date visible caption is removed while its accessible label remains.
- **Correction context:** PR #146 was merged at `25aee5524f790292d350175ba278bc47e282ed4c`, but real stand diagnosis found an unconfigured Disk root, insufficient scope on the workforce webhook, a 10,000-folder ceiling against 19,299 real folders, per-folder refresh cost, `bitrix24public.com` URL rejection, and PHP 8.5 `curl_close()` stderr causing worker failures. These were corrected. Independent reviews returned findings for folder/projection limit conflation, byte-exact trimming, queue state ambiguity, nested queue interaction, stale asset contracts and vacuous ambiguity evidence; all recorded findings were corrected and final bounded verdicts were `APPROVED`.
- **Evidence:** local focused delivery/mapping/application/read/schema/scheduler/console tests were GREEN where run; `tests/Verification/change_verification_001_test.py` 18/18 GREEN; `tests/Verification/architecture_guard_001_test.py` 59/59 GREEN; PHP/JS syntax and `git diff --check` GREEN; Impeccable detector returned `[]`. Exact Yii fixture runs in this worktree are not fully GREEN evidence because the current fixture hits a pre-existing HTTP 503 before some new assertions; live HTTP/browser evidence below is the customer-facing witness. Full local `make test`/`make verify` was not run, per owner ban. No exact-source CI has run for this follow-up branch; PR/CI remain `UNKNOWN`.
- **Live stand:** `http://127.0.0.1:8093` is healthy and runs the candidate image with preserved volumes. Old legacy webhook has working Disk scope; secret values remain only in ignored `.env`/runtime secret storage. Initial seed published 17,609 valid legacy rows and skipped one invalid URL; live incremental sync subsequently published 19,477 projection rows from 19,299 direct folders. 43 of 334 current objects exact-match documentation. Real checklist witness: `http://127.0.0.1:8093/pilot/construction-control/objects/1226/checklist` returns HTTP 200 and links order `52353.26` to the real Bitrix URL. Latest screenshot: `.local/screenshots/construction-control-order-link-mobile.png`; browser probe reported address/link top delta `0`, brand-blue link, no underline, font size 18px, no old document button, no visible opening-date label and no console/network errors.
- **Next action:** implement exactly one presentation delta in `app/YiiRuntime/Assets/pilot.css`: add underline on hover for `.fm2-check-order-link` while retaining default no underline, brand blue, focus-visible outline, plain unlinked number black, size and top alignment. Do **not** update the stand before first capturing the new focused expectation/check. Then update the pinned `pilot.css` SHA-256 in `tests/Support/yii2_production_web_cutover_contract.php`, run bounded syntax/architecture/browser checks, rebuild `make up`, capture desktop/mobile evidence, obtain independent bounded review, and commit.
- **Completion criterion for next action:** default linked number is brand blue with no underline; pointer hover visibly underlines it; keyboard focus remains obvious; unlinked number stays black; address/link top delta remains `0` (or browser rounding equivalent); live stand is healthy with no browser errors.
- **Open decisions / blockers:** whether to push this branch/open a follow-up PR and run exact-source GitHub CI remains an owner decision. General `make up-with-data` legacy import previously returned `LEGACY_IMPORT_FAILED`; existing 334 stand objects were preserved and document projection was seeded/synced separately. Current queued historical jobs may retain old failure codes from pre-fix attempts. The old webhook is a legacy credential stored in legacy source; rotation to a dedicated Disk-scoped webhook is recommended after customer validation but was explicitly deferred by owner choice to use the old webhook.
- **Preserved WIP / stand:** original `/Users/antropophag/code/fmonitor-2` WIP and its unresolved merge remain untouched. All №15 follow-up code lives in the clean separate worktree above. The stand uses Compose project `fm2-local-timofey` and preserved database/state/secrets volumes. Do not reset or delete volumes.
