# Ночной handoff — 2026-09-09

## Последнее поручение владельца

Владелец уходит спать. В новой сессии выполнять #33 и затем следующие доступные
задачи бэклога автономно, активно используя сабагентов для экономии контекста.
Технические решения принимать самостоятельно. Продуктовые решения собирать к
пробуждению. Промежуточный PR, план, review или ожидание CI не заканчивают работу.
Исполняемый промпт: `continue-overnight-2026-09-09.txt` в этом каталоге.
Это новое решение о режиме работы дополняет архитектурный приоритет.

## Проверенная исходная точка

- PR61 **MERGED** 2026-09-08 21:54:27 UTC; merge commit
  `eb88ed2f7edb4ad609662e9268e1d8defcb18ceb`.
- Проверенный PR head: `4fcebfdf45e3f986681f33c8474b6070436a7e28`.
- Actions34282154511 SUCCESS, все8jobs; literal VERIFY_OK.
  https://github.com/Antropophag/fmonitor-2/actions/runs/34282154511
- #32 выполнен, первый срез #24 выполнен; #23 закрыт. Не повторять их реализацию.
- ОТиЗ: атомарная публикация, stable operation identity/replay, проверка manifest
  перед отдельным принятием, миграции20/21, runtime ledgers без DDL.
- Политика доступа имеет одного владельца IdentityAccess; PilotHttp alias сохранён.
- `make architecture-check` включает HTTP qualification oracle перед checker7rules.
  В AGENTS есть path-trigger для HTTP и полный разбор всех CI failures.
- Headless browser: непустой native fixture, DML-only DB account, потеря ответа,
  replay без дубля, принятие и XLSX. Рабочий стенд не переключался.

## Checkout и локальные изменения

Исходный `/Users/antropophag/code/fmonitor-2` — старый checkout с посторонними WIP.
Не делать reset/clean/stash общего дерева и не принимать его за свежий main.
Сохраняются `.env.example`, неотслеживаемые аудиты/анализы, explicit-bitrix change,
локальные browser fixtures и checkpoint. Часть этих файлов уже есть в main;
сопоставлять содержимое, не объявлять их новым незавершённым кодом.

Предыдущий интеграционный checkout:
`/Users/antropophag/code/fmonitor-2-otiz-current-20260909`,
ветка `codex/otiz-atomic-integration`. В нём после CI локально изменены только:
- `docs/operations/otiz-publication-delivery-2026-09-09.md` — итоговый GREEN;
- `openspec/changes/atomic-otiz-snapshot-publication/tasks.md` — фактические13/13.
Эти отметки не пушились, чтобы не менять проверенный head ради его же отчёта.
Их можно перенести в следующий срез как историю после проверки, а не повторять тесты.

Стартовать в новом worktree от свежего `origin/main`, содержащего merge61.
Перед созданием новой ветки проверить текущие открытые PR и уже начатые #33 changes,
чтобы не дублировать параллельную работу. Не менять общую ветку из сабагента.

## Документы и ограничения

Читать в новом checkout: AGENTS, PRODUCT, CONTEXT, текущую delivery goal,
pilot spec/data model, development-process, ADR0002 и два готовых архитектурных аудита.
Затем изучать только файлы выбранного среза. Источники точного исходного поведения
уже исследованы в PR61; не запускать очередной общий аудит вместо реализации.

В #33 нельзя просто назначить `public/router.php` единственным входом: текущий
`rapid-pilot/router.php` также обслуживает ОТиЗ, календарь, очередь, schedule,
completion и HTML decorators. Сохранить эти маршруты/авторизацию/UI при замене runtime.

Отдельный runtime, DB и storage для ночных проверок; основной пользовательский
стенд и volumes сохраняются. Primary evidence/секреты вне git, `../fmonitor` read-only,
shlz-ui только через публичные exports. Реальные внешние отправки, новые импорты и
изменение полномочий не выводятся автоматически из технической автономности.

A02 (выплаты между срезами) и A03 (округление) не закрыты PR61. Продуктовые правила
выплат не изобретать ночью; независимая диагностика/тесты допустимы, неизвестные
решения — в список владельцу. Подтверждённые риски данных/доступа могут изменить
приоритет, обычные косметические замечания — нет.

## Техническая очередь

Сверить актуальные bodies/status в GitHub перед выполнением:
1. #33 — production HTTP + прямой DB + явные миграции, отдельный работающий контур.
2. #27 — актуальный маршрут запуска/эксплуатации именно поставленного runtime.
3. #34 — технический jobs/outbox/worker/scheduler, без новых продуктовых рассылок.
4. #36 — restore/update drill после нужных runtime/jobs dependencies. SLA/RPO/RTO
   и сроки хранения не объявлять утверждёнными: измерить фактические показатели,
   выполнить независимую техническую часть, решения владельцу.
5. Далее доступные технические срезы #24, #17 и #26 с сохранением поведения.
   Порядок уточнять по зависимостям, существующим specs и измерениям; уже поставленную
   пагинацию/оптимизацию повторно не делать. Полный смысл агрегатов сохраняется.
6. После них — следующие READY задачи актуального бэклога. Продуктовая неоднозначность
   блокирует зависимый срез, а не всю ночь.

Промпт задаёт цикл выполнения и журнал состояния. Новая сессия должна переносить
этот handoff/ночное поручение в собственный актуальный delivery checkpoint, чтобы
compaction не возвращал старый порядок или уже завершённый PR61.
