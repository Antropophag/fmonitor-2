# DATA-INTEGRITY existing-test compatibility candidate v1

Status: UNAPPLIED / independent Gate3 required. No reviewed root test/helper has
been changed. Production remains unchanged.

Patch: `original-data-integrity-existing-tests-amendment-v1-2026-09-06.patch`.
SHA256 `2ab9c6752263fa42ad06cd25ec6f80bab530cbe48c49a38e3f243adc463ec6fc`.
Private candidate/evidence root:
`/Users/antropophag/.local/state/fmonitor2-verification/original-data-compatibility-oyy2ioh7`.
Final `patch-evidence.json` SHA256:
`4e7e38277f78b1a1eccf1a4e55fd4bcc2558454a90ef59c0ef9e25e469cce599`.
The base manifest pins full HEAD and every copied app PHP file. The candidate
changes only11 test/helper files, including one new test-only compatibility file.
Before/after hashes, exact commands and logs are recorded. All11 files lint.

## Exact changes

- Initial/Dynamic/Shape helpers expose the existing assignment-lineage read for
  the new initial absence precheck; unsupported extra initial calls fail instead
  of inventing absence. Dynamic/LogIsolation/Lifecycle found rows include complete
  case/order/current evidence metadata. The initial reference helper becomes
  FOUND/false, the exact valid no-reference tuple.
- A test-only marker extends the real complete-lineage/fresh-reader interfaces
  when present. It defines no production symbol. The fresh fake uses a distinct
  explicit one-shot read/close port, not the ordinary repository lookup.
- Lifecycle and safe-log-isolation transcripts include initial step11 lineage
  read before allocation/finalize. Unknown lifecycle modes move their fixed fault
  scripts to the explicit fresh reader and require open/read/close traces.
- Fake CAS rivals are plans until the commit conflict actually publishes the
  competing fact. Initial precheck genuinely sees absence. The isolation test
  independently pins empty-before and exact rival-after evidence, preserving
  that the current command creates no accepted fact.
- Gate5Domain uses literal canonical one-installer hashes for orders81/82,
  complete lineage, exact assignment queries and distinct fixed root/revision
  IDs for its two-order control. Its deliberate pre-write attempt Throwable now
  supplies and asserts an explicit fresh reliable miss; the expected failure
  result is retained with an actual absence proof.
- The real missing-prefix technical-failure test gets a fully valid canonical
  DTO so future scalar validation cannot mask the intended actual SQL failure.
- The old same-PDF worker race retains winner/stale requests/audits, no loser
  domain/fingerprint/event and no extra blob. Its obsolete release-log expectation
  becomes empty because step11 rejects B before any lease. The separate approved
  DATA-WORKER-001 distinct-PDF post-finalize race retains real lease-release failure
  and allowed private unreferenced-orphan proof. Protected E2E is untouched.

Pure valid synthetic `private-content-0001` remains unchanged, as explicitly
allowed by the lifecycle contract. No failure becomes a skip or allowed failure.

## Candidate execution against unchanged production

Nine affected old scripts ran in the copied candidate, with source unchanged:

- PASS: upload, upload-validation, dynamic-ports, command-shape, real Gate5-MariaDB.
- Intended RED: safe-log isolation (missing new precheck trace), lifecycle (missing
  precheck/fresh protocol), Gate5Domain (fresh miss counters0 instead of1), worker
  transport (obsolete old implementation still releases/logs before stale return).

The four RED scripts exit255 through acceptance assertions. The five positive
scripts exit0. No candidate assertion was obtained from production output; values
come from approved DATA-INTEGRITYv06/parent and earlier independent audits.
The new public/scalar/real-adapter/worker RED suites retain separate Gate3 records.
