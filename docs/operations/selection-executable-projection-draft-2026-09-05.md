# Selection executable draft and projection acceptance

Date: 2026-09-05. Author: `/root`.
Base: `412efcb75bd88b454b2ba1ebbf9800937e96856b`.

Created `ASSIGNMENT-ORDER-COMPOSITION-SELECT-001` v0.1 and reconciled the four
existing selection OpenSpec artifacts with the proven projection gap. The
no-application invariant explicitly covers public assignments/availability and
inspection attribution, not only interval row preservation. Existing Gate 3
task retains its number; new projection RED task 2.4 precedes it by dependency.

The draft preserves direct-upload/no-render intent and records unresolved
exact authority/DTO/schema/correction/date compatibility points before Gate 1.
No production/tests/schema/grants changed. Earlier planning approval does not
approve this new executable draft or amended artifact bytes.

Draft SHA-256:
`a16c041d11529491205056de2dea84b06d8e0fb3d0a1ef78b3e3c7c3d40646d0`.
`git diff --check`: exit 0. Selection OpenSpec strict validation passed before
commit. All artifact bytes are preserved in the containing commit.
