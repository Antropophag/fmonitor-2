# Текущая цель — №123 slice A, container-managed Composer visibility

Поручение владельца 2026-09-16: продолжить [№123](https://github.com/Antropophag/fmonitor-2/issues/123) только отдельным bounded slice A от `main` `11f0fcb446d8fbd0cbfa2c88caf2e0888b9819cf` и довести candidate до PR-ready. Scope: сделать container-managed locked Composer dependencies видимыми существующим repository-relative Yii entrypoints внутри canonical `run-in-profile`, сохранив exact candidate source и не создавая host `vendor/`.

Не входят `INTENDED_RED` classification (slice B), worktree identity guard (slice C), новый `agent-bootstrap`/environment manager, host Composer setup/vendor, cross-worktree dependency copy/symlink, shared writable vendor, lock/version changes, product behavior и Docker performance/caching. `rapid-pilot` не читать. Full local suite запрещён.

Acceptance A–J и T08 measurement определены в [CONTAINER-COMPOSER-VISIBILITY-123-A](../../specs/CONTAINER-COMPOSER-VISIBILITY-123-A.md). OpenSpec lifecycle: [expose-container-composer-dependencies](../../openspec/changes/expose-container-composer-dependencies/). Root пишет scope/spec/tests; отдельный gpt-5.6-sol/low executor реализует; независимые gpt-5.6-sol/low reviewers решают planner-required Gates 3/5. Владелец явно подтвердил автономное выполнение работ по этой спецификации до merge-ready PR 2026-09-16; merge не разрешён.

Локально только bounded focused checks; exact-source GitHub CI выполняется один раз через выбранный existing consumer. Setup failures before Yii behavior должны измениться с доказанных 3/3 до 0/N; cold-ish/warm duration, host vendor existence, dependency/source origins измеряются без обещаний CI/token improvement. Предыдущий указатель №162 сохранён в Git history; dirty/conflicted worktree №157 не изменяется. Merge/deploy/settings не выполнять.
