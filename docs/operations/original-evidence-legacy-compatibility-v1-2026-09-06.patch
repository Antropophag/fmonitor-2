--- a/tests/InstallationProcess/assignment_order_original_evidence_reader_001_test.php
+++ b/tests/InstallationProcess/assignment_order_original_evidence_reader_001_test.php
@@ -12,7 +12,7 @@
 use FMonitor2\Tests\Support\AssignmentOrderOriginalDatabaseSetupV1 as Contract;
 
 // Specification: ASSIGNMENT-ORDER-ORIGINAL-UPLOAD-001 v14, independent evidence construction.
-mysqli_report(MYSQLI_REPORT_ERROR|MYSQLI_REPORT_STRICT);$host=getenv('FMONITOR_TEST_DB_HOST')?:'127.0.0.1';$port=(int)(getenv('FMONITOR_TEST_DB_PORT')?:23306);$user=getenv('FMONITOR_TEST_DB_ADMIN_USER')?:'root';$password=getenv('FMONITOR_TEST_DB_ADMIN_PASSWORD')?:'fmonitor2_test_root_local';$token=bin2hex(random_bytes(6));$database='t_aoou_reader_'.$token;$prefix='reader_';$admin=new mysqli($host,$user,$password,'',$port);$control=sys_get_temp_dir().'/aoou-reader-'.$token;$root=$control.'/private';$passwordFile=$control.'/password';$safeLog=$control.'/safe.log';$db=null;$reader=null;
+mysqli_report(MYSQLI_REPORT_ERROR|MYSQLI_REPORT_STRICT);$host=getenv('FMONITOR_TEST_DB_HOST')?:'127.0.0.1';$port=(int)(getenv('FMONITOR_TEST_DB_PORT')?:23306);$user=getenv('FMONITOR_TEST_DB_ADMIN_USER')?:'root';$password=getenv('FMONITOR_TEST_DB_ADMIN_PASSWORD')?:'fmonitor2_test_root_local';$token=bin2hex(random_bytes(6));$database='t_aoou_reader_'.$token;$prefix='reader_';$admin=new mysqli($host,$user,$password,'',$port);$control=(string)realpath(sys_get_temp_dir()).'/aoou-reader-'.$token;$root=$control.'/private';$passwordFile=$control.'/password';$safeLog=$control.'/safe.log';$db=null;$reader=null;
 try{$admin->query("CREATE DATABASE `{$database}` CHARSET utf8mb4 COLLATE utf8mb4_unicode_ci");mkdir($control,0700,true);mkdir($root,0700,true);file_put_contents($passwordFile,$password."\n");chmod($passwordFile,0600);file_put_contents($safeLog,'');chmod($safeLog,0600);$db=new mysqli($host,$user,$password,$database,$port);$db->set_charset('utf8mb4');FMonitor2\InstallationProcess\ProductionProcessSchemaMigration::apply($db,$prefix);FMonitor2\InstallationProcess\IdentityAccessSchemaMigration::apply($db,$prefix);FMonitor2\InstallationProcess\ProcessUserCapabilitiesSchemaMigration::apply($db,$prefix);FMonitor2\InstallationProcess\ProcessCommandCapabilitiesSchemaMigration::apply($db,$prefix);AssignmentOrderOriginalSchemaMigration::apply($db,$prefix);AssignmentOrderOriginalVerificationDatabaseFixture::seedExampleA($db,$prefix);
     if(!class_exists(AssignmentOrderOriginalEvidenceReaderFactory::class))throw new TestFailure('INTENDED_RED: approved AssignmentOrderOriginalEvidenceReaderFactory production seam is absent.');
     $reader=AssignmentOrderOriginalEvidenceReaderFactory::create(new AssignmentOrderOriginalEvidenceReaderConfig($host,$port,$database,$user,$passwordFile,$prefix,$root,$safeLog));
--- a/tests/InstallationProcess/assignment_order_original_maintenance_001_test.php
+++ b/tests/InstallationProcess/assignment_order_original_maintenance_001_test.php
@@ -2,7 +2,7 @@
 declare(strict_types=1);
 require dirname(__DIR__).'/bootstrap.php';require dirname(__DIR__).'/Support/AssignmentOrderOriginalDatabaseSetupV1.php';
 use FMonitor2\AssignmentOrderOriginal\{AssignmentOrderOriginalAuthorizationStatus,AssignmentOrderOriginalClock,AssignmentOrderOriginalEvidenceReaderConfig,AssignmentOrderOriginalEvidenceReaderFactory,AssignmentOrderOriginalFaultInjector,AssignmentOrderOriginalFaultPoint,AssignmentOrderOriginalMaintenanceAuthorization,AssignmentOrderOriginalMaintenanceReason,AssignmentOrderOriginalMaintenanceStatus,AssignmentOrderOriginalPrivateOrphanFixtureCommand,AssignmentOrderOriginalPrivateOrphanFixtureFactory,AssignmentOrderOriginalPrivateOrphanFixtureKind,AssignmentOrderOriginalProductionConfig,AssignmentOrderOriginalRealMaintenanceVerificationFactory,AssignmentOrderOriginalSchemaMigration,ReconcileAssignmentOrderOriginalPrivateOrphansCommand};
-mysqli_report(MYSQLI_REPORT_ERROR|MYSQLI_REPORT_STRICT);$host=getenv('FMONITOR_TEST_DB_HOST')?:'127.0.0.1';$port=(int)(getenv('FMONITOR_TEST_DB_PORT')?:23306);$user=getenv('FMONITOR_TEST_DB_ADMIN_USER')?:'root';$password=getenv('FMONITOR_TEST_DB_ADMIN_PASSWORD')?:'fmonitor2_test_root_local';$token=bin2hex(random_bytes(16));$database='t_aoou_maint_'.substr($token,0,12);$prefix='maint_';$control=sys_get_temp_dir().'/aoou-maint-'.$token;$taskRoot=$control.'/task';$productionRoot=$control.'/production';$passwordFile=$control.'/password';$safeLog=$control.'/safe.log';$admin=new mysqli($host,$user,$password,'',$port);$db=$reader=null;
+mysqli_report(MYSQLI_REPORT_ERROR|MYSQLI_REPORT_STRICT);$host=getenv('FMONITOR_TEST_DB_HOST')?:'127.0.0.1';$port=(int)(getenv('FMONITOR_TEST_DB_PORT')?:23306);$user=getenv('FMONITOR_TEST_DB_ADMIN_USER')?:'root';$password=getenv('FMONITOR_TEST_DB_ADMIN_PASSWORD')?:'fmonitor2_test_root_local';$token=bin2hex(random_bytes(16));$database='t_aoou_maint_'.substr($token,0,12);$prefix='maint_';$control=(string)realpath(sys_get_temp_dir()).'/aoou-maint-'.$token;$taskRoot=$control.'/task';$productionRoot=$control.'/production';$passwordFile=$control.'/password';$safeLog=$control.'/safe.log';$admin=new mysqli($host,$user,$password,'',$port);$db=$reader=null;
 try{$admin->query("CREATE DATABASE `{$database}` CHARSET utf8mb4 COLLATE utf8mb4_unicode_ci");mkdir($control,0700,true);mkdir($taskRoot,0700);mkdir($productionRoot,0700);file_put_contents($taskRoot.'/.aoou-verifier-owner',"aoou-private-orphan-fixture-v1\n{$token}\n");chmod($taskRoot.'/.aoou-verifier-owner',0600);file_put_contents($passwordFile,$password."\n");chmod($passwordFile,0600);file_put_contents($safeLog,'');chmod($safeLog,0600);$db=new mysqli($host,$user,$password,$database,$port);$db->set_charset('utf8mb4');FMonitor2\InstallationProcess\ProductionProcessSchemaMigration::apply($db,$prefix);FMonitor2\InstallationProcess\ProcessUserCapabilitiesSchemaMigration::apply($db,$prefix);FMonitor2\InstallationProcess\ProcessCommandCapabilitiesSchemaMigration::apply($db,$prefix);AssignmentOrderOriginalSchemaMigration::apply($db,$prefix);
     $safeLogStat=lstat($safeLog);assertSameValue([true,false,true,(function_exists('posix_geteuid')?posix_geteuid():getmyuid()),0600,realpath($safeLog)],[file_exists($safeLog),is_link($safeLog),is_file($safeLog),$safeLogStat['uid']??null,($safeLogStat['mode']??0)&0777,realpath($safeLog)],'Maintenance production config uses an existing canonical owned regular non-symlink exact-0600 safe log.');
     if(!class_exists(AssignmentOrderOriginalPrivateOrphanFixtureFactory::class))throw new TestFailure('INTENDED_RED: approved AssignmentOrderOriginalPrivateOrphanFixtureFactory seam is absent.');assertSameValue(['privateStorageRoot','tablePrefix','safeLogFile'],array_map(static fn(ReflectionParameter$p):string=>$p->getName(),(new ReflectionMethod(AssignmentOrderOriginalProductionConfig::class,'__construct'))->getParameters()),'INTENDED_RED: maintenance fixture requires the mandatory third production safeLogFile contract.');
@@ -32,4 +32,4 @@
     }
     $rejectedIds=['00000000-0000-4000-8000-000000000230','00000000-0000-4000-8000-000000000231','00000000-0000-4000-8000-000000000232','00000000-0000-4000-8000-000000000233','00000000-0000-4000-8000-000000000240','00000000-0000-4000-8000-000000000241','00000000-0000-4000-8000-000000000242'];$requests=array_values(array_filter(json_decode($reader->maintenanceRequestsCanonicalJson(),true,512,JSON_THROW_ON_ERROR)['items'],static fn(array$item):bool=>in_array($item['requestId'],$rejectedIds,true)));$audits=array_values(array_filter(json_decode($reader->maintenanceAuditsCanonicalJson(),true,512,JSON_THROW_ON_ERROR)['items'],static fn(array$item):bool=>in_array($item['requestId'],$rejectedIds,true)));assertSameValue(['00000000-0000-4000-8000-000000000233','00000000-0000-4000-8000-000000000240'],array_column($requests,'requestId'),'Only future cutoff and actual denial persist; invalid shape and config do not.');assertSameValue(array_column($requests,'requestId'),array_column($audits,'requestId'),'Exact audit/request membership.');assertSameValue(2,count($requests),'Every persistable invalid/denied maintenance result has one terminal request.');assertSameValue(2,count($audits),'Every persistable invalid/denied maintenance result has one terminal audit.');foreach([...$requests,...$audits]as$item){$suffix=(int)substr($item['requestId'],-3);$expectedReason=$suffix<240?'invalid_command':'authorization_denied';$expectedPrincipal=$suffix===240?'other-principal':'test-maintenance-01';assertSameValue([$expectedReason,false,0,0,0,0,'rejected',$expectedPrincipal],[$item['reasonCode'],$item['retryable'],$item['scanned'],$item['deleted'],$item['retained'],$item['failed'],$item['status'],$item['systemPrincipalId']],'Rejected maintenance evidence has exact reason, zero accounting and trusted principal.');}assertSameValue([$beforeRejected[2],$beforeRejected[3]],[$reader->privateBlobsCanonicalJson(),$reader->safeLogsCanonicalJson()],'Invalid/denied maintenance changes no blob or safe log.');
     fwrite(STDOUT,"ASSIGNMENT_ORDER_ORIGINAL_MAINTENANCE_OK\n");
-}finally{if($reader!==null)try{$reader->close();}catch(Throwable){}if($db instanceof mysqli)$db->close();try{$admin->query("DROP DATABASE IF EXISTS `{$database}`");}catch(Throwable){}$admin->close();foreach([$safeLog,$passwordFile,$taskRoot.'/.aoou-verifier-owner']as$file)if(is_file($file))unlink($file);foreach([$taskRoot,$productionRoot,$control]as$dir)if(is_dir($dir))rmdir($dir);}
+}finally{if($reader!==null)try{$reader->close();}catch(Throwable){}if($db instanceof mysqli)$db->close();try{$admin->query("DROP DATABASE IF EXISTS `{$database}`");}catch(Throwable){}$admin->close();$marker=$taskRoot.'/.aoou-verifier-owner';if(is_file($marker)&&!is_link($marker)&&file_get_contents($marker)==="aoou-private-orphan-fixture-v1\n{$token}\n"){$state=$taskRoot.'/.aoou-state.json';if(is_file($state)&&!is_link($state)){$empty=json_decode(file_get_contents($state),true,512,JSON_THROW_ON_ERROR);assertSameValue(['stages'=>[],'finalized'=>[]],$empty,'Owned maintenance cleanup requires empty metadata.');unlink($state);}foreach(array_merge([$taskRoot.'/.aoou-state.lock'],glob($taskRoot.'/.lock-*')?:[])as$lockFile){if(is_file($lockFile)&&!is_link($lockFile)&&(basename($lockFile)==='.aoou-state.lock'||preg_match('/^\.lock-[a-f0-9]{64}$/D',basename($lockFile))))unlink($lockFile);}}foreach([$safeLog,$passwordFile,$taskRoot.'/.aoou-verifier-owner']as$file)if(is_file($file))unlink($file);foreach([$taskRoot,$productionRoot,$control]as$dir)if(is_dir($dir))rmdir($dir);}
--- a/tests/InstallationProcess/assignment_order_original_production_boundary_001_test.php
+++ b/tests/InstallationProcess/assignment_order_original_production_boundary_001_test.php
@@ -8,7 +8,7 @@
 require_once dirname(__DIR__).'/Support/AssignmentOrderOriginalPdfCorpus.php';
 use FMonitor2\AssignmentOrderOriginal\{AssignmentOrderOriginalApplication,AssignmentOrderOriginalByteStream,AssignmentOrderOriginalClock,AssignmentOrderOriginalEvidenceReaderConfig,AssignmentOrderOriginalEvidenceReaderFactory,AssignmentOrderOriginalFaultInjector,AssignmentOrderOriginalFaultPoint,AssignmentOrderOriginalFileSafeLog,AssignmentOrderOriginalFileStorage,AssignmentOrderOriginalMemoryStream,AssignmentOrderOriginalMode,AssignmentOrderOriginalPrivateOrphanFixtureCommand,AssignmentOrderOriginalPrivateOrphanFixtureConflict,AssignmentOrderOriginalPrivateOrphanFixtureFactory,AssignmentOrderOriginalPrivateOrphanFixtureKind,AssignmentOrderOriginalPrivateOrphanFixtureUnavailable,AssignmentOrderOriginalProductionConfig,AssignmentOrderOriginalProductionConfigurationUnavailable,AssignmentOrderOriginalReason,AssignmentOrderOriginalSchemaMigration,AssignmentOrderOriginalService,AssignmentOrderOriginalStatus,AssignmentOrderOriginalStreamRead,AssignmentOrderOriginalStreamReadStatus,AssignmentOrderOriginalUpload,AssignmentOrderOriginalVerificationDatabaseFixture,ProductionAssignmentOrderOriginalFactory,SubmitAssignmentOrderOriginalCommand};
 use FMonitor2\Tests\Support\AssignmentOrderOriginalPdfCorpus;
-$token=bin2hex(random_bytes(16));$control=sys_get_temp_dir().'/aoou-production-boundary-'.$token;$remove=static function(string$path)use(&$remove):void{if(is_file($path)||is_link($path)){unlink($path);return;}if(!is_dir($path))return;foreach(scandir($path)?:[]as$entry)if($entry!=='.'&&$entry!=='..')$remove($path.'/'.$entry);rmdir($path);};$failures=[];$expectThrow=static function(string$label,callable$operation,string$expected='')use(&$failures):void{try{$operation();$failures[]=$label.': accepted';}catch(Throwable$e){if($expected!==''&&!$e instanceof $expected)$failures[]=$label.': '.get_class($e);}};$expectProductionConfigFailure=static function(string$label,callable$operation)use(&$failures):void{try{$operation();$failures[]=$label.': accepted';}catch(Throwable$e){$expected=AssignmentOrderOriginalProductionConfigurationUnavailable::class;if(get_class($e)!==$expected||$e->getMessage()!=='AssignmentOrderOriginalProductionConfigurationUnavailable'||$e->getCode()!==0||$e->getPrevious()!==null)$failures[]=$label.': non-canonical exception shape '.get_class($e).' message='.json_encode($e->getMessage()).' code='.$e->getCode().' previous='.($e->getPrevious()===null?'null':get_class($e->getPrevious()));}};
+$token=bin2hex(random_bytes(16));$control=(string)realpath(sys_get_temp_dir()).'/aoou-production-boundary-'.$token;$remove=static function(string$path)use(&$remove):void{if(is_file($path)||is_link($path)){unlink($path);return;}if(!is_dir($path))return;foreach(scandir($path)?:[]as$entry)if($entry!=='.'&&$entry!=='..')$remove($path.'/'.$entry);rmdir($path);};$failures=[];$expectThrow=static function(string$label,callable$operation,string$expected='')use(&$failures):void{try{$operation();$failures[]=$label.': accepted';}catch(Throwable$e){if($expected!==''&&!$e instanceof $expected)$failures[]=$label.': '.get_class($e);}};$expectProductionConfigFailure=static function(string$label,callable$operation)use(&$failures):void{try{$operation();$failures[]=$label.': accepted';}catch(Throwable$e){$expected=AssignmentOrderOriginalProductionConfigurationUnavailable::class;if(get_class($e)!==$expected||$e->getMessage()!=='AssignmentOrderOriginalProductionConfigurationUnavailable'||$e->getCode()!==0||$e->getPrevious()!==null)$failures[]=$label.': non-canonical exception shape '.get_class($e).' message='.json_encode($e->getMessage()).' code='.$e->getCode().' previous='.($e->getPrevious()===null?'null':get_class($e->getPrevious()));}};
 $runBounded=static function(array$command):array{$pipes=[];$process=@proc_open($command,[0=>['pipe','r'],1=>['pipe','w'],2=>['pipe','w']],$pipes);if(!is_resource($process))throw new TestFailure('SETUP_FAILURE: controlled wrong-owner probe launch failed');$stdout='';$stderr='';$exit=null;$timedOut=false;$cleanupFailure=null;try{fclose($pipes[0]);stream_set_blocking($pipes[1],false);stream_set_blocking($pipes[2],false);$deadline=hrtime(true)+10_000_000_000;while(true){foreach([1,2]as$fd){$chunk=fread($pipes[$fd],4096);if($chunk===false)throw new TestFailure('SETUP_FAILURE: controlled wrong-owner probe pipe read failed');if($fd===1)$stdout.=$chunk;else$stderr.=$chunk;}if(strlen($stdout)>4096||strlen($stderr)>4096)throw new TestFailure('SETUP_FAILURE: controlled wrong-owner probe output exceeded bound');$status=proc_get_status($process);if(!($status['running']??false)){$exit=(int)$status['exitcode'];break;}if(hrtime(true)>=$deadline){$timedOut=true;break;}usleep(10000);}
 foreach([1,2]as$fd){$chunk=stream_get_contents($pipes[$fd]);if(is_string($chunk)){if($fd===1)$stdout.=$chunk;else$stderr.=$chunk;if(strlen($stdout)>4096||strlen($stderr)>4096)throw new TestFailure('SETUP_FAILURE: controlled wrong-owner probe output exceeded bound');}}}finally{
 if(strlen($stdout)>4096||strlen($stderr)>4096)$cleanupFailure='SETUP_FAILURE: controlled wrong-owner probe output exceeded bound';$status=proc_get_status($process);if($status['running']??false){proc_terminate($process,15);$stopDeadline=hrtime(true)+1_000_000_000;do{usleep(10000);$status=proc_get_status($process);}while(($status['running']??false)&&hrtime(true)<$stopDeadline);if($status['running']??false){proc_terminate($process,9);$killDeadline=hrtime(true)+1_000_000_000;do{usleep(10000);$status=proc_get_status($process);}while(($status['running']??false)&&hrtime(true)<$killDeadline);}}if($status['running']??false)$cleanupFailure='SETUP_FAILURE: controlled wrong-owner probe could not be terminated';elseif($exit===null&&isset($status['exitcode'])&&(int)$status['exitcode']>=0)$exit=(int)$status['exitcode'];foreach([0,1,2]as$fd)if(isset($pipes[$fd])&&is_resource($pipes[$fd]))fclose($pipes[$fd]);$closed=proc_close($process);if($exit===null&&$closed>=0)$exit=$closed;}if($cleanupFailure!==null)throw new TestFailure($cleanupFailure);if($timedOut)throw new TestFailure('SETUP_FAILURE: controlled wrong-owner probe timed out');if($exit===null||$exit<0)throw new TestFailure('SETUP_FAILURE: controlled wrong-owner probe reap status unavailable');return[$exit,$stdout,$stderr];};
