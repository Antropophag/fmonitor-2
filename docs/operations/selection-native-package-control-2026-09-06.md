# Native selection — контроль первого пакета

Base HEAD14f3f0394fa33a6e2ee76872da588dd501412c7f подтверждён, tree clean.
Persistent goal отсутствовал: exact objective восстановлен из handoff1733Z без
budget. Mandatory product documents и handoff прочитаны; старые approvals reused.

Критический путь: eight native ports → original reader/locked check + optional
PDF/date/audit + HTTP/application/opening → exact-SHA VERIFY_OK/CI/fresh deployment/
restart/golden path. Preview оставлен работающим, login HTTP200; users503 остаётся
отдельным blocker, не исправлялся этим пакетом. Remote mutation отсутствует.

Исходный cap:25мин/35k tokens включая construction review, RED/Gate3 и commit.
Первое измерение goal после восстановления:96877 tokens /253 seconds. Измерение
включает восстановление контекста и чтение; это observation, не финальная стоимость.
Cap превышен: пакет пересмотрен ДО RED/production. Текущий ограниченный результат —
только native construction Gate1 review и фиксация его findings/disposition.
RED/Gate3 переходит в следующий измеряемый пакет, не отменён и не считается
выполненным. Используется компактный независимый reviewer fork none/sol low.

Candidate source2b25ad6: specs/ASSIGNMENT-ORDER-SELECTION-NATIVE-001.md.
Режимы new_order и replace_pending обязательны. Frontier13 не резервируется.
Core/schema/reader approvals не переоткрываются. Parent OpenSpec3/14 unchanged.
