# Аудит проекта и бэклога — 2026-09-08

Снимок: исходный HEAD d5f8f2d, ветка работы `codex/issue-25-verification-inventory`. Проверены открытые GitHub issues #11–36, #38–40, PR #37/#41, текущие delivery rules, runner, Makefile, CI setup, HTTP composition, ОТиЗ и frontend. Это аудит разработки и зависимостей; не нагрузочный, security или production readiness аудит. Стенд не перепроверялся и не изменялся. Незакоммиченный прежний архитектурный аудит сохранён без изменений.

## Выбор

В работу взята [#25](https://github.com/Antropophag/fmonitor-2/issues/25). Пользователь поручил выбрать по облегчению дальнейшей разработки; прежний owner priority тоже назначает #25 после Linux CI fix. Условие выполнено: [PR37](https://github.com/Antropophag/fmonitor-2/pull/37) имеет четыре SUCCESS на 9f53001; [PR41](https://github.com/Antropophag/fmonitor-2/pull/41) merged 2026-09-08, baseline SUCCESS ([run](https://github.com/Antropophag/fmonitor-2/actions/runs/34206732488)). Успех старого head не переносится автоматически на новую ветку.

## Что тормозит разработку

| Факт | Свидетельство | Последствие |
|---|---|---|
| Проверки смешивают уровни и повторяют работу | Makefile: 9 последовательных этапов; run.sh: 117 unit и 120 DB записей | Дорогая обратная связь даже для маленькой правки |
| Состав вычисляется по тексту PHP | run.sh: FMONITOR_TEST_DB/new mysqli | Зависимость через fixture легко классифицировать неверно; отсутствие rg уже ломало Linux |
| E2E вызывается трижды | DB list, отдельный e2e, pilot_demo_bootstrap_001_test.php:142 | Точный top-level дубль и вложенная композиция требуют разных решений |
| CI setup не является локальным setup | tools/delivery/ci-setup.sh требует GITHUB_ACTIONS=true и sudo apt | #23 частично выполнена, но один безопасный локальный путь ещё отсутствует |
| Native HTTP зависит от пилота | ProductionPilotHttpEntrypointFactory::localAuth загружает rapid-pilot/LocalAuth.php | Полное разделение ещё не достигнуто; переносить по маршрутам |
| ОТиЗ вызывает migration ledgers при создании | rapid-pilot/Otiz.php:44–45 | Перед #24 нужна карта runtime owners и переноса DDL |
| Исходник чек-листа — одна строка | app/PilotHttp/checklist.js | #35 нужна читаемость исходника прежде решения о framework |

Числа времени из #25 относятся к прежнему неуспешному same-head eacc4d4: baseline 12м30.8с + graph 17м22.6с = 29м53.4с runner time, а не ожидание пользователя и не новый benchmark. Успешный PR41 job: 08:50:55–09:08:05 UTC (17м10с, включая setup). Эти разные измерения нельзя выдавать за до/после на одном окружении.

## Порядок технического бэклога

1. **#25 — состав, длительности, затем устранение дублей и согласованная матрица.** Выигрыш распространяется на каждую следующую задачу; реализация первого среза не трогает runtime.
2. **#23 + #27 — воспроизводимый локальный setup и единая инструкция.** Закреплённые CI-зависимости и чистый GitHub уже существуют; нужно закрывать конкретный остаток, не создавать второй setup. README синхронизировать с фактическими командами.
3. **#32 → #24 — карта владельцев, затем один перенос поведения.** Убирать runtime-зависимости и дубли последовательно; полный rewrite не оправдан.
4. **#35 — читаемые frontend-исходники**, затем измеряемое решение о точечном framework. Выбор framework сам по себе не устраняет смешение состояния и DOM.
5. **#33 + #36 — production запуск и восстановление.** Отдельный контур и сохранение данных; нужны перед production, не условие старта каждого backlog fix.
6. **#34 — jobs/outbox** перед #11/#13 и расширением расписаний интеграции; не строить платформу без первого потребителя.

## Остальной бэклог и зависимости

- **#38, #39, #40:** текущие ручные замечания остаются отдельным продуктовым приоритетом. #40 меняет ответственного за opening относительно PRODUCT/старого пилотного контракта; перед реализацией синхронизировать актуальное решение и серверные полномочия. Не считать простой правкой фильтра. #38 связан с #19, #39 с рабочей очередью #40.
- **#12 → #16/#13:** сначала доказанная синхронизация трёх дат, затем индикаторы и уведомление. #15 — отдельный read-only контракт ссылок Битрикс. Реальные интеграционные записи не разрешены самим аудитом.
- **#11/#13/#14:** уведомления и планирование имеют незакрытые продуктовые решения (получатели, повторная отправка, перенос/отмена). #14 не вводит отвергнутую обязательную недельную инспекцию.
- **#17/#26:** существенное пересечение пагинации ОТиЗ и масштабирования. #17 — конкретный срез, #26 — измерения общего масштаба; не дублировать реализацию.
- **#19/#21/#28/#29/#30:** расширение рабочих разделов и объяснимость; опираться на существующих владельцев фактов. #21 — UI по shlz-ui, не основание строить новую предметную модель.
- **#20/#22:** миграция закреплений/подготовка пользователей и демонстрационные сценарии — разные контуры. Owner сам приглашает сотрудников; перенос пользователей не возобновлять по старому заголовку #20.
- **#31:** сбор обратной связи полезен для пилота, но не заменяет исправление уже известных #38–40.
- **#18:** эпик, не самостоятельная реализация; #23–27/#32–36 должны отражать достигнутые части и зависимости.

## Первый срез #25

OpenSpec `explicit-verification-inventory`, контракт VERIFICATION-INVENTORY-001. Явный TSV вместо эвристики, проверка полноты до запуска, read-only list, timing каждого top-level теста. Существующие наборы сохранены. Это основа для следующих изменений, а не обещание уже достигнутого ускорения.

Далее: измерить новые timing на одном окружении; удалить двойной top-level E2E с отдельным review; исследовать bootstrap child без потери contract; выделить быстрые/unit/integration/E2E/governance категории. Представить матрицу local focused / PR affected / full authoritative CI и числовые бюджеты владельцу, как требует #25. До её принятия прежние gates/CI jobs сохраняются. #25 остаётся в работе, не закрыта.

## Выполнено в первом срезе

- TSV содержит прежние 117 unit, 120 DB, 18 characterization и 1 E2E строку; добавлен один characterization контракт inventory.
- `python3 tests/Verification/verification_inventory_001_test.py`: 15 PASS, 5.117с на текущей машине (контракт runner, не benchmark полного verify).
- `python3 tests/Verification/verification_native_suites_001_test.py`: 9 PASS.
- `php tests/Verification/harness_full_aggregation_001_test.php` и `harness_fresh_test_lifecycle_001_test.php`: PASS.
- `bash -n tools/verification/run.sh`, `git diff --check`, `openspec validate explicit-verification-inventory`: PASS.
- Gate3 APPROVED после исправления пяти замечаний; история первого CHANGES_REQUESTED сохранена в reviews/tests/VERIFICATION-INVENTORY-001.md.
- Новый full make verify и deployment не выполнялись; прежний GitHub SUCCESS не является evidence новой ветки. Независимый Gate5 и architecture result фиксируются отдельно.

Дополнительная совместимость на b7ff4e1: старое ожидание missing-rg в QUALITY-GRAPH-CI-SETUP-001 заменено проверкой точных unit/db списков без rg и явного отказа без каталога. Image/tooling/provenance проверки сохранены; отдельный Gate3 APPROVED и реальный Docker test GREEN (`QUALITY-GRAPH-CI-SETUP-001 PASSED`). Финальный inventory rerun: 15 PASS, 4.402с. `make architecture-check`: ARCHITECTURE CHECK PASSED (7 rules). GitHub issue25 назначена Antropophag и остаётся OPEN; код локальный, push/merge/deployment не выполнялись.

Итог первого среза: независимый Gate5 APPROVED на exact b7ff4e1 против d5f8f2d; запись reviews/code/VERIFICATION-INVENTORY-001.md. Reviewer повторил inventory15, native9 и реальный CI setup — PASS. Все 4 задачи OpenSpec выполнены. Это завершение среза inventory/timing, не завершение #25, не full VERIFY_OK и не разрешение новой CI-матрицы.
