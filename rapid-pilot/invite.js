(()=>{const button=document.querySelector('[data-copy-invite]');const input=document.querySelector('[data-invite-url]');const status=document.querySelector('[data-copy-status]');if(!(button instanceof HTMLButtonElement)||!(input instanceof HTMLInputElement)||!(status instanceof HTMLElement))return;button.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(input.value);button.textContent='Ссылка скопирована';status.textContent='Теперь отправьте ссылку пользователю удобным способом.';}catch{input.focus();input.select();status.textContent='Не удалось скопировать автоматически. Ссылка выделена — скопируйте её вручную.';}});})();
