(()=>{"use strict";const dialog=document.querySelector("[data-inspection-dialog]"),form=dialog?.querySelector("[data-inspection-form]"),label=dialog?.querySelector("[data-inspection-object]");if(!dialog||!form||!label)return;document.addEventListener("click",event=>{const button=event.target.closest("[data-inspection-schedule]");if(button){form.action=`/pilot/objects/${button.dataset.objectId}/inspection-schedule`;label.textContent=button.dataset.objectLabel;dialog.showModal();dialog.querySelector('input[type="date"]')?.focus();return}if(event.target.closest("[data-inspection-close]"))dialog.close()});dialog.addEventListener("click",event=>{if(event.target===dialog)dialog.close()});})();
