# Proposal

Stage 2 of #258 adds real daily immutable installer-utilization observations, the three-way current summary, a grouped historical chart, saved drill-down and first/last comparison. It builds on merged PR #264 and deliberately leaves the six-week forecast for stage 3.

No production backfill, merge, deploy, synthetic working history, writer changes or financial behavior are in scope. The additional owner-requested production investigation is read-only and feeds regression coverage; local installer-card spacing may be corrected in the same PR without redesigning the shell.
