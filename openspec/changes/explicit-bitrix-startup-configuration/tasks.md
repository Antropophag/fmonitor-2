## 1. Baseline and executable contract

- [x] 1.1 Создать отдельную ветку от актуального main, перенести только этот change и сверить Make/Compose/WorkerConfiguration; записать SHA и фактический список удаляемых вызовов.
- [ ] 1.2 Оформить стабильную спецификацию BITRIX-STARTUP-CONFIG-001 с разделом «Простыми словами» и seam `make up`; проверить трассировку всех критериев #42 и Gate1 по действующему процессу.
- [ ] 1.3 Добавить минимальные проверки штатного `make up` из отдельного checkout без legacy и PHP на хосте: корректный fixture, ошибочные/отсутствующие настройки, сохранённый старый secret и штатный запуск с Bitrix; сохранить characterization и целевой RED, получить независимый test review до реализации.

## 2. Configuration and removal

- [ ] 2.1 Добавить контейнерную подготовку `.env` через существующий механизм конфигурации, безопасную диагностику и атомарную запись; подтвердить GREEN сценариями корректного/неверного ввода и повторной подготовки.
- [ ] 2.2 Подключить подготовку к Make/Compose, удалить legacy exporter и его Make-зависимость; проверить запуск через реальный Make в изолированном Compose project без соседнего репозитория и PHP на хосте.
- [ ] 2.3 Добавить безопасные `.env.example`, build exclusions и инструкцию нового запуска в актуальную документацию main; проверить синтетическим marker-токеном отсутствие секрета в build context, образе и выводе, а поиском — отсутствие активных инструкций извлечения из легаси.

## 3. Integration and delivery

- [ ] 3.1 Выполнить focused smoke штатного `make up` с локальным HTTPS fixture и отказа без Bitrix-настроек; подтвердить штатный worker, безопасные ошибки, сохранение данных и отсутствие реальных Bitrix-вызовов; записать команды и результаты.
- [ ] 3.2 Выполнить затронутые configuration/worker regressions, architecture check, lint и diff check; получить независимый code review на конкретном кандидате, устранить замечания с повтором необходимых проверок.
- [ ] 3.3 Подготовить небольшой PR по #42 с перечислением реально удалённых зависимостей и доказательств; для merge-ready подтвердить полный CI на точном head по принятой матрице, без обязательного дублирующего локального full. Проверить, что в PR не попали посторонние локальные файлы.

## 4. Done and deferred scope

Done: удалён legacy exporter и вызывающий путь, запуск воспроизводится из отдельного клона, негативные проверки безопасны, штатный up запускает Bitrix, review и требуемый CI подтверждены. Пока это только план: RED/GREEN и approvals отсутствуют, задачи реализации не отмечены выполненными.

Полная production-интеграция, scheduler/runtime, DDL ОТиЗ и эксплуатационные матрицы остаются отдельными задачами. Если при manual-pilot доставке какой-либо формальный gate отложен по действующему исключению, записать его как deferred и не объявлять весь срез Done или production-ready. Исторические approvals и установленный стенд сохраняются.
