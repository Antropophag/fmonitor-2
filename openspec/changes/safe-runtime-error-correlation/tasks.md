## 1. Gate 1 и verification planning

- [x] 1.1 Утвердить normative `SAFE-RUNTIME-ERROR-CORRELATION-001` и проверить трассировку actor/public seam, ID ownership, exact HTTP compatibility, category evidence, privacy allowlist, single-record ownership, logger failure и adjacent no-change flows.
- [x] 1.2 Подготовить `verification-input.json`, выполнить harness `prepare` от exact `origin/main` и подтвердить, что все mandatory obligations разрешены и planner зафиксировал authoritative lane/required reviews.

## 2. Gate 2 — container-focused RED

- [x] 2.1 Добавить зарегистрированный container-focused real-HTTP/factual-log test для bootstrap configuration/database failures, selected controller storage/unexpected failures, exact response/log ID, one-record count, hostile client ID, privacy canaries, logger failure и unchanged success/4xx/domain facts; доказать INTENDED_RED из подготовленного профиля.
- [x] 2.2 Выполнить planner-required независимый Gate 3 review spec/test/RED; исправить полный список findings не более чем за два correction rounds и переподготовить package при изменении bound inputs.

## 3. Gate 4 — минимальная реализация

- [ ] 3.1 Реализовать runtime-owned generator/classifier/allowlisted record writer с fail-safe sink и trusted build-version fallback; focused test должен подтвердить closed fields и отсутствие message inspection.
- [ ] 3.2 Подключить bootstrap и `SafeErrorHandler`, сохранив sanitize/security/Retry-After и error-ID header после очистки; focused wire/log cases должны стать GREEN без Yii/DB bootstrap dependency.
- [ ] 3.3 Подключить только `ExecutionController`, `OriginalController`, `ChecklistController`, сохранив существующие envelopes и unknown-result semantics; focused tests должны доказать одну запись, отсутствие повторов и unchanged facts.

## 4. Verification, reviews и PR

- [ ] 4.1 Выполнить planner-selected focused commands, применимые runtime/Yii regressions и `make architecture-check`; полный локальный `make test`/`make verify` не запускать.
- [ ] 4.2 Подготовить exact-source Gate 5 package и получить независимый final review `APPROVED`; весь список findings исправлять одним пакетом, delta review — не более двух correction rounds.
- [ ] 4.3 Создать отдельный PR, запустить только штатный PR-triggered exact-source CI через `ci-launch`, при сбое собрать полный failed-job/`REGRESSION_FAILURE` inventory и использовать не более двух correction candidates после первого.

## 5. Done definition

- [ ] 5.1 Подтвердить PR-ready head: выбранные 5xx возвращают server ID и имеют ровно одну безопасную запись; privacy/logger-failure/compatibility проверки GREEN, required reviews APPROVED, exact-source CI GREEN; delivery record содержит авторство, команды, безопасный пример и поиск по ID, без merge/deploy/settings.
