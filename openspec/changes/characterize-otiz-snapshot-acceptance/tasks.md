## 1. Gate 1 — executable specification

- [x] 1.1 Создать `specs/CHARACTERIZE-OTIZ-SNAPSHOT-ACCEPTANCE-001.md` с literal draft/children, настоящим LocalAuth/router path, exact admission → 12-table DDL/conditional `unique_reversal` repair → CSRF → row-lock ordering, evidence-backed exact status/`Location`/header presence-or-absence/plain UTF-8 body bytes с trailing LF для каждого case, fact/rejection/replay/concurrency/clock/session-isolation/cleanup contract и stable transcript; проверить независимость expected values от будущего verifier и строгую маркировку `PILOT_ONLY`.
- [ ] 1.2 Получить и durably записать явное owner approval только characterization scope; проверить, что target authority, financial meaning/evidence, separation of duties, operation id/revision, replay и payment consequences остаются исключены до RED.

## 2. Gates 2–3 — minimal reviewed RED

- [ ] 2.1 Назначить свежего RED author для smallest real LocalAuth/session/router accepted POST с literal draft, exact HTTP response, three-field snapshot delta, one event и immutable child fingerprints; выполнить до harness implementation и сохранить intended RED отдельно от setup failure.
- [ ] 2.2 Назначить другого свежего независимого test reviewer, записать review в `reviews/tests/`, исправить findings без implementation-derived expectations и проверить явный `APPROVED` до GREEN.
- [ ] 2.3 Реализовать только reviewed accepted exchange в private-prefix/unique-port-cookie-session harness; проверить неизменённый minimal test дважды GREEN с разными ownership tokens без production behavior/schema changes.

## 3. Reviewed expansion и Gate 4 GREEN

- [ ] 3.1 Расширить test новым intended RED на unauthenticated/unauthorized/bad-CSRF ordering, exact 12-table fresh creation и отдельный existing-table conditional `unique_reversal` repair, missing/already accepted/open blocker/open warning/resolved blocker, sequential replay, separate Moscow bounds, winner-neutral real two-worker concurrency, `session.gc_probability=0`, exact owned-session cleanup, ambient DB/session decoys, bounded process/SQL cleanup, repeatable normalized transcript и отдельные intentional `SETUP_FAILURE`/`REGRESSION`; проверить sensitivity к response-only fake, extra child/event mutation, single-worker pseudo-race и cleanup omission.
- [ ] 3.2 Назначить ещё одного свежего независимого test reviewer expanded RED; записать versioned review, устранить все findings и проверить явный `APPROVED` до expanded GREEN.
- [ ] 3.3 Минимально расширить только verifier/harness до GREEN всей reviewed behavior/isolation/failure matrix; проверить её дважды с distinct ports/cookies/session ids, cleanup после intentional setup/assertion failures и отсутствие pilot domain logic changes.
- [ ] 3.4 Зарегистрировать verifier ровно один раз в canonical characterization stage и выполнить focused oracle, related OTIZ regressions, `git diff --check` и `make architecture-check`; проверить отсутствие роста runtime-DDL/SQL/mutation baselines.

## 4. Gate 5 и Done

- [ ] 4.1 Обновить PB-09 inventory/backlog/status: oracle covered, а target `PREMIUM-SNAPSHOT-ACCEPTANCE-001`, canonical premium persistence, exact capability, four-eyes/evidence и payment closure остаются `NEEDS_GRILL`; проверить отсутствие promoted financial semantics.
- [ ] 4.2 Назначить отдельного свежего независимого code reviewer, не участвовавшего в RED/GREEN, и записать review в `reviews/code/`; проверить явный `APPROVED` для real-seam sensitivity, immutable content/history, concurrency, session/process cleanup и non-promotion pilot debt.
- [ ] 4.3 Выполнить canonical characterization и `make verify`, классифицировать только подтверждённые unrelated baseline failures и отметить Done лишь при durable reviewed GREEN evidence; затем sync/archive change по OpenSpec lifecycle.
