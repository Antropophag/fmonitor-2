## 1. Gate 1 и план проверки

- [x] 1.1 Root создаёт canonical executable spec `OTIZ-SAVED-INSTALLER-ALLOCATION-EXPLANATION-001` по delta-spec с полной HTTP/browser acceptance matrix и явной трассировкой к bounded этапу #29; проверка: примеры независимо фиксируют значения нескольких работников, первую/последующую публикации, старый snapshot, missing basis/allocation, XSS, denied actor, no-write и регрессию #263.
- [x] 1.2 Root создаёт `verification-input.json`, запускает `python3 tools/delivery/harness.py prepare` для нового change и читает все обязательства/commands подготовленного test-author package; проверка: planner lane и `required_reviews` записаны, unresolved obligations отсутствуют до Gate 2.

## 2. Gate 2 — RED

- [x] 2.1 Root добавляет изолированный Yii HTTP fixture через действующий snapshot detail seam; проверка: тест доказывает snapshot-only identity/date/distributed amount/contribution/KTУ/share/basis/amount, historical stability, missing-data copy, escaping, authorization и отсутствие DML.
- [x] 2.2 Root добавляет browser fixture с несколькими работниками, длинным основанием и narrow viewport, сохраняя формы/`operationId` #263; проверка: intended RED вызван отсутствующей расшифровкой, а существующий form-recovery regression остаётся GREEN.
- [x] 2.3 Root сохраняет bounded RED evidence и complete candidate snapshot; если planner требует Gate 3, независимый `gpt-5.6-sol / low` reviewer утверждает spec/tests и acceptance mapping до реализации.

## 3. Gate 4 — минимальная реализация

- [x] 3.1 Отдельный `gpt-5.6-sol / low` executor по prepared package добавляет только необходимую snapshot projection/presentation композицию; проверка: ни один текущий workforce/composition источник и ни один writer не вызывается.
- [x] 3.2 Executor добавляет локальный server-rendered partial/details в существующий object drawer с отдельными форматами вклад/КТУ/доля/money и честными empty states; проверка: нет формулы allocation, raw JSON/payload, нового route или shared JS/CSS.
- [x] 3.3 Executor запускает только planner-selected focused HTTP/browser checks, regressions settlement/form-recovery/ledger и применимые architecture checks; проверка: GREEN evidence содержит точные команды, elapsed time и отсутствие повторного full local suite.

## 4. Gate 5 и публикация

- [x] 4.1 Root сверяет complete candidate со scope, фиксирует реальный user before/after и reconstructible exact source, затем independent `gpt-5.6-sol / low` final reviewer даёт полный findings list и verdict по planner-required package.
- [x] 4.2 После APPROVED root применяет corrections при необходимости, подтверждает exact reviewed bytes, создаёт осмысленный commit и rebase-ит на свежий `origin/main` без поглощения параллельных #265/#258; проверка: diff не затрагивает объявленные non-goals.
- [ ] 4.3 Root push-ит ветку, открывает PR с `Refs #29` без автоматического закрытия issue и запускает один exact-source CI через выбранный существующий consumer; проверка: CI GREEN именно для PR head, PR содержит before/after, авторов, reviews и честный список остатка широкой #29.

## 5. Done

- [ ] 5.1 Done только когда локальные focused checks GREEN, обязательные independent reviews APPROVED, exact-source CI GREEN и PR открыт; merge/deploy/стенд/реальные финансовые действия не выполняются, полная #29 остаётся открытой.
