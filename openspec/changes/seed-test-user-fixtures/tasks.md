## 1. Preconditions и Gate 1

- [ ] 1.1 Дождаться landing ordered frontier: canonical workforce runner v5 →
  identity/access → checklist-template → inspection-evidence →
  inspection-planning → classification-provenance → object-detail-snapshot →
  generation-metadata opaque receipt extension; fresh-проверкой зафиксировать
  exact versions/order/transcript. Installation-completion, premium,
  migrated-evidence, quarantine и legacy-active исключены, если literal Gate 1
  явно не вводит их consumer.
- [ ] 1.2 Подготовить executable `TEST-USER-FIXTURE-SEED-001` с literal fictional
  manifest/version/ID ranges/`.invalid` identities, exact role/capability and
  workforce/object scenario matrix, independent semantic hashes, secret/redaction,
  seed-once/restart/conflict/concurrency/reset examples; получить fresh technical
  review и explicit owner `APPROVED` до test edits.
- [ ] 1.3 Доказать отдельным no-personal-data review, что manifest не содержит
  production/legacy identifiers, deliverable emails, real documents, source
  payloads или secrets; сохранить review record до Gate 2.

## 2. Gate 2 RED и Gate 3 test review

- [ ] 2.1 Назначить свежего RED-автора и создать public `make up`/setup verifier
  для fresh seed, literal manifest/fingerprint, public login/directories/golden
  initial state, rejection inputs, partial/conflict/concurrency, no-source,
  no-secret и readiness ordering; сохранить qualifying RED отдельно от setup
  failure.
- [ ] 2.2 Добавить state-changing golden action → ordinary restart verifier и
  доказать RED на reseed/overwrite либо отсутствующей validation-only contract,
  не изменяя production expectations.
- [ ] 2.3 Добавить ownership-checked `make reset` → recreate verifier с foreign
  decoys и ambiguity rejection; destructive test targets должны быть uniquely
  owned, enumerated и полностью очищены.
- [ ] 2.4 Передать spec, tests и RED evidence другому свежему независимому test
  reviewer; записать `reviews/tests/TEST-USER-FIXTURE-SEED-001.md`, исправлять
  findings новыми авторами/reviewer-агентами и получить explicit `APPROVED` до
  production changes.

## 3. Gate 4 minimal GREEN

- [ ] 3.1 Добавить отдельный fixture initializer с immutable checked-in
  fictional manifest и parser/semantic fingerprint; проверить manifest/no-personal-
  data/redaction cases GREEN без repository credentials.
- [ ] 3.2 Реализовать seed-once orchestration после canonical prerequisites под
  generation lock с preflight-all, setup ports/public application seam и
  initializer-owned semantic receipt, передаваемым generation owner как opaque
  prerequisite envelope; проверить fresh/conflict/partial/concurrency REDs GREEN без
  прямой mutation process facts.
- [ ] 3.3 Перевести ordinary `make up`/restart на validation-only fixture receipt
  и required identities; проверить byte-equivalent preservation user-created
  process history/counters/artifacts и fail-closed drift без repair.
- [ ] 3.4 Подключить существующий explicit `make reset` только через exact
  Compose ownership/resource proof и recreate path; проверить approved reset/
  foreign-decoy matrix GREEN без production destructive seam.
- [ ] 3.5 Добавить architecture ratchets против runtime/HTTP seed, canonical-
  migration row seeding, production source access, secret/personal-data literals
  и новой fixture/domain logic в `rapid-pilot`; проверить targeted forbidden
  fixtures и `make architecture-check`.

## 4. Regression и release evidence

- [ ] 4.1 На fresh owned Compose volumes выполнить `make up`, public login для
  утверждённых fictional roles, directories/projections и один prepare →
  register → open journey; проверить exact observable results без production
  source/network dependency.
- [ ] 4.2 Выполнить ordinary restart после user mutations и доказать сохранение
  generation/fixture receipt/process history/counters/artifacts; затем explicit
  reset/recreate и доказать исходный semantic fingerprint в новой generation.
- [ ] 4.3 Выполнить focused identity/workforce/process/golden tests,
  `git diff --check`, strict OpenSpec validation, `make architecture-check`,
  `make fresh-test-verify` и `make verify`; классифицировать каждую baseline/new
  failure без ослабления assertions и оставить Compose empty после проверки.

## 5. Gate 5 и Done

- [ ] 5.1 Передать approved spec/tests, production diff и verification evidence
  свежему независимому code reviewer; записать
  `reviews/code/TEST-USER-FIXTURE-SEED-001.md` с explicit `APPROVED`, исправляя
  findings только через требуемый возврат к соответствующему gate.
- [ ] 5.2 Обновить TEST-USER runbook/operations status и отметить slice Done
  только когда fictional seed воспроизводим, restart state-preserving, reset
  ownership-checked, external/personal/secret inputs отсутствуют, все regression/
  architecture checks green и оба независимых reviews approved.
