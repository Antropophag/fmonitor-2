## 1. Gate 1–2: scope, planner и RED

- [x] 1.1 Зарегистрировать bounded correction как текущий delivery goal, создать `verification-input.json`, выполнить `harness.py prepare` и подтвердить planner-selected lane/obligations до изменения executable code
- [x] 1.2 Root инвентаризирует реальные Yii2 HTML routes с общим sidebar и добавляет focused HTTP regression, который сравнивает semantic menu membership/order одного identity на root и минимальных nested screens при full и restricted permissions
- [x] 1.3 Запустить зарегистрированный focused test на актуальном `origin/main` и записать детерминированный RED строго на смене menu membership, без setup failure
- [x] 1.4 Выполнить только planner-required pre-implementation review; FAST продолжает без Gate 3, а эскалация в STANDARD/CRITICAL требует независимый Gate 3 APPROVED

## 2. Gate 4: минимальная реализация

- [x] 2.1 Отдельный executor удаляет статический navigation fallback и направляет каждый общий sidebar через canonical permission-aware renderer; nullable/explicit current section меняет только current marker
- [x] 2.2 Executor устраняет найденные в bounded inventory standalone membership copies, только если они входят в выбранные shared-sidebar surfaces, и не меняет route authorization, RBAC или layout
- [x] 2.3 Выполнить planner-selected focused local checks и regression; подтвердить одинаковый membership/order на root/nested routes, прежние deny outcomes и отсутствие persisted side effects

## 3. Gate 5 и exact-source verification

- [x] 3.1 Подготовить reconstructible exact-source package и получить один независимый final review для FAST либо полный planner-required набор после эскалации; findings исправлять с повторной bounded проверкой
- [ ] 3.2 Создать meaningful commit и PR без merge/deploy, затем запустить один existing exact-source GitHub CI consumer; при сбое сначала собрать полный failed-job и `REGRESSION_FAILURE` inventory
- [ ] 3.3 Обновить delivery record фактическими авторами, review/return counts, focused evidence, exact commit/PR/CI и честным PR-ready статусом
