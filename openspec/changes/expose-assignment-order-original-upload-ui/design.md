## Context

Composition HTTP Gate5bfcf6c03 готов; original core selected binding approved.
Parent full read имеет dependency на actual engineer assignment owner. Этот
вертикальный пакет доставляет writer UI, не объявляя весь parent complete.

## Goals / Non-Goals

Real operator upload/correction без hidden PDF, correct documentDate/retry identity.
No general download/history authorization, effective composition/opening или migration.

## Decisions

Fresh coordinator маршрутизирует originals POST и originals/submit GET/HEAD в
тонкий adapter. Context/form query принадлежит AssignmentOrderOriginal; SQL только
native owner. Context uses registered immutable composition + validated original
lineage. Form grants exact local read + current action; command repeats native cap.

PDF raw body avoids PHP multipart duplicate erasure. Header canonical JSON/base64
closed shape; adapter bounds received body and hands original filename/media/stream
approved command. Same-origin JS submits File, explicit canonical metadata and key.
No HTTP filesystem persistence; private originals/terminals/audits owned existing core.

## Risks / Trade-offs

Upload needs JS; no misleading fallback. Full read/download and engineer assignments
remain parent/lifecycle obligations. Native config/credential files come from bootstrap,
not web commands. Runtime ownership mismatch fail closed.

## Migration Plan

Gate1→native HTTP RED→Gate3→minimal GREEN→UI/regression/architecture→Gate5/commit.
No current8092 image switch; isolated owned native browser fixture verifies behavior.
