## Why

Native selection/PDF уже в UI. Следующий пользовательский шаг — подписанный
оригинал, который пока доступен только application command.

## What Changes

ASSIGNMENT-ORDER-ORIGINAL-UPLOAD-HTTP-001: initial/correction через real HTTP и
операторскую форму с date prefill. Reuse approved createForSelections/native audit.
Binary PDF body + bounded canonical metadata предотвращают silent duplicate fields.
General history/download scope сохраняется в parent expose-assignment-order-original-http;
assigned engineer policy привязывается к следующему actual composition application owner.

## Capabilities

### New Capabilities
- `pilot/assignment-order-original-upload-ui`: selection→original/correction form.

### Modified Capabilities

## Impact

HTTP adapter/public query и same-origin JS; domain owner unchanged. No application,
opening, history compatibility, new template storage или runtime grants/DDL.
Owner решения закрыты; transport/config technical choices проходят independent Gate1.
