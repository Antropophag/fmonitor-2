## 1. Gate 1 и catalogue prerequisites

- [x] 1.1 Дождаться фактического landing workforce, identity/access,
  checklist-template и inspection-evidence migrations; зафиксировать exact
  predecessor catalogue и literal planning version в executable schema spec,
  затем проверить последовательность canonical runner.
- [x] 1.2 Согласовать executable `INSPECTION-PLANNING-SCHEMA-001` с exact двумя
  manifests, composed prefix ceiling 25/26, compatible partial recovery, populated
  preservation и runtime-no-DDL; записать явный owner `APPROVED` до test edits.
- [x] 1.3 Синхронизировать technical 25/26 full-catalogue prefix contract с
  predecessor draft specs после разрешённых review/approval действий и проверить
  отсутствие противоречащих 26–32-byte composed-runner support claims; сохранить
  family-local 28/29 только как direct-family arithmetic/evidence.

## 2. Gate 2 RED и Gate 3 test review

- [x] 2.1 Отдельным test-author создать MariaDB matrix с test-owned literals для
  clean, populated repeat, обеих exact partial families, conflicts, collation,
  JSON CHECK absent/extra/changed/duplicate-equivalent, composed prefix 25/26,
  family-local identifier inventory, decoy isolation и exact runner output;
  не заявлять lock/ledger/concurrent-runner semantics; доказать qualifying RED
  при исправном environment.
- [x] 2.2 Добавить DDL-denied runtime characterization для scheduling POST,
  Calendar, object queue, construction-control и bootstrap consumers;
  missing/incompatible family должна дать exact Gate 1 outcomes (включая
  queue 12-hex reference regex) без partial response, schema/data mutation или
  ready publication.
- [x] 2.3 Отдельным reviewer записать `APPROVED` в
  `reviews/tests/INSPECTION-PLANNING-SCHEMA-001.md`; при замечаниях повторять RED
  и review свежими agents до approval.

## 3. Gate 4 minimal implementation

- [x] 3.1 Реализовать strict planning-family migration с validated explicit
  database-default collation, family-wide preflight и deterministic result;
  focused migration matrix должна стать GREEN.
- [x] 3.2 Зарегистрировать exact sequential version и сузить composed runner
  prefix ceiling до 25 с 26-byte rejection до DB connection/access; clean/repeat runner и invalid-prefix-before-DB tests
  должны проходить.
- [x] 3.3 Удалить owned-family DDL/`ensureSchema` calls из scheduling, Calendar,
  queue/control и bootstrap runtime paths, заменив exact fail-closed schema
  precondition; DDL-denied characterization должна стать GREEN.
- [x] 3.4 Обновить architecture ratchet только на реально удалённый debt и
  проверить `make architecture-check` без нового SQL/runtime-DDL ownership.

## 4. Verification и Gate 5

- [x] 4.1 Выполнить focused schema/behavior tests, calendar и scheduling
  characterization, `make lint`, `make architecture-check`, built-image/fresh
  lifecycle и `make verify`; не ослаблять unrelated regressions.
- [x] 4.2 Отдельным code reviewer проверить Standards и Spec axes и записать
  `APPROVED` в `reviews/code/INSPECTION-PLANNING-SCHEMA-001.md`.
- [x] 4.3 Отметить tasks выполненными только после фактических gates: canonical
  runner единолично владеет exact family, runtime DDL отсутствует, populated data
  сохраняются и product scheduling semantics остаются в GRILL-001.
