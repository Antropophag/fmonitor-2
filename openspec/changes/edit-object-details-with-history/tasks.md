## 1. Gate 1 — contract and field frontier

- [x] 1.1 Root creates `specs/OBJECT-DETAILS-EDITING-001.md` from the approved issue/OpenSpec contract, including the public command seam, exact eleven-field matrix, date exclusion, Kшах derivation, authorization, replay/concurrency, history and consumer outcomes; verify every OpenSpec scenario maps to a stable-spec acceptance item.
- [x] 1.2 Root inventories current native models, technical payload fields and real readers/filters/documents/ERP/Bitrix/OTIZ consumers; record every existing field as editable, integration-owned, process-owned or derived-system with a named owner and verify no ordinary existing field is silently omitted.
- [x] 1.3 Create `verification-input.json`, run harness `prepare`, read the mandatory Quality Graph obligations/commands and resolve every applicable/unapplicable coverage item before Gate 2.

## 2. Gate 2 — root-authored RED tests

- [x] 2.1 Add table-driven public-owner/DB RED tests for fill/correct/no-op of every allowed field, typed normalization, leading zeros, factory-number byte limit/zero candidate, unknown/mixed fields and Kшах rejection/derivation; capture intended RED evidence.
- [x] 2.2 Add RED authorization/replay/concurrency/transaction tests for active status, capability, object scope, request conflict, stale revision, parallel winner and injected rollback; verify every rejection creates zero override/event facts.
- [x] 2.3 Add RED import-repeatability/migration/recovery tests proving mirror/snapshot/hash preservation, additive schema lifecycle, backup inventory and restored replay/history continuity.
- [x] 2.4 Add RED consumer tests for card, queue/search/filter/pagination, Bitrix links, ERP candidates/matching and OTIZ effective technical evidence without rewriting old outputs.
- [x] 2.5 Add canonical Yii HTTP/browser RED tests for CSRF/method/auth/outcomes, two-group `shlz-ui` modal, unchanged card except edit icon, validation input retention, cancel/no-op, reload, two immutable edits, escaped field diff and history beyond eight events.

## 3. Gate 3 — independent test review

- [x] 3.1 Capture a reconstructible exact RED source and dispatch an independent `gpt-5.6-sol / low` reviewer with the prepared package; record one complete Gate 3 findings list and `APPROVED` verdict before production implementation.
- [x] 3.2 Root corrects any Gate 3 findings, reruns only affected focused RED commands and obtains independent re-review when required; record return count/reasons and keep expectations unchanged after approval.

## 4. Gate 4 — executor implementation

- [x] 4.1 Separate `gpt-5.6-sol / low` executor implements additive canonical persistence, DML grants, fixed field registry, effective-value projection and atomic owner until approved owner/DB tests pass.
- [x] 4.2 Executor connects importer repeatability and current consumers (card/queue/search/filter, Bitrix/ERP matching, OTIZ operands/Kшах) and verifies focused consumer tests without changing historical outputs.
- [x] 4.3 Executor registers `objects.details.edit` for exact business roles, adds canonical Yii route/controller and builds the approved compact icon plus two-group `shlz-ui` modal; apply the shared accessible close affordance to all app modals and verify HTTP/browser tests with Playwright, not the owner’s interactive browser.
- [x] 4.4 Executor adds unified deterministic history pagination/«Показать ещё», safe diff rendering and validation return path; verify multiple edits and >8-event browser scenario.
- [x] 4.5 Executor updates canonical migration/recovery inventories and runs planner-selected bounded focused checks plus applicable architecture/auth qualification; do not run local full `make test`/`make verify`.

## 5. Gate 5 and PR-ready delivery

- [x] 5.1 Root checks candidate completeness against the contract/plan, captures reconstructible exact source and dispatches an independent `gpt-5.6-sol / low` final reviewer with all required Gate 3/evidence artifacts.
- [x] 5.2 Resolve the reviewer’s complete findings list with changed code/tests re-reviewed as required; record an explicit `APPROVED` Gate 5 verdict for exact source.
- [x] 5.3 Preserve the validated prototype as primary evidence on its throwaway branch and record the owner decisions: unchanged card except icon, `shlz-ui` grouped modal, no editable Кшах and Playwright-only automated browser QA.
- [ ] 5.4 Commit/push the exact reviewed candidate, open the PR and run the selected existing exact-source GitHub CI consumer once; collect the complete failed-job and `REGRESSION_FAILURE` inventory before any correction.
- [ ] 5.5 Update the delivery record with exact source, authors, elapsed time, reviews/returns, focused commands, CI outcome and remaining UNKNOWN/deferred deployment; verify PR-ready status without merge or external deployment.

## 6. Done definition

- [ ] 6.1 Done requires the full editable-field matrix through the real owner and canonical Yii route, atomic append-only history and pagination, authorization/replay/concurrency, all named consumers on effective values, importer/schema/recovery preservation, approved required reviews and GREEN exact-source CI; the local stand prototype alone is not completion.

## 7. Production feedback hotfix

- [x] 7.1 Root adds a canonical browser RED regression proving that several distinct unchanged legacy speed representations do not enter a material-only patch or block its save.
- [x] 7.2 A separate executor implements the minimal generic changed-field submission without accepting an invalid changed value or special-casing one legacy code.
- [x] 7.3 Run bounded focused checks and obtain an independent final review of the exact hotfix source before publication/deployment.
