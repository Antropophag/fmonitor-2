## Why

Approved native selection/PDF ещё недоступны пользователю. Нужна рабочая форма
new_order/replace_pending без обязательного PDF и без alternate fresh writers.

## What Changes

ASSIGNMENT-ORDER-COMPOSITION-HTTP-001 доставляет real HTTP selection/query/PDF.
Actor FKR/manager; oracle approved native owners; seam canonical HTTP entrypoint.
Public shlz-ui используется по уточнению owner2026-09-07. Original upload/read,
application/opening и fresh bootstrap остаются отдельными следующим slices.

## Capabilities

### New Capabilities
- `pilot/assignment-order-composition-http`: form → native selection → optional PDF.

### Modified Capabilities

## Impact

Thin app/PilotHttp adapter + read query в composition owner; rapid router только
admission. Fresh deployment flag закрывает old POST writers без их migration.
No unresolved product semantics. Existing Gates5 не переоткрываются.
