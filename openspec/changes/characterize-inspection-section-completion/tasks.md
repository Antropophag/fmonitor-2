## 1. Gate 1 executable specification

- [x] 1.1 Create `specs/CHARACTERIZE-INSPECTION-SECTION-COMPLETION-001.md` with literal section-8 item/photo/completion HTTP fixtures, exact raw/projection/no-weight facts, replay/stale/ahead/repeat/concurrency/revoke contrasts, isolated rejection boundaries, browser-auto-enqueue source evidence, closed ownership and stable transcript; verify expected values are independent from future verifier and every pilot hazard is explicitly non-target.
- [ ] 1.2 Obtain and durably record owner approval for characterization-only scope; verify target UX, authorization, template-native readiness, correction/photo invalidation, payload conflict and repeat/concurrency policies remain excluded before RED.

## 2. Gates 2–3 minimal reviewed RED

- [ ] 2.1 Assign a fresh RED author to add the smallest test requiring production-composed GET/session/CSRF plus real item, photo and section-completion HTTP exchanges with independent raw/projection/no-extra-progress evidence; verify `tools/verification/run.sh red <test>` fails for missing oracle rather than setup.
- [ ] 2.2 Assign a different fresh independent test reviewer, record review under `reviews/tests/`, resolve findings without implementation-derived expectations, and verify explicit `APPROVED` before GREEN.
- [ ] 2.3 Implement only reviewed accepted lifecycle in a private fixture/verifier; verify unchanged minimal test turns GREEN twice with distinct tokens and production behavior/schema remain untouched.

## 3. Reviewed expansion and Gate 4 GREEN

- [ ] 3.1 Extend test under a new intended RED to exact/changed replay, lower-stale/ahead, distinct repeat, one winner-neutral two-server race, rejection matrix, last-photo revoke inconsistency, source-pinned browser auto-enqueue and deterministic cleanup; verify response-only fakes, mutable prerequisites, single-connection concurrency and GET/POST mutation confusion fail.
- [ ] 3.2 Assign another fresh independent test reviewer for expanded RED, record a versioned review, resolve every finding, and verify explicit `APPROVED` before expanded implementation.
- [ ] 3.3 Implement only reviewed expansion without changing checklist server/browser, application modules or production schema; verify unchanged expanded test turns GREEN twice with distinct clean tokens.
- [ ] 3.4 Register verifier once in canonical characterization; verify normalized transcript, bounded SQL/process/artifact cleanup, decoy preservation and setup/RED/regression classification.
- [ ] 3.5 Run focused characterization, item/photo/revoke/offline/template regressions, lint and `make architecture-check`; verify production DDL/SQL/rapid-pilot debt and hotspot baselines do not grow.

## 4. Gate 5 and Done

- [ ] 4.1 Add a target-backlog contrast note so future section completion cannot inherit automatic enqueue, hard-coded historical readiness, repeated facts or stale-photo completion; verify every unresolved target policy remains NEEDS_GRILL before target RED.
- [ ] 4.2 Assign a different fresh independent code reviewer that authored neither implementation increment and record review under `reviews/code/`; verify explicit `APPROVED` covers real-seam sensitivity, expectation independence, prerequisite immutability, concurrency/process safety, cleanup and non-promotion of pilot hazards.
- [ ] 4.3 Run canonical characterization and `make verify`, classify every failure, and mark Done only when reviewed GREEN evidence is durable and future target work cites it solely as explicit contrast.
